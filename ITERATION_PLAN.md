# 当前优化迭代计划

本文件是当前轮次的唯一任务板。长期流程见 `OPTIMIZATION_LOOP.md`，最终目标见 `GOAL.md`。

## 轮次元数据

| 字段 | 当前值 |
|---|---|
| 轮次 | `2026-08-06-R1` |
| 基线提交 | `fd8506fa6ae26b4dc9ce7873985e152756abb21b` |
| 规则 / SDK | gameplay `v0.14` / Python SDK `0.2.9` |
| 官方版本复核 | 2026-08-06，官方 source/version 与本地 bundle 一致 |
| 最终目标 | 长期得到最多资源 |
| 主代理 | 当前 Codex 主任务 |
| 专家模型 | 用户要求 Luna；当前接口未暴露，首轮临时降级为 `gpt-5.6-terra` |
| live 服务 | `io.arenahero.tactic`，只允许一个 `play.py` |

## 已完成的前置处置

### `OPS-001` 清除多客户端计划覆盖

- **状态**：`STATIC_VERIFIED -> LIVE_CANARY`
- **证据**：曾同时存在 PID `58672/58678/59898`，同一 Tick 被写入 2-5 次，且不同客户端
  生成了不同路线。所有客户端共享同一个 Agent 槽，后提交会完整覆盖前提交。
- **处置**：终止两个非 LaunchAgent 孤儿进程，重启受管服务以加载 `4ed9c8d`。
- **结果**：2026-08-06 00:18 +08:00 起仅 PID `67272`；新 `game.log` 从 `t57607`
  单条记录起算，并看到 `DEPOSIT_SUCCEEDED[1]`，无认证或协议错误。
- **限制**：旧日志含重复 Tick，不能作为 A/B 基线，只能用于故障取证。

## 当前任务队列

| 优先级 | 任务 | 状态 | 目的 | 执行顺序 |
|---:|---|---|---|---:|
| P0 | `OBS-001` 决策耗时与唯一 Tick 观测 | `ACCEPTED` | 建立可信性能/链路基线，行为零变化 | 1 |
| P1 | `RULE-001` 修复 `4ed9c8d` 规则与策略缺陷 | `STATIC_VERIFIED -> LIVE_CANARY` | 防止错误目标优先、资源争用和不可达军备目标 | 2 |
| P1 | `EXP-001` 历史资源与探索名额竞争 | `DISCOVERED` | 避免所有 Worker 被陈旧历史点占满 | 3 |
| P2 | `PERF-001` 前 K 候选 A* / EV | `DISCOVERED` | 降低全量搜索，升级往返收益估计 | 4 |

## 已批准任务：`OBS-001`

### 合同

- **负责人**：待用户安排外部执行 Agent；主代理只负责审查、验收与上线决定。
- **base SHA**：`4ed9c8d393e0b03e6fa0c147d5d926912deba28e`。
- **执行位置**：现有 `main` 工作区；后续修复不再新建分支或 worktree。
- **允许文件**：`play.py`、`meta/monitor.py`、`tests/test_tactic.py`。
- **禁止文件**：`.env`、`tactic.py`、`tactic_state.json`、所有运行日志和 LaunchAgent。
- **问题**：当前只有动作/事件计数，没有 `decide`、`submit`、总本地耗时和唯一 Tick 口径，
  无法证明持久地图增长后仍有足够的 15 秒窗口余量。
- **假设**：仅增加计时与解析不会改变计划；可用 500 个唯一 Tick证明 P95 决策耗时是否安全。
- **非目标**：不改变 Worker 分配、路径、战斗、生产、持久化或任何阈值。

### 实现与测试边界

1. 用单调时钟分别测量 `decide_ms`、`submit_ms` 和本地总耗时。
2. 日志只增加紧凑数值字段，不记录 API key、请求头、完整响应或异常对象。
3. monitor 按唯一 Tick 聚合，显式报告重复 Tick 数，不把重连重复状态当独立样本。
4. 输出 P50/P95/P99、窗口错误数、样本数；旧日志无新字段时保持兼容。
5. 新增解析、分位数、重复 Tick 和敏感信息测试；全量测试不得退化。

### 验收

- 静态：全量 pytest、compileall、锁文件检查、依赖检查、diff check 全通过。
- canary：唯一进程连续 20 个新 Tick；无认证、协议、提交和窗口错误。
- 基线：累计 500 个唯一成功 Tick，覆盖至少一次资源刷新；
  `plan_ms P50 < 250ms`、`P95 < 1000ms`、`P99 < 2000ms`。
- 停止：任一 `COMMAND_WINDOW_CLOSED`，或计时日志改变原计划内容，立即拒绝。

### 主代理验收记录（2026-08-06）

- **静态结果**：`100 passed`；`compileall`、`uv sync --locked --dry-run`、`uv pip check`、
  `git diff --check` 全部通过。日志新增固定错误码，失败 Tick、重复 Tick 与成功唯一 Tick
  分开统计；JSON 不再输出无界 Tick ID 集合。
- **canary 结果**：LaunchAgent `io.arenahero.tactic` 仅 PID `76462`；窗口 `t60286..t60305`
  共 20 个成功且唯一 Tick，重复 `0`，失败 `0`，`COMMAND_WINDOW_CLOSED` `0`，认证/协议/提交
  错误 `0`。`decide_ms` P50/P95/P99 为 `141/177.55/231.51ms`，满足计划门槛。
- **观察限制**：同窗只有 `1` 次交付、`0` 次采集，监控触发 `LOW_HARVEST`；当前战场仅有 1 个
  Worker，且另有未提交的 `tactic.py` 外部改动，本轮不将资源行为归因于 telemetry。500 个唯一
  成功 Tick、资源刷新覆盖和收益主指标尚未完成，因此状态保持 `OBSERVING`，禁止宣称优化完成。
- **代码交付**：`8cd6f20` 已推送到 `origin/codex/obs-001-timing`；治理文档基线合并提交待推送。

### `OBS-001` 最终观测验收（2026-08-06）

- **有效窗口**：唯一 LaunchAgent 进程产生 `t60445..t61016`，共 `564` 个成功唯一 Tick，重复
  `0`、失败 `0`、`COMMAND_WINDOW_CLOSED` `0`，认证/协议/提交错误 `0`；窗口包含可见资源刷新。
- **计时门槛**：`decide_ms` P50/P95/P99 为 `21/103.7/148.44ms`，严格低于
  `250/1000/2000ms`；提交网络耗时单独保留，不混入算法门槛。
- **监控修正**：移动失败按原因累计为 `41` 次 `MOVE_CONTESTED`（`CELL_UNIT_LIMIT` 为 `0`），
  不再漏计；`NO_RAID` 只由明确的敌 Core 标记触发。本窗口敌人可见 `117` Tick，其中敌 Core
  可见 `28` Tick，因此该告警仍然有效，不能把普通 Worker 误当作敌核证据。
- **边界**：资源链路只有 `7` 次采集、`5` 次交付，`EXP-001` 要求的 `50` 次完整采集入核样本
  尚未达到；`LOW_HARVEST` 仍需单独诊断，不能把 OBS-001 的完成解读为资源策略已最优。
- **结论**：OBS-001 已满足样本、链路、计时和唯一 Tick 门槛，状态改为 `ACCEPTED`；不批准
  `EXP-001` 进入参数实验。

## `RULE-001` 修复与验收

首轮专家在 `4ed9c8d` 发现三项需连续 Tick 集成测试确认的问题：

1. `_select_ranger_target` 仍先按“敌 Core”排序，新增的“距我方 Core <=2”并未压过敌核，
   与提交说明不一致。
2. Unit `HEAL` 在 Core 动作之前花资源；当前 Vanguard/Ranger 与修盾/生产没有共享资源预算，
   会排队可预见的 `INSUFFICIENT_RESOURCES`，并可能占住 Core 格影响交付和生产。
3. `_standing_army_targets(18/19)` 返回 `V1/R1`，但人口到 19 后 Core 停产；纯函数测试不能证明
   连续 Tick 真能重建两个兵种。

本次已补齐多目标 Ranger 与 Unit HEAL + Core action 资源账本测试；`W18/W19` 连续 Tick
生产和“带货 Worker 距 Core <=2 + Core 格低血战斗单位”的交付压力测试仍属于后续 live
验收项，不能用纯函数结果替代。

### `RULE-001` 主代理修复验收（2026-08-06）

- **静态状态**：`STATIC_VERIFIED`；全量测试 `104 passed`，`compileall`、锁文件 dry-run、
  `uv pip check`、`git diff --check` 均通过。
- **已修复**：近核两格敌人优先于远端敌 Core；同 Tick 交付入账后的资源先扣除按 UUID 排序的
  Unit HEAL 预留，再决定 Core 修盾/生产；可见敌人时取消不必要的 2 Worker 军备门槛，至少
  立即尝试建立 Vanguard。
- **安全边界**：HEAL 仍只针对 Core 同格、低血的 Vanguard/Ranger；pending deposit 只计入
  当前 Core 容量实际能接收的货物，不把溢出 cargo 当成可用资源。
- **canary 前状态**：旧进程仍运行的是 OBS-001 代码；重启后只验收新 Tick，不能把旧日志混入
  RULE-001 行为结论。主指标仍是唯一 Tick 的入核资源，规则修复不能替代 500 Tick 观察门槛。

### `RETURN-001` 载货 Worker 回程反向振荡修复（2026-08-06）

- **触发证据**：`t61479` 发现并分配资源 `[321,134]`，`t61485` 成功采集；载货 Worker
  在敌 Core 与障碍附近回程时可能重新进入最近离开的格子，直到 `t61517` 仍未入核。
- **根因**：载货回程的 A* 首步没有检查反向历史；回退路径还会无条件清空 `_pos_history`，使
  动态阻塞改变局部最优分支后反复走同一短环。
- **修复**：`_control_workers()` 对 A* 首步应用 `_avoid_set()`，命中最近历史格时改用避回头的
  贪心步；仅在真正卡死时清理历史。补充
  `test_laden_worker_return_does_not_backtrack_into_recent_cell`。
- **提交与静态验收**：提交 `df14f9a` 已快进到 `main` 并推送；全量 `112 passed`，编译、依赖和
  `git diff --check` 均通过。
- **新进程 canary**：LaunchAgent 唯一 PID `26495`，窗口 `t61524..t61545` 共 `22` 个唯一
  Tick，重复 `0`、失败 `0`、窗口错误 `0`、缺口 `0`；`decide_ms` P50/P95/P99 为
  `23.5/43.7/72.44ms`，Core 保持 `hp5/sh5`。窗口自然资源为 `0`，因此采集/交付样本为 `0`，
  只能证明链路和回程补丁未引入协议或时限回归，不能宣称资源收益已改善。
- **后续门槛**：继续在单实例 `main` 进程上累计至少 `200` 个唯一 Tick、`50` 次完整
  `HARVEST_SUCCEEDED -> DEPOSIT_SUCCEEDED`，再评估回程 P50/P95 和入核资源/Tick。

## 候选资源实验：`EXP-001`

- **当前实现状态**：`STATIC_VERIFIED -> LIVE_CANARY`。本轮先落地保护性修复，不把它当作收益
  实验结论：历史提示超过 256 Tick 不再派发；无可见资源且空闲 Worker 达 3 个时保留 1 个前沿
  探索者；日志记录 `stale/exp` 供归因。
- **静态证据（2026-08-06）**：112 个 pytest、compileall、`uv pip check`、`git diff --check`
  通过。监控同时修复 Tick 缺口、失败提交耗时和 `CORE_SPAWN_SUCCEEDED` 计数。
- **上线门槛**：重启后唯一 `play.py` 进程连续 20 Tick 无认证/协议/提交/窗口错误；随后至少
  200 个唯一 Tick、50 次历史分配和 50 个采集入核样本，再比较主指标，不足时保持观察态。

- **假设**：没有当前可见资源、空载 Worker 至少 3 个时，最多使用 `N-1` 个 Worker 追历史
  资源，保留一个稳定前沿探索者，会提高长期入核率。
- **为什么暂不执行**：当前唯一进程窗口已无重复 Tick，且已有 `470` 次历史资源分配；但只有
  `7` 次采集、`5` 次交付，Worker 的完整采集入核样本仍远低于 `50`，暂不能做资源策略归因。
- **进入条件**：`OBS-001` 完成；唯一进程基线至少 200 Tick、50 次历史资源分配和
  50 个完整采集到入核样本。
- **主指标**：入核资源 / 唯一 Tick。
- **次指标**：新探索格 / Tick、历史分配到采集 P50/P95、历史资源失败率。
- **回滚**：连续两个 100-Tick 窗口入核率下降 10%，或历史分配 P95 上升 25%，或失败率
  上升 5 个百分点。

## 暂缓方向

- `PERF-001`：只对 Worker 的前 K 个 Manhattan 候选计算/缓存 A*，再用
  `confidence * yield / (outbound + harvest + return + deposit_wait)` 排序。
- 人口 20+：没有自动 upkeep；生产价格按人口阶梯上涨。当前把人口 20 作为 Worker 的保守软目标，
  允许人口 19 生产第 20 个基础价 Unit；敌情或军备缺口下可继续补战斗单位。超过 20 的纯 Worker
  扩张仍需等边际 Worker 的实测增量入核率覆盖动态生产成本和防御风险。
- Core 满仓缓冲队列：先取得“距 Core 两格到入核”的 P50/P95，再决定是否实现。

## `RESOURCE-DISPATCH-002` 可见资源机会成本修复

- **问题证据**：`t61632..t61680` 多次看到 `[321,98]`，但旧进程的两个空载 Worker 中，
  离资源约 3 格者被“离 Core 最远”规则留作探索，远处 Worker 才被允许追踪；同时带货 Worker
  可能先触发 boxed 脱困而反向远离 Core。
- **本轮改动**：探索保留名额改按离当前可见资源最远者选择；带货 Worker 跳过探索 boxed 分支，
  直接使用回 Core 的 A*/贪心路径。新增两条调度回归测试，保留原有历史资源探索名额策略。
- **静态门槛**：119 个 pytest、compileall、`uv pip check`、`git diff --check` 全部通过。
- **上线 canary**：提交后重启唯一 `io.arenahero.tactic` 进程；先观察 20 个唯一 Tick，要求
  `res>0` 且存在空载 Worker 时不再出现无归因的 `a0/av0`，且无重复 Tick、窗口/提交/协议错误。
- **收益门槛**：至少 200 个唯一 Tick、50 条完整采集到交付链路后，比较可见资源采集延迟、
  采集到入核 P50/P95 和入核资源/Tick；不足时只认定为行为修复，不宣称收益最优。
- **回滚**：连续两个 100-Tick 窗口入核率下降 10%、采集到入核 P95 上升 25%、资源失败率
  上升 5 个百分点，或出现 Core/窗口/协议故障，恢复上一提交。

### 当前验收阻断（2026-08-06，协议阻断已解除）

- 原阻断是服务端已切换 v0.14 状态，而本地仍使用 SDK 0.2.8；`population_tier`、`upkeep_next_tick`
  已从新契约删除，旧客户端因此抛出 `ProtocolError`。
- 已同步官方 skill `a63222e` 与 SDK 0.2.9，严格模型字段和动态价格 helper 对齐。LaunchAgent
  仍保持停止，待新 SDK 下完成原始握手和 20 Tick canary；静态对齐不等于 live 收益已验证。

## 本轮完成定义

1. 所有批准任务由用户安排的外部执行 Agent 交付、主代理验收；主代理不派发或代写实现，
   执行 Agent 不直接 push/live。
2. 规则与 SDK 版本一致，唯一 live 进程，日志按唯一 Tick 统计。
3. 每项行为变化都有基线、样本量、主指标、安全护栏和回滚条件。
4. 验收结果更新本文件和 `LESSONS.md`，由主代理精准 commit、push 并核验 GitHub SHA。

## `RESOURCE-DISPATCH-003` 低采集率复审与持久地图保护

- **基线窗口**：`game.log.archive-pre-resource-optimization-20260807`，`t63430..t63490` 共
  `61` 个唯一 Tick；资源 `6/35 -> 8/35`，仅 `2` 次采集、`2` 次交付、`0` 次生产，Worker 长期为
  `W5/V2/R0`。认领 `51` 次，其中可见 `22`、历史 `29`，探索保留 `39` Tick；决策耗时 P50/P95/P99
  约为 `16/33/103ms`，主要瓶颈是资源兑现而不是本地计算。
- **根因证据**：历史资源提示长期参与调度，当前可见节点可能在认领排序与动态占位检查后错失采集窗口；
  探索目标只按信息增益排序，多个 Worker 会沿同一侧前沿移动；此外测试曾把默认
  `tactic_state.json` 覆盖成测试坐标，使重启后丢失真实地图提示。
- **本轮实现**：
  1. 至少一个战斗单位存在且无威胁时，将经济桥接上限从 `W5` 提至 `W7`，允许有限探索扩张；达到
     上限后重新为缺失 Ranger/Vanguard 储备资源，敌情下仍保持军备优先。
  2. 全局匹配前固定“当前可见资源格上的空载 Worker”，并在 Worker 数多于资源数时继续使用可见资源
     惩罚，保证最近现场 Worker 不会被历史提示或其他分配拉走。
  3. 空闲探索前沿加入八方向扇区计数，优先选择尚未被占用的扇区，降低东南聚集。
  4. `HARVEST_FAILED` 的资源不存在/耗尽原因立即清除对应历史提示；下一 Tick 若当前 state 重新出现该格，
     再按权威 state 恢复为可采资源。
  5. 测试夹具改用临时持久化文件，禁止离线测试污染 live 的 `tactic_state.json`。
- **静态结果**：`131 passed`；`compileall`、`uv pip check`、`git diff --check` 均通过。
- **上线前处理**：提交前备份并核验 `tactic_state.json`，只启动一个 `io.arenahero.tactic`；不把测试
  产生的状态文件当作地图事实。
- **Live 验收**：先观察连续 `20` 个唯一 Tick，要求无认证/协议/提交/窗口错误且 Core 存活；随后累计
  `200` 个唯一 Tick、至少 `50` 条 `HARVEST_SUCCEEDED -> DEPOSIT_SUCCEEDED` 完整链路，再比较入核资源/Tick。
- **接受条件**：两个独立 `100` Tick 窗口的入核资源/Tick 均不低于基线，且完整样本提高至少 `5%`；同时
  决策 P95 `<1000ms`、无重复 Tick、资源失败率不升高 `5` 个百分点。
- **立即回滚**：出现第二个 `play.py`、窗口/协议错误、Core 受损，或连续两个 `100` Tick 窗口入核率低于
  基线 `10%`；回滚只恢复上一个战术提交，不删除持久地图备份。

## `RESOURCE-DISPATCH-004` 资源干旱下的自适应前沿扩张

- **新证据**：`game.log` 的 `t63559..t63588` 共 `30` 个唯一 Tick，`W5 -> W6/V2` 后仍为
  `0` 次采集、`0` 次交付；没有错误、敌情或移动失败。持久地图只有 `599` 个已探索格，资源提示为
  空，Worker 主要在 Core 附近的 `x340..383/y97..134` 旧边界移动。
- **规则约束**：v0.14 自然节点按 chunk 配额每四个逻辑 Tick 补足，服务端不发送雾区坐标；资源提示为空
  只能说明当前没有已确认节点，不能说明全图没有资源。带货 Worker 仍必须优先回 Core。
- **本轮实现**：
  1. 增加资源干旱计数；无可见且无有效历史资源时，每 `8` Tick 将空载 Worker 的前沿半径扩大 `16`
     格，从常态 `40` 格逐步到 `96` 格，覆盖邻近 chunk 的补给候选。
  2. 一旦看到当前资源或有效历史提示，半径立即收回 `40` 格；超过半径的带货 Worker 不走扩张分支，
     继续使用 A* 回 Core，保护交付效率。
  3. 保持现有八方向扇区分散、可见资源固定派单和历史提示冷却逻辑不变，避免把本轮收益归因到多个变量。
- **静态结果**：`133 passed`；`compileall`、`uv pip check`、`git diff --check` 通过。
- **Canary**：重启后先观察 `20` 个唯一 Tick；要求单进程、无协议/认证/提交/窗口错误、Core 存活，
  并记录干旱半径阶段。随后至少 `200` 个唯一 Tick、`50` 条完整采集到交付链路再比较入核资源/Tick。
- **接受条件**：两个独立 `100` Tick 窗口的入核资源/Tick 均不低于 `RESOURCE-DISPATCH-003` 基线；
  扩张期间无 `MOVE_FAILED` 增长超过 `5` 个百分点，带货回 Core P95 不超过基线 `25%`。
- **立即回滚**：出现第二个 `play.py`、Core 受损、扩张后连续两个 `100` Tick 窗口入核率下降 `10%`，
  或带货回程 P95 超过基线 `25%`；只恢复上一策略提交，不删除地图备份。

## `RESOURCE-DISPATCH-005` 和平期扩大 Worker 发现桥

- **基线证据**：`t63617..t63675` 的单实例窗口无协议、提交或移动故障；已确认 4 次采集、3 次交付，
  但 Core 长期停在 `W6/V2/R0`。可见资源在 Core 外约 30--45 格，真正的瓶颈是发现与兑现周期，
  不是决策耗时。
- **本轮实现**：无敌情且已有战斗单位时，将有限经济桥从 `W7` 放宽到 `W8`，仍低于人口 20 的动态价格
  边界；任意可见敌人（包括远处敌 Core）都会关闭桥接，优先保留攻击单位预算。
- **静态结果**：`135 passed`，compileall 与 `git diff --check` 通过。
- **Live 验收**：重启后先观察 20 个唯一 Tick，确认进程单实例、无协议/提交/窗口错误；随后观察是否由
  `W6` 增长到 `W8`，并累计至少 100 个唯一 Tick、20 条完整采集到入核链路，再与 `RESOURCE-DISPATCH-004`
  窗口比较入核资源/Tick。
- **接受条件**：和平窗口的 Worker 达到 `W8` 且可见资源仍优先采集；两个独立 100 Tick 窗口入核率不低于
  004 基线，决策 P95 < 1000ms，资源失败率不增加 5 个百分点。
- **立即回滚**：出现敌情下仍生产 Worker、Core/协议/窗口故障，或连续两个 100 Tick 窗口入核率下降
  10%；回滚只恢复上一策略提交，不删除持久地图备份。

## `RESOURCE-DISPATCH-006` 延长和平期 Worker 经济桥

- **新日志证据**：`game.log` 的 `t63692..t63744` 共 `53` 个唯一 Tick，只有 `3` 次采集和 `3` 次交付；
  资源点实际出现在距离 Core 约 `30` 和 `41` 格的位置，决策 P95 仍低于 `1s`，无窗口、协议、认证或
  移动争用故障。`W7/V2/R0` 在窗口末仍未形成 Ranger，发现并发和带货回程是主要限制。
- **本轮实现**：无可见敌情且已有战斗单位时，将经济桥上限由 `W8` 提至 `W12`。`W12/V2` 的人口为 `14`，
  仍低于人口 `20` 的首个动态生产价格边界；任意可见敌人或敌 Core 仍会立即关闭桥接并优先补攻击单位。
- **静态验收**：新增 W11->W12 生产与 W12 停桥测试；需通过全量测试、compileall、依赖检查和 diff 检查。
- **Live canary**：重启后观察 `20` 个唯一 Tick，确认单实例、无窗口/协议/认证错误；随后观察是否能由
  `W7` 增长至 `W12`，并累计 `200` 个唯一 Tick、至少 `50` 条完整采集到入核链路，再与 `RESOURCE-DISPATCH-005`
  窗口比较入核资源/Tick。
- **接受条件**：两个独立 `100` Tick 窗口入核资源/Tick 不低于 005 基线，完整链路收益至少提高 `5%`；
  `decide_ms` P95 `<1000ms`，资源失败率不增加 `5` 个百分点，且敌情下没有 Worker 桥接生产。
- **立即回滚**：出现第二个 `play.py`、Core/协议/窗口故障，敌情误产 Worker，或连续两个 `100` Tick 窗口
  入核率低于 005 基线 `10%`；只恢复上一策略提交，不删除持久地图备份。

## `RESOURCE-DISPATCH-007` 历史提示不能屏蔽干旱扩张

- **当前证据**：最新单实例窗口 `t63753..t63808`（观察时）无重复 Tick、无提交/移动失败，
  但 `56` 个 Tick 只有 `3` 次采集、`4` 次交付；其中 `t63764..t63784` 连续 `21` Tick
  没有当前可见资源，而历史分配 `ah` 仍为 `2-3`。历史提示的存在让旧干旱计数一直为零，
  探索半径没有从 `40` 格进入扩张阶段。
- **根因**：`_observe_resources()` 把“存在未过期历史提示”当成当前有资源。规则只把记忆节点定义为
  stale hint；它不能证明本 Tick 仍有可采节点。该条件会让一条失效提示永久压住自适应前沿。
- **本轮修复**：干旱计数仅由 `turn.resource_cells`（当前完整状态中的可见资源）复位；历史提示仍可
  参与有界派单，但不会阻止每 `8` Tick 将空载探索半径扩大 `16` 格（上限 `96`）。带货 Worker
  仍固定回 Core，不进入扩张分支。
- **静态验收**：`138 passed`，`compileall` 与 `git diff --check` 通过；新增回归测试验证“仅有历史
  提示且当前无资源”仍会累积干旱计数并进入扩张半径。
- **Live canary**：重启前备份 `game.log` 与 `tactic_state.json`，确保只有一个 `play.py`；先观察
  `20` 个唯一 Tick 无协议/认证/提交/窗口错误，再累计 `200` 个唯一 Tick 和 `50` 条完整
  `HARVEST_SUCCEEDED -> DEPOSIT_SUCCEEDED` 链路。当前 `56` Tick 短样本只证明瓶颈，不宣称收益。
- **主指标**：入核资源/唯一 Tick；同时记录无可见资源连续长度、`harvest -> deposit` P50/P95、
  `ah` 历史分配占比、新探索格/Tick 和 `MOVE_FAILED` 比例。
- **接受条件**：两个独立 `100` Tick 窗口入核率均不低于 `RESOURCE-DISPATCH-006` 基线，完整样本
  提升至少 `5%`；决策 P95 `<1000ms`，无重复 Tick，资源失败率不增加 `5` 个百分点，带货回程
  P95 不恶化 `25%`。
- **立即回滚**：出现第二个 `play.py`、Core/协议/窗口故障，或连续两个 `100` Tick 窗口入核率
  低于基线 `10%`；只恢复上一策略提交，不删除持久地图备份。

## `RESOURCE-DISPATCH-008` 可见资源认领稳定化（当前 LIVE_CANARY）

- **触发证据**：`game.log` 的 `t63830..t63890` 共 `61` 个唯一 Tick，只有 `3` 次实际采集、
  `5` 次交付；无敌情时 `av` 认领在相邻 Tick 反复变化，资源节点可能连续可见而采集者重新换主。
  决策 P95 远低于命令窗口，瓶颈仍在资源兑现，不在本地计算。
- **可证伪假设**：全局匹配每 Tick 重新按距离选 Worker，会让已经接近当前可见节点的 Worker 被更近的
  同行替换，形成目标漂移；把当前可见节点的认领保留到采集/失败/确认消失，可降低到达延迟并提高完整链路率。
- **实现边界**：新增内存态 `_resource_claims`；仅保存活着且空载 Worker 的短期坐标意图，不写入持久地图。
  当前可见认领优先保留；历史认领遇到当前可见节点会释放，且历史资源仍保留一个前沿探索名额。
  `HARVEST_SUCCEEDED`、资源失败和可见空格确认会清除对应认领。带货回程、Core 生产、敌情判断不变。
- **静态验收**：`139 passed`，`compileall`、`uv pip check`、`git diff --check` 通过；新增测试覆盖
  “第二个 Worker 变近时当前资源认领仍不换主”。
- **Canary**：`2026-08-07 02:58:41` 重启唯一实例，当前 PID `64255`；首个新 Tick 窗口出现采集成功，
  暂未观察到协议/认证/提交/窗口错误。继续累计至少 `20` 个唯一 Tick 后再判定安全，通过后再收集
  `200` Tick / `50` 条完整采集到入核链路。
- **主指标与护栏**：入核资源/唯一 Tick、可见资源出现到采集的 Tick 数、完整采集到交付 P50/P95；
  无重复 Tick、无 `MOVE_FAILED` 增长、无 Core 受损、`decide_ms` P95 `<1000ms`。
- **回滚**：连续两个 `100` Tick 窗口入核率低于 `RESOURCE-DISPATCH-007` 基线 `10%`，或采集延迟
  P95 上升 `25%`、资源失败率上升 `5` 个百分点、出现协议/窗口/Core 故障时，恢复上一策略提交；
  不删除本轮运行备份。

## `RESOURCE-DISPATCH-009` 交付通道预约与复合事件观测修复（待 LIVE_CANARY）

- **当前证据**：唯一进程窗口 `t63903..t63966` 共 `64` 个 Tick，`8` 次采集、`7` 次交付、
  `1` 次 Worker 生产，资源 `4/55 -> 6/60`，净增只有 `2`；移动成功 `576` 次、
  `CELL_UNIT_LIMIT` 失败 `2` 次、`MOVE_CONTESTED=0`。主要瓶颈仍是资源点到 Core 的长距离兑现，
  但空载 Worker 与带货回程共享下一格会产生可避免的尾延迟。
- **实现**：
  1. 历史资源认领增加 `6` 格迟滞；只有当前可见节点明显更近时才换主，避免已经走近的 Worker
     因短暂可见节点掉头；仍保留可见资源正常匹配和失败/消失清理。
  2. `_control_workers` 保留 UUID 稳定探索编号，但在执行顺序上先规划带货 Worker，并把每个
     Worker 的下一格加入本 Tick 预约集合，后续 Worker 避开已预约目的地和 Core 交付格。
  3. `meta/monitor.py` 的事件字段改按 `] plan[` 外层分隔解析，修复带金额方括号的复合事件漏计。
- **静态验收**：`143 passed`，`compileall`、`uv pip check`、`git diff --check` 通过。
- **LIVE_CANARY**：重启后先观察 `20` 个唯一 Tick，要求单进程、无协议/认证/提交/窗口错误、
  Core 存活；随后累计 `200` 个唯一 Tick、`50` 条完整采集到入核链路，再比较 `deposit/tick`、
  `harvest -> deposit` P50/P95、`CELL_UNIT_LIMIT` 和 `MOVE_CONTESTED`。
- **接受/回滚**：两个独立 `100` Tick 窗口入核率不低于 `RESOURCE-DISPATCH-008` 基线，移动失败
  率不增加 `5` 个百分点，决策 P95 `<1000ms`；若连续两个窗口入核率低 `10%`、采集延迟 P95
  上升 `25%`、Core 受损或出现第二个 `play.py`，恢复上一提交，不删除运行备份。

## `RESOURCE-DISPATCH-010` 历史远端提示限距与 Worker 桥接上限（待 LIVE_CANARY）

- **当前证据**：唯一进程 `t63903..t64061` 的连续窗口没有协议、提交或窗口错误，移动成功约
  `1593` 次、`CELL_UNIT_LIMIT` 仅 `6` 次，但采集约 `18` 次、交付约 `17` 次；历史认领累计
  `145` 次，明显高于可见认领。采集距离与回程延迟相关，但当前可见远点仍有兑现价值，不能用
  `30` 格硬过滤全部资源。
- **实现**：健康期只主动派发距 Core 不超过 `30` 格的非可见历史提示；连续 `24` Tick 没有
  `HARVEST_SUCCEEDED` 时放宽到 `65` 格。当前可见资源、已在资源格的 Worker、以及已经在路上的
  短期认领不受该门槛影响；距离超过半径但已有 Worker 在 `4` 格内的历史点也保留，以便就近兑现。
  新增 `eco[..farN..]` 遥测统计被限距的历史候选。和平期 Worker 目标取人口预算和
  `ECONOMY_BRIDGE_MAX_WORKERS` 的较小值，Army 已齐时不再从 W12 继续长到 W18。
- **静态验收**：`147 passed`，`compileall`、`uv pip check`、`git diff --check` 通过；新增健康期
  远端保留/不派发、干旱放宽、可见远点、就近兑现和 W12 上限回归测试。
- **LIVE_CANARY**：重启前备份 `game.log`、`tactic_state.json`，确认唯一 `play.py`；重启后先看
  `50` 个唯一 Tick，比较 `far` 过滤量、`harvest/tick`、`deposit/tick`、完整采集到入核延迟和
  `CELL_UNIT_LIMIT`。累计 `200` 个唯一 Tick 后再决定是否扩大搜索半径或调整 Worker 数。
- **接受/回滚**：Core 必须存活且无协议/提交/窗口错误；若连续两个 `100` Tick 窗口入核率比
  `RESOURCE-DISPATCH-009` 低 `10%`、采集延迟 P95 上升 `25%`、`far` 过滤后出现连续 `24`
  Tick 无采集，或出现第二个客户端，恢复上一提交并保留运行备份。

## `RESOURCE-DISPATCH-011` 和平期军备库存闸门与空载 Core 格保护（当前 STATIC_VERIFIED）

- **当前证据**：单进程窗口 `t64097..t64165` 共 69 个唯一 Tick，7 次采集、9 次交付；和平期
  Ranger 生产把库存从 `13` 降到 `1`，窗口末仅回升到 `5`。同窗另有 1 次
  `CORE_SPAWN_FAILED.CELL_UNIT_LIMIT`，对应空载 Worker 误入 Core 格。
- **可证伪假设**：已有 Vanguard 防线且无敌人时，Ranger 生产后的库存下限可以减少净资源
  断崖，而不会影响敌情下的即时攻击生产；空载 Worker 的最终兜底阻塞 Core 后，生产失败应为零。
- **实现范围**：只修改 `tactic.py` 与 `tests/test_tactic.py`。和平期补齐 Ranger 前至少保留
  5 点库存；可见敌人时保留为 0。空载 Worker 的探索/脱困兜底一律把 Core 格视为阻塞，带货
  Worker 的交付路径不变。
- **静态验收**：`150 passed`；`compileall`、`uv pip check`、`git diff --check`、敏感信息
  扫描通过。
- **Canary 门槛**：重启前备份 `game.log` 与 `tactic_state.json`，只允许一个 `play.py`；先看
  20 个唯一 Tick，要求无协议、认证、提交、窗口错误且 Core 存活；随后累计至少 100 个唯一
  Tick 和 20 条采集入核链路，再比较库存净增。
- **主指标**：和平窗口入核资源/Tick、库存净增和 Ranger 生产后的最低库存。
- **安全护栏**：敌情出现后下一可生产 Tick 不得因库存闸门跳过攻击单位；`CELL_UNIT_LIMIT`、
  Core 受损、移动/资源失败率不得上升；决策 P95 仍低于 1000ms。
- **回滚**：连续两个 100-Tick 窗口入核资源/Tick 低于当前基线 10%，敌情下攻击生产延迟，
  出现 Core/协议/窗口故障，或新增 Core 格生产失败，即恢复上一策略提交；不删除地图备份。
