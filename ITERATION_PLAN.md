# 当前优化迭代计划

本文件是当前轮次的唯一任务板。长期流程见 `OPTIMIZATION_LOOP.md`，最终目标见 `GOAL.md`。

## 轮次元数据

| 字段 | 当前值 |
|---|---|
| 轮次 | `2026-08-07-R2` |
| 基线提交 | `e45cec0` |
| 规则 / SDK | gameplay `v0.14` / Python SDK `0.2.9` |
| 官方版本复核 | 2026-08-06，官方 source/version 与本地 bundle 一致 |
| 最终目标 | 长期得到最多资源 |
| 主代理 | 当前 Codex 主任务 |
| 专家模型 | 用户要求 Luna；当前接口未暴露，首轮临时降级为 `gpt-5.6-terra` |
| live 服务 | `io.arenahero.tactic`，只允许一个 `play.py` |

## 已完成的前置处置

### `OPS-001` 清除多客户端计划覆盖

- **状态**：`STATIC_VERIFIED -> LIVE_CANARY`
- **证据**：曾同时存在 PID `58672/58678/59898`，同一 Tick 被写入 2-5 次，且不同客户端
  生成了不同路线。所有客户端共享同一个 Agent 槽，后提交会完整覆盖前提交。
- **处置**：终止两个非 LaunchAgent 孤儿进程，重启受管服务以加载 `4ed9c8d`。
- **结果**：2026-08-06 00:18 +08:00 起仅 PID `67272`；新 `game.log` 从 `t57607`
  单条记录起算，并看到 `DEPOSIT_SUCCEEDED[1]`，无认证或协议错误。
- **限制**：旧日志含重复 Tick，不能作为 A/B 基线，只能用于故障取证。

## 当前任务队列

| 优先级 | 任务 | 状态 | 目的 | 执行顺序 |
|---:|---|---|---|---:|
| P0 | `OBS-001` 决策耗时与唯一 Tick 观测 | `ACCEPTED` | 建立可信性能/链路基线，行为零变化 | 1 |
| P1 | `RULE-001` 修复 `4ed9c8d` 规则与策略缺陷 | `STATIC_VERIFIED -> LIVE_CANARY` | 防止错误目标优先、资源争用和不可达军备目标 | 2 |
| P1 | `EXP-001` 历史资源与探索名额竞争 | `DISCOVERED` | 避免所有 Worker 被陈旧历史点占满 | 3 |
| P2 | `PERF-001` 前 K 候选 A* / EV | `DISCOVERED` | 降低全量搜索，升级往返收益估计 | 4 |
| P0 | `RULE-002` 战斗执行链与 Core 生存 | `IN_PROGRESS` | 让可逆突袭真正抵达并开火，合法攻击优先于治疗，Core HP 自动恢复 | 5 |
| P1 | `OBS-002` Manual 归因与资源成本遥测 | `STATIC_VERIFIED` | 将 Manual Tick 从 Agent KPI 剥离，并记录 v0.14 成本/收益 | 6 |

## 已批准任务：`OBS-001`

### 合同

- **负责人**：待用户安排外部执行 Agent；主代理只负责审查、验收与上线决定。
- **base SHA**：`4ed9c8d393e0b03e6fa0c147d5d926912deba28e`。
- **执行位置**：现有 `main` 工作区；后续修复不再新建分支或 worktree。
- **允许文件**：`play.py`、`meta/monitor.py`、`tests/test_tactic.py`。
- **禁止文件**：`.env`、`tactic.py`、`tactic_state.json`、所有运行日志和 LaunchAgent。
- **问题**：当前只有动作/事件计数，没有 `decide`、`submit`、总本地耗时和唯一 Tick 口径，
  无法证明持久地图增长后仍有足够的 15 秒窗口余量。
- **假设**：仅增加计时与解析不会改变计划；可用 500 个唯一 Tick证明 P95 决策耗时是否安全。
- **非目标**：不改变 Worker 分配、路径、战斗、生产、持久化或任何阈值。

### 实现与测试边界

1. 用单调时钟分别测量 `decide_ms`、`submit_ms` 和本地总耗时。
2. 日志只增加紧凑数值字段，不记录 API key、请求头、完整响应或异常对象。
3. monitor 按唯一 Tick 聚合，显式报告重复 Tick 数，不把重连重复状态当独立样本。
4. 输出 P50/P95/P99、窗口错误数、样本数；旧日志无新字段时保持兼容。
5. 新增解析、分位数、重复 Tick 和敏感信息测试；全量测试不得退化。

### 验收

- 静态：全量 pytest、compileall、锁文件检查、依赖检查、diff check 全通过。
- canary：唯一进程连续 20 个新 Tick；无认证、协议、提交和窗口错误。
- 基线：累计 500 个唯一成功 Tick，覆盖至少一次资源刷新；
  `plan_ms P50 < 250ms`、`P95 < 1000ms`、`P99 < 2000ms`。
- 停止：任一 `COMMAND_WINDOW_CLOSED`，或计时日志改变原计划内容，立即拒绝。

### 主代理验收记录（2026-08-06）

- **静态结果**：`100 passed`；`compileall`、`uv sync --locked --dry-run`、`uv pip check`、
  `git diff --check` 全部通过。日志新增固定错误码，失败 Tick、重复 Tick 与成功唯一 Tick
  分开统计；JSON 不再输出无界 Tick ID 集合。
- **canary 结果**：LaunchAgent `io.arenahero.tactic` 仅 PID `76462`；窗口 `t60286..t60305`
  共 20 个成功且唯一 Tick，重复 `0`，失败 `0`，`COMMAND_WINDOW_CLOSED` `0`，认证/协议/提交
  错误 `0`。`decide_ms` P50/P95/P99 为 `141/177.55/231.51ms`，满足计划门槛。
- **观察限制**：同窗只有 `1` 次交付、`0` 次采集，监控触发 `LOW_HARVEST`；当前战场仅有 1 个
  Worker，且另有未提交的 `tactic.py` 外部改动，本轮不将资源行为归因于 telemetry。500 个唯一
  成功 Tick、资源刷新覆盖和收益主指标尚未完成，因此状态保持 `OBSERVING`，禁止宣称优化完成。
- **代码交付**：`8cd6f20` 已推送到 `origin/codex/obs-001-timing`；治理文档基线合并提交待推送。

### `OBS-001` 最终观测验收（2026-08-06）

- **有效窗口**：唯一 LaunchAgent 进程产生 `t60445..t61016`，共 `564` 个成功唯一 Tick，重复
  `0`、失败 `0`、`COMMAND_WINDOW_CLOSED` `0`，认证/协议/提交错误 `0`；窗口包含可见资源刷新。
- **计时门槛**：`decide_ms` P50/P95/P99 为 `21/103.7/148.44ms`，严格低于
  `250/1000/2000ms`；提交网络耗时单独保留，不混入算法门槛。
- **监控修正**：移动失败按原因累计为 `41` 次 `MOVE_CONTESTED`（`CELL_UNIT_LIMIT` 为 `0`），
  不再漏计；`NO_RAID` 只由明确的敌 Core 标记触发。本窗口敌人可见 `117` Tick，其中敌 Core
  可见 `28` Tick，因此该告警仍然有效，不能把普通 Worker 误当作敌核证据。
- **边界**：资源链路只有 `7` 次采集、`5` 次交付，`EXP-001` 要求的 `50` 次完整采集入核样本
  尚未达到；`LOW_HARVEST` 仍需单独诊断，不能把 OBS-001 的完成解读为资源策略已最优。
- **结论**：OBS-001 已满足样本、链路、计时和唯一 Tick 门槛，状态改为 `ACCEPTED`；不批准
  `EXP-001` 进入参数实验。

## `RULE-001` 修复与验收

首轮专家在 `4ed9c8d` 发现三项需连续 Tick 集成测试确认的问题：

1. `_select_ranger_target` 仍先按“敌 Core”排序，新增的“距我方 Core <=2”并未压过敌核，
   与提交说明不一致。
2. Unit `HEAL` 在 Core 动作之前花资源；当前 Vanguard/Ranger 与修盾/生产没有共享资源预算，
   会排队可预见的 `INSUFFICIENT_RESOURCES`，并可能占住 Core 格影响交付和生产。
3. `_standing_army_targets(18/19)` 返回 `V1/R1`，但人口到 19 后 Core 停产；纯函数测试不能证明
   连续 Tick 真能重建两个兵种。

本次已补齐多目标 Ranger 与 Unit HEAL + Core action 资源账本测试；`W18/W19` 连续 Tick
生产和“带货 Worker 距 Core <=2 + Core 格低血战斗单位”的交付压力测试仍属于后续 live
验收项，不能用纯函数结果替代。

### `RULE-001` 主代理修复验收（2026-08-06）

- **静态状态**：`STATIC_VERIFIED`；全量测试 `104 passed`，`compileall`、锁文件 dry-run、
  `uv pip check`、`git diff --check` 均通过。
- **已修复**：近核两格敌人优先于远端敌 Core；同 Tick 交付入账后的资源先扣除按 UUID 排序的
  Unit HEAL 预留，再决定 Core 修盾/生产；可见敌人时取消不必要的 2 Worker 军备门槛，至少
  立即尝试建立 Vanguard。
- **安全边界**：HEAL 仍只针对 Core 同格、低血的 Vanguard/Ranger；pending deposit 只计入
  当前 Core 容量实际能接收的货物，不把溢出 cargo 当成可用资源。
- **canary 前状态**：旧进程仍运行的是 OBS-001 代码；重启后只验收新 Tick，不能把旧日志混入
  RULE-001 行为结论。主指标仍是唯一 Tick 的入核资源，规则修复不能替代 500 Tick 观察门槛。

### `RETURN-001` 载货 Worker 回程反向振荡修复（2026-08-06）

- **触发证据**：`t61479` 发现并分配资源 `[321,134]`，`t61485` 成功采集；载货 Worker
  在敌 Core 与障碍附近回程时可能重新进入最近离开的格子，直到 `t61517` 仍未入核。
- **根因**：载货回程的 A* 首步没有检查反向历史；回退路径还会无条件清空 `_pos_history`，使
  动态阻塞改变局部最优分支后反复走同一短环。
- **修复**：`_control_workers()` 对 A* 首步应用 `_avoid_set()`，命中最近历史格时改用避回头的
  贪心步；仅在真正卡死时清理历史。补充
  `test_laden_worker_return_does_not_backtrack_into_recent_cell`。
- **提交与静态验收**：提交 `df14f9a` 已快进到 `main` 并推送；全量 `112 passed`，编译、依赖和
  `git diff --check` 均通过。
- **新进程 canary**：LaunchAgent 唯一 PID `26495`，窗口 `t61524..t61545` 共 `22` 个唯一
  Tick，重复 `0`、失败 `0`、窗口错误 `0`、缺口 `0`；`decide_ms` P50/P95/P99 为
  `23.5/43.7/72.44ms`，Core 保持 `hp5/sh5`。窗口自然资源为 `0`，因此采集/交付样本为 `0`，
  只能证明链路和回程补丁未引入协议或时限回归，不能宣称资源收益已改善。
- **后续门槛**：继续在单实例 `main` 进程上累计至少 `200` 个唯一 Tick、`50` 次完整
  `HARVEST_SUCCEEDED -> DEPOSIT_SUCCEEDED`，再评估回程 P50/P95 和入核资源/Tick。

## 候选资源实验：`EXP-001`

- **当前实现状态**：`STATIC_VERIFIED -> LIVE_CANARY`。本轮先落地保护性修复，不把它当作收益
  实验结论：历史提示超过 256 Tick 不再派发；无可见资源且空闲 Worker 达 3 个时保留 1 个前沿
  探索者；日志记录 `stale/exp` 供归因。
- **静态证据（2026-08-06）**：112 个 pytest、compileall、`uv pip check`、`git diff --check`
  通过。监控同时修复 Tick 缺口、失败提交耗时和 `CORE_SPAWN_SUCCEEDED` 计数。
- **上线门槛**：重启后唯一 `play.py` 进程连续 20 Tick 无认证/协议/提交/窗口错误；随后至少
  200 个唯一 Tick、50 次历史分配和 50 个采集入核样本，再比较主指标，不足时保持观察态。

- **假设**：没有当前可见资源、空载 Worker 至少 3 个时，最多使用 `N-1` 个 Worker 追历史
  资源，保留一个稳定前沿探索者，会提高长期入核率。
- **为什么暂不执行**：当前唯一进程窗口已无重复 Tick，且已有 `470` 次历史资源分配；但只有
  `7` 次采集、`5` 次交付，Worker 的完整采集入核样本仍远低于 `50`，暂不能做资源策略归因。
- **进入条件**：`OBS-001` 完成；唯一进程基线至少 200 Tick、50 次历史资源分配和
  50 个完整采集到入核样本。
- **主指标**：入核资源 / 唯一 Tick。
- **次指标**：新探索格 / Tick、历史分配到采集 P50/P95、历史资源失败率。
- **回滚**：连续两个 100-Tick 窗口入核率下降 10%，或历史分配 P95 上升 25%，或失败率
  上升 5 个百分点。

## 暂缓方向

- `PERF-001`：只对 Worker 的前 K 个 Manhattan 候选计算/缓存 A*，再用
  `confidence * yield / (outbound + harvest + return + deposit_wait)` 排序。
- 人口 20+：没有自动 upkeep；生产价格按人口阶梯上涨。当前把人口 20 作为 Worker 的保守软目标，
  允许人口 19 生产第 20 个基础价 Unit；敌情或军备缺口下可继续补战斗单位。超过 20 的纯 Worker
  扩张仍需等边际 Worker 的实测增量入核率覆盖动态生产成本和防御风险。
- Core 满仓缓冲队列：先取得“距 Core 两格到入核”的 P50/P95，再决定是否实现。

## `RESOURCE-DISPATCH-002` 可见资源机会成本修复

- **问题证据**：`t61632..t61680` 多次看到 `[321,98]`，但旧进程的两个空载 Worker 中，
  离资源约 3 格者被“离 Core 最远”规则留作探索，远处 Worker 才被允许追踪；同时带货 Worker
  可能先触发 boxed 脱困而反向远离 Core。
- **本轮改动**：探索保留名额改按离当前可见资源最远者选择；带货 Worker 跳过探索 boxed 分支，
  直接使用回 Core 的 A*/贪心路径。新增两条调度回归测试，保留原有历史资源探索名额策略。
- **静态门槛**：119 个 pytest、compileall、`uv pip check`、`git diff --check` 全部通过。
- **上线 canary**：提交后重启唯一 `io.arenahero.tactic` 进程；先观察 20 个唯一 Tick，要求
  `res>0` 且存在空载 Worker 时不再出现无归因的 `a0/av0`，且无重复 Tick、窗口/提交/协议错误。
- **收益门槛**：至少 200 个唯一 Tick、50 条完整采集到交付链路后，比较可见资源采集延迟、
  采集到入核 P50/P95 和入核资源/Tick；不足时只认定为行为修复，不宣称收益最优。
- **回滚**：连续两个 100-Tick 窗口入核率下降 10%、采集到入核 P95 上升 25%、资源失败率
  上升 5 个百分点，或出现 Core/窗口/协议故障，恢复上一提交。

### 历史验收阻断（2026-08-06，协议阻断已解除）

- 原阻断是服务端已切换 v0.14 状态，而本地仍使用 SDK 0.2.8；`population_tier`、`upkeep_next_tick`
  已从新契约删除，旧客户端因此抛出 `ProtocolError`。这是历史取证，不是当前版本声明。
- 已同步官方 skill `a63222e` 与 SDK 0.2.9，严格模型字段和动态价格 helper 对齐。LaunchAgent
  仍保持停止，待新 SDK 下完成原始握手和 20 Tick canary；静态对齐不等于 live 收益已验证。

## 本轮完成定义

1. 所有批准任务由用户安排的外部执行 Agent 交付、主代理验收；主代理不派发或代写实现，
   执行 Agent 不直接 push/live。
2. 规则与 SDK 版本一致，唯一 live 进程，日志按唯一 Tick 统计。
3. 每项行为变化都有基线、样本量、主指标、安全护栏和回滚条件。
4. 验收结果更新本文件和 `LESSONS.md`，由主代理精准 commit、push 并核验 GitHub SHA。

## `RESOURCE-DISPATCH-003` 低采集率复审与持久地图保护

- **基线窗口**：`game.log.archive-pre-resource-optimization-20260807`，`t63430..t63490` 共
  `61` 个唯一 Tick；资源 `6/35 -> 8/35`，仅 `2` 次采集、`2` 次交付、`0` 次生产，Worker 长期为
  `W5/V2/R0`。认领 `51` 次，其中可见 `22`、历史 `29`，探索保留 `39` Tick；决策耗时 P50/P95/P99
  约为 `16/33/103ms`，主要瓶颈是资源兑现而不是本地计算。
- **根因证据**：历史资源提示长期参与调度，当前可见节点可能在认领排序与动态占位检查后错失采集窗口；
  探索目标只按信息增益排序，多个 Worker 会沿同一侧前沿移动；此外测试曾把默认
  `tactic_state.json` 覆盖成测试坐标，使重启后丢失真实地图提示。
- **本轮实现**：
  1. 至少一个战斗单位存在且无威胁时，将经济桥接上限从 `W5` 提至 `W7`，允许有限探索扩张；达到
     上限后重新为缺失 Ranger/Vanguard 储备资源，敌情下仍保持军备优先。
  2. 全局匹配前固定“当前可见资源格上的空载 Worker”，并在 Worker 数多于资源数时继续使用可见资源
     惩罚，保证最近现场 Worker 不会被历史提示或其他分配拉走。
  3. 空闲探索前沿加入八方向扇区计数，优先选择尚未被占用的扇区，降低东南聚集。
  4. `HARVEST_FAILED` 的资源不存在/耗尽原因立即清除对应历史提示；下一 Tick 若当前 state 重新出现该格，
     再按权威 state 恢复为可采资源。
  5. 测试夹具改用临时持久化文件，禁止离线测试污染 live 的 `tactic_state.json`。
- **静态结果**：`131 passed`；`compileall`、`uv pip check`、`git diff --check` 均通过。
- **上线前处理**：提交前备份并核验 `tactic_state.json`，只启动一个 `io.arenahero.tactic`；不把测试
  产生的状态文件当作地图事实。
- **Live 验收**：先观察连续 `20` 个唯一 Tick，要求无认证/协议/提交/窗口错误且 Core 存活；随后累计
  `200` 个唯一 Tick、至少 `50` 条 `HARVEST_SUCCEEDED -> DEPOSIT_SUCCEEDED` 完整链路，再比较入核资源/Tick。
- **接受条件**：两个独立 `100` Tick 窗口的入核资源/Tick 均不低于基线，且完整样本提高至少 `5%`；同时
  决策 P95 `<1000ms`、无重复 Tick、资源失败率不升高 `5` 个百分点。
- **立即回滚**：出现第二个 `play.py`、窗口/协议错误、Core 受损，或连续两个 `100` Tick 窗口入核率低于
  基线 `10%`；回滚只恢复上一个战术提交，不删除持久地图备份。

## `RESOURCE-DISPATCH-004` 资源干旱下的自适应前沿扩张

- **新证据**：`game.log` 的 `t63559..t63588` 共 `30` 个唯一 Tick，`W5 -> W6/V2` 后仍为
  `0` 次采集、`0` 次交付；没有错误、敌情或移动失败。持久地图只有 `599` 个已探索格，资源提示为
  空，Worker 主要在 Core 附近的 `x340..383/y97..134` 旧边界移动。
- **规则约束**：v0.14 自然节点按 chunk 配额每四个逻辑 Tick 补足，服务端不发送雾区坐标；资源提示为空
  只能说明当前没有已确认节点，不能说明全图没有资源。带货 Worker 仍必须优先回 Core。
- **本轮实现**：
  1. 增加资源干旱计数；无可见且无有效历史资源时，每 `8` Tick 将空载 Worker 的前沿半径扩大 `16`
     格，从常态 `40` 格逐步到 `96` 格，覆盖邻近 chunk 的补给候选。
  2. 一旦看到当前资源或有效历史提示，半径立即收回 `40` 格；超过半径的带货 Worker 不走扩张分支，
     继续使用 A* 回 Core，保护交付效率。
  3. 保持现有八方向扇区分散、可见资源固定派单和历史提示冷却逻辑不变，避免把本轮收益归因到多个变量。
- **静态结果**：`133 passed`；`compileall`、`uv pip check`、`git diff --check` 通过。
- **Canary**：重启后先观察 `20` 个唯一 Tick；要求单进程、无协议/认证/提交/窗口错误、Core 存活，
  并记录干旱半径阶段。随后至少 `200` 个唯一 Tick、`50` 条完整采集到交付链路再比较入核资源/Tick。
- **接受条件**：两个独立 `100` Tick 窗口的入核资源/Tick 均不低于 `RESOURCE-DISPATCH-003` 基线；
  扩张期间无 `MOVE_FAILED` 增长超过 `5` 个百分点，带货回 Core P95 不超过基线 `25%`。
- **立即回滚**：出现第二个 `play.py`、Core 受损、扩张后连续两个 `100` Tick 窗口入核率下降 `10%`，
  或带货回程 P95 超过基线 `25%`；只恢复上一策略提交，不删除地图备份。

## `RESOURCE-DISPATCH-005` 和平期扩大 Worker 发现桥

- **基线证据**：`t63617..t63675` 的单实例窗口无协议、提交或移动故障；已确认 4 次采集、3 次交付，
  但 Core 长期停在 `W6/V2/R0`。可见资源在 Core 外约 30--45 格，真正的瓶颈是发现与兑现周期，
  不是决策耗时。
- **本轮实现**：无敌情且已有战斗单位时，将有限经济桥从 `W7` 放宽到 `W8`，仍低于人口 20 的动态价格
  边界；任意可见敌人（包括远处敌 Core）都会关闭桥接，优先保留攻击单位预算。
- **静态结果**：`135 passed`，compileall 与 `git diff --check` 通过。
- **Live 验收**：重启后先观察 20 个唯一 Tick，确认进程单实例、无协议/提交/窗口错误；随后观察是否由
  `W6` 增长到 `W8`，并累计至少 100 个唯一 Tick、20 条完整采集到入核链路，再与 `RESOURCE-DISPATCH-004`
  窗口比较入核资源/Tick。
- **接受条件**：和平窗口的 Worker 达到 `W8` 且可见资源仍优先采集；两个独立 100 Tick 窗口入核率不低于
  004 基线，决策 P95 < 1000ms，资源失败率不增加 5 个百分点。
- **立即回滚**：出现敌情下仍生产 Worker、Core/协议/窗口故障，或连续两个 100 Tick 窗口入核率下降
  10%；回滚只恢复上一策略提交，不删除持久地图备份。

## `RESOURCE-DISPATCH-006` 延长和平期 Worker 经济桥

- **新日志证据**：`game.log` 的 `t63692..t63744` 共 `53` 个唯一 Tick，只有 `3` 次采集和 `3` 次交付；
  资源点实际出现在距离 Core 约 `30` 和 `41` 格的位置，决策 P95 仍低于 `1s`，无窗口、协议、认证或
  移动争用故障。`W7/V2/R0` 在窗口末仍未形成 Ranger，发现并发和带货回程是主要限制。
- **本轮实现**：无可见敌情且已有战斗单位时，将经济桥上限由 `W8` 提至 `W12`。`W12/V2` 的人口为 `14`，
  仍低于人口 `20` 的首个动态生产价格边界；任意可见敌人或敌 Core 仍会立即关闭桥接并优先补攻击单位。
- **静态验收**：新增 W11->W12 生产与 W12 停桥测试；需通过全量测试、compileall、依赖检查和 diff 检查。
- **Live canary**：重启后观察 `20` 个唯一 Tick，确认单实例、无窗口/协议/认证错误；随后观察是否能由
  `W7` 增长至 `W12`，并累计 `200` 个唯一 Tick、至少 `50` 条完整采集到入核链路，再与 `RESOURCE-DISPATCH-005`
  窗口比较入核资源/Tick。
- **接受条件**：两个独立 `100` Tick 窗口入核资源/Tick 不低于 005 基线，完整链路收益至少提高 `5%`；
  `decide_ms` P95 `<1000ms`，资源失败率不增加 `5` 个百分点，且敌情下没有 Worker 桥接生产。
- **立即回滚**：出现第二个 `play.py`、Core/协议/窗口故障，敌情误产 Worker，或连续两个 `100` Tick 窗口
  入核率低于 005 基线 `10%`；只恢复上一策略提交，不删除持久地图备份。

## `RESOURCE-DISPATCH-007` 历史提示不能屏蔽干旱扩张

- **当前证据**：最新单实例窗口 `t63753..t63808`（观察时）无重复 Tick、无提交/移动失败，
  但 `56` 个 Tick 只有 `3` 次采集、`4` 次交付；其中 `t63764..t63784` 连续 `21` Tick
  没有当前可见资源，而历史分配 `ah` 仍为 `2-3`。历史提示的存在让旧干旱计数一直为零，
  探索半径没有从 `40` 格进入扩张阶段。
- **根因**：`_observe_resources()` 把“存在未过期历史提示”当成当前有资源。规则只把记忆节点定义为
  stale hint；它不能证明本 Tick 仍有可采节点。该条件会让一条失效提示永久压住自适应前沿。
- **本轮修复**：干旱计数仅由 `turn.resource_cells`（当前完整状态中的可见资源）复位；历史提示仍可
  参与有界派单，但不会阻止每 `8` Tick 将空载探索半径扩大 `16` 格（上限 `96`）。带货 Worker
  仍固定回 Core，不进入扩张分支。
- **静态验收**：`138 passed`，`compileall` 与 `git diff --check` 通过；新增回归测试验证“仅有历史
  提示且当前无资源”仍会累积干旱计数并进入扩张半径。
- **Live canary**：重启前备份 `game.log` 与 `tactic_state.json`，确保只有一个 `play.py`；先观察
  `20` 个唯一 Tick 无协议/认证/提交/窗口错误，再累计 `200` 个唯一 Tick 和 `50` 条完整
  `HARVEST_SUCCEEDED -> DEPOSIT_SUCCEEDED` 链路。当前 `56` Tick 短样本只证明瓶颈，不宣称收益。
- **主指标**：入核资源/唯一 Tick；同时记录无可见资源连续长度、`harvest -> deposit` P50/P95、
  `ah` 历史分配占比、新探索格/Tick 和 `MOVE_FAILED` 比例。
- **接受条件**：两个独立 `100` Tick 窗口入核率均不低于 `RESOURCE-DISPATCH-006` 基线，完整样本
  提升至少 `5%`；决策 P95 `<1000ms`，无重复 Tick，资源失败率不增加 `5` 个百分点，带货回程
  P95 不恶化 `25%`。
- **立即回滚**：出现第二个 `play.py`、Core/协议/窗口故障，或连续两个 `100` Tick 窗口入核率
  低于基线 `10%`；只恢复上一策略提交，不删除持久地图备份。

## `RESOURCE-DISPATCH-008` 可见资源认领稳定化（当前 LIVE_CANARY）

- **触发证据**：`game.log` 的 `t63830..t63890` 共 `61` 个唯一 Tick，只有 `3` 次实际采集、
  `5` 次交付；无敌情时 `av` 认领在相邻 Tick 反复变化，资源节点可能连续可见而采集者重新换主。
  决策 P95 远低于命令窗口，瓶颈仍在资源兑现，不在本地计算。
- **可证伪假设**：全局匹配每 Tick 重新按距离选 Worker，会让已经接近当前可见节点的 Worker 被更近的
  同行替换，形成目标漂移；把当前可见节点的认领保留到采集/失败/确认消失，可降低到达延迟并提高完整链路率。
- **实现边界**：新增内存态 `_resource_claims`；仅保存活着且空载 Worker 的短期坐标意图，不写入持久地图。
  当前可见认领优先保留；历史认领遇到当前可见节点会释放，且历史资源仍保留一个前沿探索名额。
  `HARVEST_SUCCEEDED`、资源失败和可见空格确认会清除对应认领。带货回程、Core 生产、敌情判断不变。
- **静态验收**：`139 passed`，`compileall`、`uv pip check`、`git diff --check` 通过；新增测试覆盖
  “第二个 Worker 变近时当前资源认领仍不换主”。
- **Canary**：`2026-08-07 02:58:41` 重启唯一实例，当前 PID `64255`；首个新 Tick 窗口出现采集成功，
  暂未观察到协议/认证/提交/窗口错误。继续累计至少 `20` 个唯一 Tick 后再判定安全，通过后再收集
  `200` Tick / `50` 条完整采集到入核链路。
- **主指标与护栏**：入核资源/唯一 Tick、可见资源出现到采集的 Tick 数、完整采集到交付 P50/P95；
  无重复 Tick、无 `MOVE_FAILED` 增长、无 Core 受损、`decide_ms` P95 `<1000ms`。
- **回滚**：连续两个 `100` Tick 窗口入核率低于 `RESOURCE-DISPATCH-007` 基线 `10%`，或采集延迟
  P95 上升 `25%`、资源失败率上升 `5` 个百分点、出现协议/窗口/Core 故障时，恢复上一策略提交；
  不删除本轮运行备份。

## `RESOURCE-DISPATCH-009` 交付通道预约与复合事件观测修复（待 LIVE_CANARY）

- **当前证据**：唯一进程窗口 `t63903..t63966` 共 `64` 个 Tick，`8` 次采集、`7` 次交付、
  `1` 次 Worker 生产，资源 `4/55 -> 6/60`，净增只有 `2`；移动成功 `576` 次、
  `CELL_UNIT_LIMIT` 失败 `2` 次、`MOVE_CONTESTED=0`。主要瓶颈仍是资源点到 Core 的长距离兑现，
  但空载 Worker 与带货回程共享下一格会产生可避免的尾延迟。
- **实现**：
  1. 历史资源认领增加 `6` 格迟滞；只有当前可见节点明显更近时才换主，避免已经走近的 Worker
     因短暂可见节点掉头；仍保留可见资源正常匹配和失败/消失清理。
  2. `_control_workers` 保留 UUID 稳定探索编号，但在执行顺序上先规划带货 Worker，并把每个
     Worker 的下一格加入本 Tick 预约集合，后续 Worker 避开已预约目的地和 Core 交付格。
  3. `meta/monitor.py` 的事件字段改按 `] plan[` 外层分隔解析，修复带金额方括号的复合事件漏计。
- **静态验收**：`143 passed`，`compileall`、`uv pip check`、`git diff --check` 通过。
- **LIVE_CANARY**：重启后先观察 `20` 个唯一 Tick，要求单进程、无协议/认证/提交/窗口错误、
  Core 存活；随后累计 `200` 个唯一 Tick、`50` 条完整采集到入核链路，再比较 `deposit/tick`、
  `harvest -> deposit` P50/P95、`CELL_UNIT_LIMIT` 和 `MOVE_CONTESTED`。
- **接受/回滚**：两个独立 `100` Tick 窗口入核率不低于 `RESOURCE-DISPATCH-008` 基线，移动失败
  率不增加 `5` 个百分点，决策 P95 `<1000ms`；若连续两个窗口入核率低 `10%`、采集延迟 P95
  上升 `25%`、Core 受损或出现第二个 `play.py`，恢复上一提交，不删除运行备份。

## `RESOURCE-DISPATCH-010` 历史远端提示限距与 Worker 桥接上限（待 LIVE_CANARY）

- **当前证据**：唯一进程 `t63903..t64061` 的连续窗口没有协议、提交或窗口错误，移动成功约
  `1593` 次、`CELL_UNIT_LIMIT` 仅 `6` 次，但采集约 `18` 次、交付约 `17` 次；历史认领累计
  `145` 次，明显高于可见认领。采集距离与回程延迟相关，但当前可见远点仍有兑现价值，不能用
  `30` 格硬过滤全部资源。
- **实现**：健康期只主动派发距 Core 不超过 `30` 格的非可见历史提示；连续 `24` Tick 没有
  `HARVEST_SUCCEEDED` 时放宽到 `65` 格。当前可见资源、已在资源格的 Worker、以及已经在路上的
  短期认领不受该门槛影响；距离超过半径但已有 Worker 在 `4` 格内的历史点也保留，以便就近兑现。
  新增 `eco[..farN..]` 遥测统计被限距的历史候选。和平期 Worker 目标取人口预算和
  `ECONOMY_BRIDGE_MAX_WORKERS` 的较小值，Army 已齐时不再从 W12 继续长到 W18。
- **静态验收**：`147 passed`，`compileall`、`uv pip check`、`git diff --check` 通过；新增健康期
  远端保留/不派发、干旱放宽、可见远点、就近兑现和 W12 上限回归测试。
- **LIVE_CANARY**：重启前备份 `game.log`、`tactic_state.json`，确认唯一 `play.py`；重启后先看
  `50` 个唯一 Tick，比较 `far` 过滤量、`harvest/tick`、`deposit/tick`、完整采集到入核延迟和
  `CELL_UNIT_LIMIT`。累计 `200` 个唯一 Tick 后再决定是否扩大搜索半径或调整 Worker 数。
- **接受/回滚**：Core 必须存活且无协议/提交/窗口错误；若连续两个 `100` Tick 窗口入核率比
  `RESOURCE-DISPATCH-009` 低 `10%`、采集延迟 P95 上升 `25%`、`far` 过滤后出现连续 `24`
  Tick 无采集，或出现第二个客户端，恢复上一提交并保留运行备份。

## `RESOURCE-DISPATCH-011` 和平期军备库存闸门与空载 Core 格保护（当前 STATIC_VERIFIED）

- **当前证据**：单进程窗口 `t64097..t64165` 共 69 个唯一 Tick，7 次采集、9 次交付；和平期
  Ranger 生产把库存从 `13` 降到 `1`，窗口末仅回升到 `5`。同窗另有 1 次
  `CORE_SPAWN_FAILED.CELL_UNIT_LIMIT`，对应空载 Worker 误入 Core 格。
- **可证伪假设**：已有 Vanguard 防线且无敌人时，Ranger 生产后的库存下限可以减少净资源
  断崖，而不会影响敌情下的即时攻击生产；空载 Worker 的最终兜底阻塞 Core 后，生产失败应为零。
- **实现范围**：只修改 `tactic.py` 与 `tests/test_tactic.py`。和平期补齐 Ranger 前至少保留
  5 点库存；可见敌人时保留为 0。空载 Worker 的探索/脱困兜底一律把 Core 格视为阻塞，带货
  Worker 的交付路径不变。
- **静态验收**：`150 passed`；`compileall`、`uv pip check`、`git diff --check`、敏感信息
  扫描通过。
- **Canary 门槛**：重启前备份 `game.log` 与 `tactic_state.json`，只允许一个 `play.py`；先看
  20 个唯一 Tick，要求无协议、认证、提交、窗口错误且 Core 存活；随后累计至少 100 个唯一
  Tick 和 20 条采集入核链路，再比较库存净增。
- **主指标**：和平窗口入核资源/Tick、库存净增和 Ranger 生产后的最低库存。
- **安全护栏**：敌情出现后下一可生产 Tick 不得因库存闸门跳过攻击单位；`CELL_UNIT_LIMIT`、
  Core 受损、移动/资源失败率不得上升；决策 P95 仍低于 1000ms。
- **回滚**：连续两个 100-Tick 窗口入核资源/Tick 低于当前基线 10%，敌情下攻击生产延迟，
  出现 Core/协议/窗口故障，或新增 Core 格生产失败，即恢复上一策略提交；不删除地图备份。

## `RESOURCE-DISPATCH-012` 近端前沿优先与四向覆盖（当前 STATIC_VERIFIED）

- **当前证据**：唯一实例窗口 `t64191..t64237` 共 47 个 Tick，资源 `5 -> 9`，只有 2 次采集、
  4 次交付；无协议、提交、窗口、移动失败或 Core 风险。持久探索格仅增加约 25 个，说明空载
  Worker 大部分时间仍在已探索地面上前往远端边界。复现当前地图后，旧评分会为了较大的
  `frontier_gain` 把 Worker 派到 50-70 格外，距离优先的反例测试在旧代码下失败。
- **可证伪假设**：资源为空时，先让最多四个主方向各有一个近端前沿，再让剩余 Worker 选择自身
  最近的可达前沿，可缩短“分配 -> 新增视野”的等待；四向覆盖仍能避免东南单侧聚集。
- **实现范围**：仅修改 `_assign_explore_targets()` 的候选排序与回归测试；不改变资源事实、历史
  提示、带货回程、战斗、生产和干旱半径。新增的方向覆盖预算为 4，候选之间保持至少 6 格间距，
  目标仍是坐标意图，不保存 SDK 控制器。
- **静态验收**：全量 pytest `151 passed`；compileall、`uv pip check`、`git diff --check` 通过。
- **LIVE_CANARY**：重启前备份 `game.log` 与 `tactic_state.json`，确认只有一个 `play.py`；先观察
  50 个唯一 Tick，记录新增探索格/Tick、`HARVEST_SUCCEEDED`、`DEPOSIT_SUCCEEDED`、采集到入核
  延迟和 `MOVE_FAILED`。随后累计 200 个唯一 Tick、至少 50 条完整采集到入核链路，再与
  `RESOURCE-DISPATCH-011` 的唯一 Tick 窗口比较入核资源/Tick。
- **接受/回滚**：Core 存活且无协议/认证/提交/窗口错误；两个独立 100-Tick 窗口入核资源/Tick
  不低于基线，新增探索格/Tick 提升且完整链路不退化 10% 以上。若连续两个窗口入核率低于基线
  10%、带货回程 P95 上升 25%、出现移动失败/Core 风险或第二个客户端，恢复上一提交，不删除地图备份。

## `RESOURCE-DISPATCH-013` Chunk 回扫与历史认领释放（待 LIVE_CANARY）

- **当前证据**：live `t64264..t64324` 共 61 个唯一 Tick，资源 `0 -> 4`，无协议、提交、窗口、
  Core 或移动故障；持久探索地图约 4,063 格，`exp1` 长时间持续。v0.14 每四个逻辑 Tick 在已跟踪
  Chunk 内补充自然节点，全部空载 Worker 追前沿会漏掉已探索区域的补充资源。
- **可证伪假设**：无当前可见资源时固定 2 个空载 Worker 做 Chunk 纵向回扫，其余 Worker 继续
  四向前沿；健康期远端历史认领在 Worker 尚未接近或走过半程时释放，可提高发现率并减少长距离空载。
- **实现范围**：`tactic.py` 新增回扫名额、`ref` 经济遥测及历史认领进度门槛；`meta/monitor.py` 汇总
  `refresh_reservations`；补充资源调度和监控回归测试。已存在的 W15 桥接、可见资源优先、A* 回程和
  持久地图事实不变。
- **静态验收**：全量 `pytest -q`、`python -m compileall -q .`、`uv pip check`、`git diff --check`；
  监控 JSON 必须能解析 `ref`，且敏感信息扫描无 API Key。
- **LIVE_CANARY**：重启前备份 `game.log` 与 `tactic_state.json`，确认只有一个 `play.py`；先观察
  50 个唯一 Tick，记录 `W`、`ref`、新增探索格、`HARVEST_SUCCEEDED`、`DEPOSIT_SUCCEEDED`、
  采集到入核延迟、`far`、`CELL_UNIT_LIMIT` 和 Core 状态。累计 200 个唯一 Tick、至少 50 条完整
  采集到入核链路后，再与 `RESOURCE-DISPATCH-012` 比较入核资源/Tick。
- **接受/回滚**：Core 存活且无协议/认证/提交/窗口错误；两个独立 100-Tick 窗口入核资源/Tick
  不低于基线，采集到入核 P95 不恶化 25%，`ref` 在无可见资源窗口保持约 2 且无额外移动失败。
  若入核率连续两个窗口低于基线 10%、采集 P95 上升 25%、出现 Core/窗口故障或第二个客户端，
  恢复上一提交，保留地图备份。

## `RULE-002` 战斗执行链与 Core 生存（当前 IN_PROGRESS）

- **问题证据**：此前约 110 个 Tick 看见敌 Core，但固定 `resources >= 60` 门槛和
  `CHASE_MAX_TICKS=8` 使远程 Ranger 从未进入射程；同时新增 V4/R4 军备消耗约 58 点资源，
  没有 `SHOOT/SWEEP`、敌核摧毁或捕获收益。低血 Unit 还会先 HEAL，放弃当 Tick 的合法攻击；
  Core 受伤也不会自动恢复 HP。
- **可证伪假设**：动态 Ranger 重建成本加 3 点储备、按合法射击位 ETA 的有界追击预算、UUID
  稳定的守卫/突袭角色，能让可逆突袭在资源可承受时实际抵达；攻击优先与 Core HEAL 能降低
  资源损失而不破坏同 Tick 账本。
- **实现边界**：仅调整 `tactic.py` 与 `tests/test_tactic.py` 的战斗、Core 动作和掉落货堆记忆；
  不改变用户手动迁核、不伪造资源数量、不让历史敌核坐标触发无限扩军。
- **静态验收**：`pytest -q tests/test_tactic.py` 通过 158 项；已覆盖低血 Ranger 攻击优先、
  远程敌核动态门槛/ETA、Core HP 治疗、视野外掉落货堆提示和此前资源调度回归。
- **LIVE_CANARY**：重启后先观察 20 个唯一 Tick，随后累计 200 个唯一 Tick；必须记录敌核可见
  到首次合法攻击延迟、`CORE_RESOURCES_CAPTURED`、增量军备/治疗成本、Core HP 和资源净收益。
- **接受/回滚**：Core 存活且无协议/认证/提交/窗口错误；若敌核可执行预算出现但连续 50 Tick
  没有合法攻击，或两个 100-Tick 窗口的净收益低于 `e45cec0` 10%，恢复本任务反向提交，保留
  `tactic_state.json.archive-*` 运行备份。

## `OBS-002` Manual 归因与 v0.14 成本遥测（当前 STATIC_VERIFIED）

- **实现**：`play.py` 使用 SDK `events()` 记录 canonical `AGENT/MANUAL` receipt；每个 Tick
  记录受限事件详情（生产、修盾、Unit/Core HEAL、捕获、溢出成本），不写入密钥或服务端错误正文。
  `meta/monitor.py` 预扫描 receipt，把 Manual Tick 从 Agent 主 KPI 排除并保留 Manual 事件直方图；
  同时按事件自身 `tick` 和 Worker `actor` 计算 `HARVEST_SUCCEEDED -> DEPOSIT_SUCCEEDED` 链路。
- **静态验收**：兼容旧 `turns()` 假客户端；监控仍支持旧日志字段；全量 pytest `161 passed`、compileall、
  `uv pip check`、`git diff --check` 和敏感信息扫描通过后才允许提交。
- **剩余限制**：Worker 级链路现已可观测，但当前仅有 `7` 条完整链路；窗口边界仍有 `1` 个未匹配
  入核事件，必须累计至少 `50` 条并复核未匹配原因后，才能调整 Worker 数量或回程策略。

## 2026-08-07 `RULE-002` live canary 快照（继续观察）

- **运行窗口**：LaunchAgent `io.arenahero.tactic` 仍为唯一 PID `35285`；新代码窗口
  `t65805..t65843` 共 `39` 个连续唯一 Tick，重复 Tick、失败 Tick、窗口、提交、协议和认证错误均为
  `0`，`Core hp5/sh5` 全程存活。事件层另有 `1` 次 `UNIT_MOVE_FAILED.CELL_UNIT_LIMIT`，未形成
  失败 Tick、资源损失或伤亡。
- **突袭兑现**：敌 Core 可见 `9` Tick、达到可执行突袭预算 `9` Tick；发生 `12` 次合法
  `SHOT_HIT`，并在 `t65832` 完成 `DESTRUCTION_PARTICIPATION.CORE` 与
  `CORE_RESOURCES_CAPTURED amount=5`。这证明动态门槛和 ETA 追击已经在 live 形成有效攻击链。
- **经济结果**：窗口内 `HARVEST=5`、`DEPOSIT amount=3`、捕获 `5`，无生产、治疗、修盾或溢出
  成本，净资源 `+8`。样本仍不足 `200` Tick / `50` 条完整链路，不能宣称长期收益最优。
- **性能护栏**：规划耗时 P50/P95/P99 约 `406/1695/2532ms`，总耗时最高约 `3.7s`，仍低于
  `15s` 命令窗口但已超过本地 P50/P95/P99 目标。当前先保留已验证的 A* 和突袭行为，下一轮只针对
  路径搜索耗时做单假设专家复核；若出现窗口错误或 P95 接近命令窗口，再实施有界搜索优化。
- **下一门槛**：继续累计至 `200` 个唯一 Tick 和至少 `50` 条
  `HARVEST_SUCCEEDED -> DEPOSIT_SUCCEEDED` 链路，再按 Agent-only 净资源/Tick、捕获抵扣军备成本、
  入核 P50/P95 和性能护栏决定是否调整 Worker 数量或路径算法。

## 2026-08-07 Worker 链路遥测 checkpoint（继续观察）

- **窗口**：`t65805..t65907` 共 `103` 个唯一 Agent Tick，Manual `0`，失败 Tick、Tick 缺口、窗口、
  提交、协议和认证错误均为 `0`；Core `hp5/sh5`，单位无死亡。
- **资源链路**：`HARVEST=7`、`DEPOSIT amount=8`、敌核捕获 `5`，Agent-only 净资源 `+13`；按
  Worker `actor` 匹配得到 `7` 条完整采集到入核链路，延迟 P50/P95 为 `16/39.7` Tick，未匹配入核 `1`。
- **性能**：规划耗时 P50/P95/P99 为 `318/1515.1/2388.5ms`，总耗时仍远低于 15 秒命令窗口；暂不
  修改 A* 或突袭逻辑，继续收集同一版本样本。

## 2026-08-07 live observation checkpoint（t65956，继续观察）

- **窗口**：同一 LaunchAgent `io.arenahero.tactic` 仍只有 PID `35285`；`t65805..t65956` 共 `152`
  个连续 Agent Tick，Manual `0`，重复、失败、窗口、提交、协议、认证和 Tick 缺口均为 `0`。
- **资源结果**：累计 `HARVEST=10`、`DEPOSIT amount=8`、敌核捕获 `5`，生产/治疗/修盾/溢出成本
  均为 `0`，Agent-only 净资源 `+13`（`0.086/唯一 Tick`）；Worker 级完整链路仍为 `7`，
  未匹配采集 `3`、未匹配入核 `1`，链路 P50/P95 为 `16/39.7` Tick。
- **护栏**：Core `hp5/sh5` 全程存活，单位无死亡；`CELL_UNIT_LIMIT` 移动失败仍为 `2`，没有
  `MOVE_CONTESTED`、资源找不到或 Core 移动失败。敌 Core 可见/达到动态突袭预算各 `31` Tick，
  但这段增量没有第二次合法攻击或捕获，因敌核可见不连续，暂不按“连续 50 Tick 无攻击”判定失败。
- **性能判断**：规划 P50/P95/P99 为 `308.5/1152.3/2278.25ms`，总耗时 P50/P95/P99 为
  `1435/2841.85/3383.99ms`，仍有数量级命令窗口余量；专家复核完成前不修改 A*、Worker 数量或
  战斗分配。继续累计至 `200` Tick 和 `50` 条完整链路，再做唯一变量比较。

## 2026-08-07 `RULE-002` second raid checkpoint（t65983，继续观察）

- **第二次突袭时序**：第二个敌 Core 在 `t65965` 开始连续可见，`t65973` 出现首个合法
  `SHOT_HIT`，首次攻击延迟 `8` Tick；`t65979` 完成 `DESTRUCTION_PARTICIPATION.CORE`，并
  `CORE_RESOURCES_CAPTURED amount=5`。动态 ETA 追击预算在同一连续可见段内兑现，没有修改战斗代码。
- **窗口结果**：累计 `179` 个唯一 Agent Tick，Manual、失败、缺口、窗口、提交、协议和认证均为
  `0`；第二次摧毁后 Agent-only 净资源 `+21`，其中入核 `11`、两次敌核捕获合计 `10`，生产/治疗/
  修盾/溢出成本仍为 `0`。完整 Worker 链路 `10` 条，链路延迟 P50/P95 `23.5/43` Tick。
- **拥挤护栏**：`CELL_UNIT_LIMIT` 移动失败累计 `7`（集中在 `t65975..t65979`），
  `MOVE_CONTESTED`、资源找不到、Core 移动失败、Unit 死亡和 Core 受击均为 `0`；先观察到 `200` Tick
  再判断是否需要单独处理战斗入口拥挤。
- **下一步**：保持同一版本累计到 `200` Tick 和 `50` 条完整链路；将第二次突袭的 `8` Tick 首攻延迟、
  捕获 `5` 和增量阻塞 `5` 次作为后续比较基线，不把两次突袭收益与 Worker/A* 变量混合。

## 2026-08-07 `RULE-002` 201-Tick 基线封存

- **归档窗口**：`game.log.archive-pre-6d69068-20260807-1150` 覆盖 `t65805..t66005`，共 `201` 个
  连续唯一 Agent Tick；Manual、重复、失败、缺口、窗口、提交、协议和认证均为 `0`。
- **资源闭环**：入核 `12`、两次敌核捕获合计 `10`，无生产、治疗、修盾或溢出成本，Agent-only
  净资源 `+22`（`0.109/唯一 Tick`）；完整 Worker 链路 `11` 条，P50/P95 为 `25/43` Tick，
  未匹配采集/入核各 `1`。200-Tick 门槛已满足，但 `50` 条完整链路门槛仍未满足。
- **战斗与护栏**：两次敌核摧毁、Core 满血满盾、单位无死亡；累计 `8` 次
  `CELL_UNIT_LIMIT`、无 `MOVE_CONTESTED`。规划 P50/P95/P99 为 `318/1136/2393ms`，总耗时
  P50/P95/P99 为 `1418/2810/3563ms`，全程未接近 15 秒窗口。

## `PERF-001` A* 归因与 Ranger 满格绕行（CANARY_COMPLETE，收益样本不足）

- **实现提交**：`6d69068` 为每 Tick 记录 A* 调用、展开、预算耗尽和成功路径数，并让追击 Ranger
  避开已有两个友方单位的满格；全量测试 `163 passed` 后已推送并由唯一 PID `63156` 运行。
- **监控闭环**：`meta/monitor.py` 解析 `path[c,e,b,p]`，汇总总量及单 Tick 调用/展开 P50/P95；这是
  行为中性监控补充，不需要重启 live，也不把新窗口拼接进旧基线。
- **32-Tick canary**：归档 `game.log.archive-pre-e2aa73e-20260807-120242` 覆盖 `t66005..t66036`，
  无失败、缺口或 Core/Unit 损失；入核 `1`、完整链路 `1`（延迟 `20` Tick），另有 `2` 次
  `CELL_UNIT_LIMIT`。决策 P50/P95/P99 为 `268/615.25/744.84ms`。
- **A* 归因**：共调用 `300` 次、展开 `662324` 个节点、预算耗尽 `161` 次、成功路径 `139` 条；
  单 Tick 调用 P50/P95 `11/12`，展开 P50/P95 `22649.5/37918.8`。命令窗口安全，但资源样本不足。

## `PERF-002` 空载 Worker A* 预算减半（当前 OBSERVING）

- **单一变量**：提交 `e2aa73e` 只把空载 Worker 前往资源与探索前沿的 A* 展开上限从 `4000` 降到
  `2000`；带货回 Core、战斗寻路、资源认领、Worker 数量和突袭逻辑不变。新增两项回归测试并推送。
- **假设**：前一 canary 每 Tick 约 `9-12` 次 A* 且超过半数预算耗尽；对允许直线回退的空载 Worker
  限制展开，可降低本地耗时而不损害采集/入核。预算耗尽本身不是成功，资源兑现才是主指标。
- **验收**：唯一 PID `64418` 从 `t66036` 开始新窗口；先观察 20 个唯一 Tick 无窗口、协议、Core 和
  移动故障，再累计至少 `100` Tick、`20` 条完整链路。比较 A* 展开/Tick、决策 P50/P95、
  `HARVEST/DEPOSIT`、链路 P50/P95 和净资源/Tick。
- **回滚**：连续 20 Tick 出现路径相关移动停滞、资源失败或决策错误，或两个 50-Tick 窗口的入核率
  比 201-Tick 基线低 `10%`；只反向提交 `e2aa73e`，保留归档和持久地图。

### 20-Tick 安全 canary（2026-08-07）

- **有效窗口**：唯一 PID `64418` 从 `t66036` 接管；安全护栏按变更已实际控制的
  `t66037..t66056` 共 20 个连续唯一 Agent Tick 判定。Manual、重复、缺口、窗口、提交、协议、
  认证和决策错误均为 `0`；Core 全程 `hp5/sh5`，单位无死亡，变更生效后的移动与资源失败为 `0`。
- **切换边界**：`t66036` state 中的 `CELL_UNIT_LIMIT` 属于 resolution tick `66035`，由重启前
  计划产生；首个新 Turn 会携带上一 Tick 的结算事件，不能把它归因给 `e2aa73e`。
- **性能结果**：包含首个新计划在内的 `t66036..t66056` 共 21 个样本，A* 调用 `149` 次、展开
  `254607`、预算耗尽 `64` 次、成功路径 `85` 条；单 Tick 展开 P50/P95 从基线
  `22649.5/37918.8` 降到 `12502/20576`（约 `44.8%/45.7%`），决策 P50/P95 从
  `268/615.25ms` 降到 `204/385ms`（约 `23.9%/37.4%`）。无命令窗口风险，但 P99 样本仍不稳定。
- **资源边界**：窗口库存 `52 -> 55`，Agent-only 净资源 `+3`；只有 `1` 条完整采集入核链路，
  远低于 `20` 条阶段门槛。因此只接受安全 canary，继续观察到至少 `100` Tick / `20` 条完整链路，
  不把计算量下降提前写成长期资源收益。

### 83-Tick 中止边界（2026-08-07）

- **封存窗口**：`game.log.archive-pre-rule-003-20260807-122340` 覆盖 `t66036..t66118`，共 `83` 个
  连续唯一 Agent Tick；Manual、重复、缺口、窗口、提交、协议、认证和决策错误均为 `0`，Core
  全程 `hp5/sh5`，单位无死亡。
- **资源与性能**：净资源 `+6`（`0.072/唯一 Tick`），仅 `2` 条完整采集入核链路，远低于 `20` 条；
  A* 展开 P50/P95 为 `8382/22010.6`，决策 P50/P95 为 `155/352.8ms`。这继续支持性能护栏，仍不能
  验收长期资源收益。
- **中止原因**：代码复核发现 `RULE-003` 军备正确性缺口，优先级高于等待性能实验补齐业务样本。
  因此不把 83 Tick 窗口判为接受或回滚；`e2aa73e` 继续保留，后续新版本窗口需把军备修复作为明确
  版本边界，禁止与旧窗口直接做单变量资源收益比较。

## `RULE-003` 远处可见普通敌人触发军备（OBSERVING）

- **问题证据**：`decide()` 的注释和 `GOAL.md` 要求任意可见敌人及时触发攻击单位生产，但
  `_threats_to_core()` 只保留距 Core `<=6` 的敌人，`_control_core()` 的 `enemy_present` 又只读取近核
  威胁或可见敌 Core。被前线 Worker 看见、但离 Core 较远的普通敌方 Unit 会被误判为和平窗口；
  在 `W1/r10` 的确定性复现中旧策略实际提交 `SPAWN WORKER`。
- **单一修复**：将 `bool(turn.visible_enemies)` 作为独立 `enemy_visible` 信号传入 Core 控制器，仅用于
  关闭和平 Worker 桥、取消和平 Ranger 库存保留并开放军备地板。近核 `threats` 仍单独决定修盾和
  防御编制，`enemy_core_visible` 仍单独决定突袭扩编；不改 A*、Worker 上限、战斗寻路或攻击选择。
- **TDD 与静态结果**：新增前线 Worker 看见距 Core 7 格敌 Ranger 的回归测试；修复前稳定失败为
  `WORKER != VANGUARD`，修复后目标测试通过，全量 `167 passed`，compileall 与 `git diff --check`
  通过。
- **Live canary**：提交并推送后只重启 `io.arenahero.tactic`，确认仍恰好一个 `play.py`；从新计划完成
  结算后的首个 Tick 起观察 20 个连续唯一 Tick，要求无窗口/协议/提交错误、Core/单位损失和新增移动/
  资源失败。行为验收需等远处普通敌方 Unit 再次可见：下一可生产 Tick 不得继续造 Worker，库存足够时
  应生产缺失 Vanguard/Ranger，否则应保留军备预算。
- **回滚**：若远处普通敌人造成无界扩军、和平期因陈旧敌人记忆长期冻结经济，或出现 Core/窗口/协议
  故障，只反向提交 `RULE-003`；当前 state 不再可见敌人时必须立即恢复和平策略。

### 20-Tick 安全 canary（2026-08-07）

- **版本边界**：提交 `afb243f` 推送后先卸载 LaunchAgent，确认 `play.py=0`，再整体归档旧日志并重新
  加载；唯一 PID `67848` 从 `t66134` 生成新计划，没有新旧客户端竞争 Agent 槽。
- **安全结果**：按 `t66135..t66154` 共 20 个连续唯一 Tick 判定；Manual、重复、缺口、窗口、提交、
  协议、认证和决策错误均为 `0`，Core 全程 `hp5/sh5`，单位无死亡，移动/资源失败均为 `0`。
  截至 `t66155` 的 22 个新版本样本净资源 `+2`，无生产、治疗、修盾或溢出成本。
- **行为边界**：窗口内可见敌人 Tick 为 `0`，因此只能接受安全 canary；远处普通敌人出现后的生产/
  留存军备预算仍只有 TDD 覆盖，状态保持 `OBSERVING`。`LOW_HARVEST` 来自短窗 `1/22` 次采集，继续
  作为经济信号观察，不归因于没有实际触发的敌情分支。

## 定时复核自动化阻断（2026-08-07）

- **现状**：当前 `/Users/hx/.codex/automations` 下没有 `automation.toml`；“Arena Hero 资源优化复核”
  heartbeat 创建调用再次长期无返回，终止后仍未落盘，因此定时任务未创建。
- **降级边界**：不使用系统 cron 冒充 Codex 线程 heartbeat。`io.arenahero.tactic` 继续负责唯一 live
  客户端；每次人工续接先复核官方规则/SDK、唯一进程、唯一 Tick KPI 和 GitHub SHA。
- **恢复条件**：Codex 自动化接口可正常返回时，创建每 6 小时 heartbeat，仅失败运行通知；任务内容
  必须沿用本文件的样本、单变量、canary、提交/推送和回滚门槛，禁止无门槛自动调参。

## `PERF-003` 满核带货容量电梯（STATIC_VERIFIED，待 live canary）

- **基线与证据**：基线 `main@4d5e85e`，规则 gameplay `v0.14`、SDK `0.2.9`。专家按
  `t66300..t72364` 审计发现，`t69441..t70440` 约 1,000 Tick 处于 `r115/115`、W15/V4/R4，
  15 个 Worker 均带货且没有采集/交付；普通 Worker 目标已受军备预算压到 W12，因此常规生产分支
  不会释放容量。该窗口不是 A* 找不到路，而是 Core 满容量导致交付闭环锁死。
- **单一假设与实现**：仅当当前 Turn 同时满足“Core 满、每个 Worker `cargo>0`、无可见敌人、军备
  不短缺、Worker 数 `<MAX_WORKERS(21)`”时，允许一个动态定价的 Worker 作为容量电梯；新 Worker
  使下一 Tick 容量增加 5，其他探索、A*、战斗和敌情规则不变。新增 `sat[full,elev]` Tick 遥测。
  `meta/monitor.py` 同时修正 `idle_gold_streak` 为历史最长连续满容量平台，并增加 `FULL_CORE_LOCK` 告警。
- **安全边界**：若 Core 格上的带货 Worker 没有足够的独立合法离核格（障碍、敌占或友方满格），
  不排容量电梯；Core 移动结算前不会用一个必败的 `CELL_UNIT_LIMIT` 生产动作换取遥测上的假成功。
- **允许文件**：`tactic.py`、`play.py`、`meta/monitor.py`、`tests/test_tactic.py`、本文档和
  `LESSONS.md`。不暂存 `uv.lock`、`tactic_state.json.archive-*`、`game.log*` 或 `play.out*`。
- **静态验收**：全量 `pytest` `171 passed`；`compileall`、`git diff --check`、`uv pip check` 通过。
- **live canary**：提交后重启唯一 `io.arenahero.tactic`，从新计划完成结算后的下一 Tick 起先观察
  20 个连续唯一 Tick；随后累计至少 100 个唯一 Tick/20 条完整采集→入核链路。重点比较满核带货 Tick、
  容量电梯成功生产、满核最长平台、HARVEST/DEPOSIT、链路 P50/P95、Agent 净资源/Tick、决策 P50/P95，
  并要求无新 `TICK_MISMATCH`、Tick 缺口、Core/Unit 死亡或资源失败。三次既有未归因的 50/50/95
  资源骤降仍单独标记，不并入本实验收益。
- **8342d8c 安全窗口（2026-08-08）**：本地 SDK `0.2.9`、PyPI 最新 `0.2.9`，在线 source/version
  仍为 gameplay `v0.14`、server `b24cfcd`、SDK `423d252`。重启后的唯一 PID `84599` 在
  `t72455..t72475` 产生 21 个唯一 Tick；失败、重复、缺口、Manual、窗口/提交/协议错误、Unit 死亡、
  移动/资源失败均为 `0`，Core 全程 `5/5`，库存 `95/100`，编制 `W12/V4/R4`。窗口内
  `sat[full,elev]`、生产、采集、交付均为 `0`，因此只接受安全护栏通过，不能宣称容量或长期资源收益。
  窗口后的 `t72476` 另有 1 次普通 `UNIT_MOVE_FAILED/CELL_UNIT_LIMIT`，未触发容量电梯或 Core 生产，
  单独留给拥挤移动问题排查。
- **回滚**：若 20 Tick 内出现容量电梯连续生产、敌情下漏造攻击单位、`CELL_UNIT_LIMIT`/资源失败、
  命令窗口/协议/提交错误，或两个干净 50-Tick 窗口的入核率较 `RULE-002` 201-Tick 基线下降 10%，
  只反向提交本任务改动；不删除运行归档，不回滚用户的 `uv.lock` 修改。

## `RULE-004` Core 邻格退让不得把 Core 当作兜底目标（STATIC_VERIFIED，待 canary）

- **证据**：专家复核 `t72455..t72531` 共 77 个唯一 Tick，资源仅在 10 个 Tick 可见，
  可见资源均被分配；未见 `HARVEST_FAILED`、不可达或冷却。窗口内有 1 次普通
  `UNIT_MOVE_FAILED/CELL_UNIT_LIMIT`：带货 Worker 从 Core 邻格退让时，Core 上另一带货 Worker
  同 Tick 执行 `DEPOSIT`，移动先于交付，原路径的兜底选择把 Core 作为唯一开放格。
- **单一修复**：只在“Core 已有 Unit、带货 Worker 位于 Core 邻格、可用相邻出口不超过 1 个”的
  退让分支中，将 `core_pos` 加入局部 blocked；无合法离核步时等待，不改变满核容量电梯、A*、
  资源半径或战斗策略。
- **离线验收**：新增回归覆盖“唯一剩余开放格是 Core”场景；目标测试通过，随后必须执行全量
  `pytest`、`compileall`、`uv pip check` 和 `git diff --check`。
- **live canary**：提交并重启唯一 `io.arenahero.tactic` 后，从新计划完成结算的下一 Tick 起先观察
  20 个连续唯一 Tick；要求无新增 `CELL_UNIT_LIMIT` 交付冲突、资源失败、窗口/协议/提交错误、
  Core/Unit 死亡。之后继续累计至少 100 Tick/20 条完整采集→入核链路；资源可见率仍作为主瓶颈指标，
  不把短窗库存变化当作长期收益。
- **下一变量**：若安全 canary 通过且低资源可见率持续，再单独试验无资源时 Chunk 回扫 Worker
  从 2 降到 1；不能与本修复同一版本混合，以便比较可见资源率、探索新格数、采集/入核率和净资源/Tick。

## `RULE-005` 资源充足时主动追击普通敌方 Unit（STATIC_VERIFIED，SAFE_CANARY_VERIFIED，待敌情样本）

- **用户目标**：当库存足以承担一次战斗单位重建时，发现一定范围内的普通敌方 Unit 应主动出击，
  不能只等敌人进入 Core 防守圈；所有追击仍服务于保护和扩大长期资源净增长。
- **现状证据**：既有策略已具备 Ranger 的 1–3 格合法射击、非守卫 Ranger 的 `CHASE_RADIUS=15`
  和 12–48 Tick ETA 预算，但普通敌方 Unit 在 7–14 格范围内即使库存为 0 也会触发追击；
  首个 Ranger 仍固定守 Core。专家建议只收紧这个资源边界，不改变 A*、生产、近核防御或敌 Core 突袭。
- **单一修复**：复用 `unit_cost(RANGER, population) + RAID_REBUILD_RESERVE(3)` 作为可逆门槛。普通
  敌方 Unit 距 Core `<=6` 格时仍无条件按护核逻辑追击；处于既有 `<15` 格范围但超过 6 格时，
  只有库存达到门槛才主动追击。近距离可合法 `SHOOT` 的目标始终优先，不受该门槛影响。
- **静态验收**：新增低资源中距离不追击、高资源同目标追击、低资源近核仍追击三项回归；全量测试
  `174 passed`，并通过 `compileall`、`uv pip check`、`git diff --check`。
- **live canary**：提交后重启唯一 `io.arenahero.tactic`；先观察 20 个唯一 Tick 的窗口/协议/提交、
  Core/Unit、生存与移动护栏，再等待目标敌方 Unit 在 7–14 格可见时验证“低资源不追、高资源追击”。
  必须记录首攻延迟、`SHOT_HIT`/`SWEEP`、单位损失、增量生产/治疗成本和 Agent-only 净资源；没有实际
  敌情时只能接受安全 canary，不能宣称主动出击收益。
- **安全 canary（2026-08-08）**：唯一 PID `11870` 重启后从 `t72602` 起连续产生 `t72602..t72623`
  共 22 个唯一 Tick；22/22 为 `ST[ACCEPTED]`，无 Manual、窗口/协议/提交错误、移动/资源失败、
  Core/Unit 死亡或 Core 伤害。库存保持 `100/100`，Core 全程 `5/5`，编制稳定为 `W12/V4/R4`；
  窗口内 `vis0[-]` 全部无敌情，期间仅有两次 `HARVEST_SUCCEEDED`，没有可用于判断追击收益的
  `SHOT_HIT`、`SWEEP`、生产成本或捕获事件。因此只将本轮标记为安全护栏通过，主动出击行为仍待
  7–14 格普通敌方 Unit 的真实可见样本；后续继续累计至少 100–200 个 clean Tick 和 20 条完整采集→入核链路。
- **回滚**：若低资源仍远追、Core guard 被拉走、追击导致 `MOVE_FAILED`/伤亡，或两个干净 50-Tick
  窗口的入核率较 `RULE-002` 基线下降 10%，只反向提交本任务改动；保留日志与经验归档。

## `ECONOMY-006` 24 人口单变量扩容（ROLLED_BACK，2026-08-08）

- **实验与规则**：`v0.14` 没有 per-Tick upkeep；人口 20 是第 21 个 Unit 的首个动态价格边界，
  `N=20..24` 仍是同一价档。实验把总人口预算从隐式 20 放宽到 24、Worker bridge 从 W15 放到
  W16，保留敌情优先、军备地板、容量电梯和 Manual 优先级。
- **离线结果**：提交 `d8c2b6d` 的 180 项测试、`compileall`、`uv pip check`、`git diff --check`
  和敏感信息扫描均通过；覆盖动态价 Worker、N24 停止、容量电梯总人口护栏、敌情攻击优先和手动
  超预算不自毁。
- **在线结果**：扩容确实把编制从 `W13/V4/R4` 推到 `W16/V4/R4`、人口 `21→24`，Worker 单价为
  `7`；库存约 `99→78` 后只回升到约 `80`，没有证明净资源收益改善。20 Tick 安全 canary 中
  新增约 6 次 `UNIT_MOVE_FAILED/CELL_UNIT_LIMIT`，集中在 Core 邻格，明显高于旧窗口同期 1 次；
  Core 未死亡，但移动拥塞风险超过收益证据门槛。
- **处置**：生成回滚提交 `4be4b41` 并推送到 `origin/main`，重启唯一 `io.arenahero.tactic`。
  回滚只恢复代码策略，不未经授权销毁已生成的 W16；因此回滚后的 `t72789..t72808` 仍以现有
  `W16/V4/R4` 运行，20 个唯一 Tick 无生产、无死亡、无 Core 异常，资源 `82→85`，2 次采集、3 次
  入核、2 次移动失败。该窗口只证明回滚代码安全，不能作为扩容收益样本。
- **下一步**：若重新尝试扩容，先离线构造 Worker 与 Vanguard/Ranger 同 Tick 目标冲突的回归测试，
  明确移动预约覆盖所有同 Tick 目的地并验证 Core 邻格容量；在修复前不得再次放宽人口或把短窗库存
  变化当作长期资源收益。继续累计稳定版本的 100–200 Tick 与 20 条完整采集→入核链路，并按
  官方规则/SDK 版本复核后再决定下一变量。

## `RULE-006` Worker 读取战斗单位同 Tick 目的地预约（SAFE_CANARY_VERIFIED，待长窗收益，2026-08-08）

- **问题证据**：`decide()` 先排队 Ranger/Vanguard 的移动，再进入 `_control_workers()`；Worker 原先
  只维护自己的 `reserved_destinations`。扩容窗口的 Core 邻格 `CELL_UNIT_LIMIT` 表明 Worker 可能
  把已有战斗单位将要进入的格子再次预约成第三个实体，导致战斗单位移动失败或增加拥塞。
- **单一修复**：在 `_control_workers()` 初始化预约集合时读取当前 Tick 已排队的非 Worker `MOVE` 目的地，
  采用现有 Worker 预约相同的保守一格一预约策略；冲突 Worker 留待下一 Tick，不改战斗目标、A*、
  生产、敌情信号或 Manual 优先级。
- **离线验收**：新增“Vanguard 进入已有 Ranger 占一席的格子、Worker 同 Tick 试图进入同格”回归，
  修复前稳定失败，修复后 Worker 不再提交冲突移动；全量测试 `175 passed`，目标测试、
  `compileall`、`uv pip check` 与 `git diff --check` 需在提交前再次确认。
- **live canary**：代码提交并推送后重启唯一 `io.arenahero.tactic`；从新计划完成结算后的下一 Tick
  观察 20 个唯一 Tick，要求无新增 `CELL_UNIT_LIMIT`/`MOVE_CONTESTED`、资源失败、窗口/协议/提交
  错误、Core/Unit 死亡。随后累计至少 100–200 Tick 与 20 条完整采集→入核链路，比较移动失败率、
  入核率、净资源/Tick 和决策耗时；不能用短窗库存变化宣称收益。`5868021` 重启后的
  `t72859..t72878` 20 个唯一 Tick 已通过护栏：无移动/资源失败、死亡、Core 伤害或提交异常，
  资源 `90→91`，2 次采集、1 次入核；这是安全结果，不是长期收益结论。
- **回滚**：若 Worker 等待导致采集/入核率下降 10% 以上、战斗单位仍频繁失败、或出现 Core/窗口/协议
  故障，只反向提交本规则修复；保留测试和运行归档，不销毁现有 W16。

## `RESOURCE-DISPATCH-007` 四象限 Worker 分散巡查 + 双层战斗巡逻（SAFE_CANARY_VERIFIED，长窗观察中，2026-08-08）

- **用户问题**：当前 Worker 位置明显集中，资源发现效率低；用户要求空载 Worker 分散巡查，且由
  Vanguard/Ranger 组成近核与远核两层巡逻队，在 Core 周边发现敌人时主动处理并把资源发现写入持久地图。
- **可信基线**：旧进程窗口 `t72858..t72960` 共 103 条 state/102 次转移，资源 `90 -> 97`，自然
  `HARVEST_SUCCEEDED=6`、`DEPOSIT_SUCCEEDED=7`；完整采集→入核延迟样本 `[13,15,16,30,32,37]`
  Tick，P50=23、P95=32。最近窗口约 13/16 个 Worker 落在同一东北象限；Core 邻格没有 Worker
  聚集，说明瓶颈主要是发现和远程回程，不是 Core 容量。
- **实现**：
  1. 持久地图前沿不足时，空载 Worker 使用四象限远端巡查站点；站点距 Core 的 Manhattan 距离为 32，
  每象限 6 个、共 24 个，当前人口上限内不复用目标。站点保持 128 Tick，给从 Core 出发的 Worker
     到站和稳定观察留出时间；已知可采资源和真实前沿仍高于巡查站点。
  2. 其余 Vanguard/Ranger 组成最多两组：近层半径约 7、远层半径约 15；第一名 Ranger 保留 Core
     近身守卫。每 24 Tick 轮换环形站点；无敌情时巡逻，有敌情时先攻击/追击/护核。巡逻目标不可达时
     回退到护核逻辑，不让错误站点锁死战斗单位。
  3. 战斗单位和 Worker 共用当前 Tick MOVE 目的地预约，避免同一目的格被排成第三个实体；巡逻单位
     的视野仍通过既有 `_observe_resources()` 写入 `known_resources/resource_hints/explored_cells`。
- **静态验收**：全量 `pytest` `183 passed`；`compileall`、`uv pip check`、`git diff --check` 通过。
  周期锚定进程首次 authoritative Tick，避免重启边界立即换站；在加载真实持久地图规模（6485
  explored cells、1054 obstacles）的本地假状态上，100 次 `decide` 的 P50/P95/最大耗时约
  `13.8/177/281ms`，A* 预算耗尽为 `0`。
- **安全 canary（2026-08-08）**：唯一 PID `80941` 重启后，严格排除重启边界的旧计划结算，
  `t73013..t73032` 共 20 个连续唯一新 Tick；无窗口/协议/提交/决策错误、无 `MOVE_FAILED`、资源
  失败、Core/Unit 死亡或 Core 伤害。库存 `100→104`，11 个 Tick 看到资源格，1 次采集、4 次入核；
  `decide_ms` P50/P95/最大为 `100/192/1779ms`，总本地耗时 P50/P95/最大为 `1060/3229/9462ms`，
  仍低于 15 秒命令窗口，但 submit 长尾需继续监控。由于完整采集→入核配对样本仍不足 20，且窗口内
  没有敌情，主动出击行为和长期资源收益均未验收。
- **锚定修正版 canary（2026-08-08）**：提交 `4258a17` 后唯一 PID `90297` 从 `t73056..t73075`
  连续 20 个新 Tick；库存 `105→107`，2 次采集、2 次入核，8 个 Tick 看到资源格。全部
  `ST[ACCEPTED]`，无窗口/协议/提交/决策错误、`MOVE_FAILED`、资源失败、Core/Unit 死亡或 Core 伤害。
  `decide_ms` P50/P95/最大 `74/145/185ms`，总本地耗时 P50/P95/最大 `1024/1112/1157ms`；
  本 canary 只证明锚定修正的安全性，仍没有敌情和足够完整链路样本。
- **长窗计划**：继续保持唯一实例，累计至少 100–200 Tick、20–50 条完整采集→入核链路，比较四
  象限覆盖、发现→分配、采集→入核 P50/P95、入核资源/Tick 和预约造成的等待 Tick。未达到长窗样本前，
  只能接受安全 canary，不能宣称资源收益最优。
- **回滚**：若连续两个 100-Tick 窗口入核率下降 10%、采集→入核 P95 上升 25%、四象限覆盖未改善，
  或出现 `CELL_UNIT_LIMIT`/`MOVE_CONTESTED`、Core/窗口/协议故障，恢复上一策略提交；保留地图和
  运行归档，不删除用户的 `uv.lock` 或 archive 文件。

## `RESOURCE-DISPATCH-008` 延长四象限站点驻留窗口（SAFE_CANARY_VERIFIED，长窗观察中，2026-08-08）

- **根因证据**：`c13a74b` 至 `81f5b44` 的旧策略在 PID `90297` 的 `t73055..t73120` 共 66 个
  Tick 中，Worker 最终仍偏向 NW/NE（窗口均值约 NW `7.06`、NE `6.17`、SE `2.03`、SW `0.74`）。
  专家按真实持久地图复核发现，32 格站点的实际绕障路径最长约 53 Tick；旧的 64-Tick 阶段在部分
  Worker 到站前就换站，造成持续北侧路径偏置，而非四象限目标生成失效。
- **变更**：提交 `c6af83a` 将 `_WORKER_SECTOR_PHASE_TICKS` 从 `64` 调整为 `128`，保留资源/历史
  目标优先级、四象限唯一站点、进程首次 authoritative Tick 锚定和双层战斗巡逻；没有改人口、A*、
  战斗或资源认领规则。新增边界测试确保锚点 `0` 下 `t=127` 不换站、`t=128` 才轮换。
- **静态验收**：`pytest` `183 passed`，`compileall`、`uv pip check`、`git diff --check` 通过；
  SDK 仍为官方 `arena-hero 0.2.9`，与 v0.14 规则一致。
- **新进程安全 canary**：重启 LaunchAgent 后唯一 PID `4061` 从完整的 `t73169..t73188` 观察 20 个
  accepted Tick；排除重启边界 `t73168` 的一次 `TICK_MISMATCH`。资源 `117→120`，2 次采集、3 次入核；
  无 `UNIT_MOVE_FAILED`、`CELL_UNIT_LIMIT`、`MOVE_CONTESTED`、资源失败、死亡、Core 伤害或持续提交错误。
  该窗口有一次远距敌核可见（约 43 格，超出当前追击/射程），不能作为主动攻击收益样本；`NO_RAID` 为
  通用告警，不据此改战斗逻辑。
- **验收状态**：这是驻留周期修复的安全证明，不是长期资源收益证明。继续累计至少 128–200 个新 Tick，
  比较四象限最终分布、资源/Tick、完整采集→入核 P50/P95、满核持续和容量电梯行为；若连续两个 100-Tick
  窗口入核率下降 10% 或 SW 覆盖仍低于 1 且无新发现，再回滚到上一周期。

## `RESOURCE-DISPATCH-014` 当前 Tick 巡查站点唯一预约（SAFE_CANARY_VERIFIED，继续观察，2026-08-08）

- **问题证据**：`RESOURCE-DISPATCH-008` 的完整窗口 `t73169..t73296`（唯一启动 marker `24191`）
  资源 `117→120`，采集 `10`、入核 `3`，移动/竞态失败和死亡均为 `0`；Worker 象限均值仍为
  `NW 8.24 / NE 4.63 / SE 1.55 / SW 1.20`。用同一份持久障碍地图重放站点选择时，Worker index `1`
  和 `5` 在候选 `(8,-24)` 被障碍挡住后都回退到相同相对站点 `(12,-20)`，说明“目标生成唯一”只在
  无障碍夹具成立，真实绕障时会重复预约并放大北侧偏置。
- **单一变更**：为 `_worker_sector_patrol_target()` 增加当前 Tick 的 `reserved` 集合；
  `_control_workers()` 先登记已有前沿目标，再逐个登记新巡查站点，候选命中已预约格时继续按固定顺序
  尝试下一个站点。不改变 128 Tick 驻留、资源/历史目标优先级、战斗巡逻、人口或生产策略。
- **静态验收**：先加入障碍绕行重复目标回归并确认旧实现失败，再实现修复；全量 `pytest 184 passed`，
  `compileall`、`uv pip check`、`git diff --check` 通过。基于真实持久障碍集合重放，16 个 Worker
  站点变为 `16/16` 唯一，四象限各 `4` 个；本地 SDK `arena-hero 0.2.9` 与 gameplay `v0.14` 一致。
- **新版本 canary**：PID `11962`、启动 marker `24480`，严格窗口 `t73311..t73330` 共 20 个
  唯一状态；全部 `ST[ACCEPTED]`，无提交/窗口/移动/资源失败、死亡或 Core 伤害，Core 保持 `5/5`，
  `decide_ms` P50/P95/最大 `92.5/172.25/177ms`。资源保持 `120/120`，自然采集 `1`、入核 `0`；
  该窗口样本仅证明安全，不证明长期收益或四象限最终覆盖，敌核可见 `2` Tick 的通用 `NO_RAID` 不作为
  攻击失败证据。
- **后续门槛**：继续保持唯一实例，至少再积累 `100–200` 个唯一 Tick 与完整采集→入核链路；比较
  四象限覆盖、资源/Tick、采集→入核 P50/P95、重复站点数和移动失败率。若连续两个 100-Tick 窗口
  入核率下降 `10%`、站点重复仍出现或出现 `CELL_UNIT_LIMIT`/`MOVE_CONTESTED`、Core/窗口/协议故障，
  反向提交本次修复；满核容量电梯另立单变量任务，不在本轮混改。

## `RESOURCE-DISPATCH-015` 可见资源唯一可达 Worker 与探索保留冲突（STATIC_VERIFIED，待 canary，2026-08-08）

- **问题证据**：`RESOURCE-DISPATCH-014` 之后的离线夹具显示，近 Worker 可能被障碍和自有 Core
  封死，远 Worker 才能到达可见资源。仅按 Manhattan 距离会把资源认领给近者；加入可见资源 A*
  预检后，后续“历史资源存在时保留一个探索 Worker”的分支仍可能把唯一可达远者排除，匹配再次退回
  近者。该组合会让可见资源永远不采集。
- **单一修复**：对至多 4 个当前可见资源执行有界 A* 可达性预检；预检把自有 Core 作为空载 Worker
  不可进入的动态阻塞；已证明无任何可达 Worker 的可见节点不进入匹配；对探索保留增加覆盖护栏，
  只有每个可见节点仍有其他可达 Worker 时才牺牲一个探索名额，同时保持已通过迟滞的历史认领优先级。
  历史资源仍使用原有有界 A* 与冷却，不做全图路径预检。
- **回归覆盖**：新增“近者封死、远者可达”“历史资源触发探索保留但不得抢走唯一可达者”“最终
  Worker 计划实际向可达资源移动”三项测试；测试夹具支持障碍批次。
- **静态验收**：全量 `pytest` `187 passed`；`compileall`、`uv pip check`、`git diff --check` 通过。
  预检只作用于最多 4 个可见节点；专家重放 16 Worker/4 节点约 80 次 A*，正常场景约 38ms，
  全封死场景约 257ms、预算命中 68 次，仍低于 15 秒命令窗口但必须继续监控 `astar_budget_hits`
  和 `decide_ms` 长尾。
- **现有 live 证据边界**：`t73311..t73441` 的旧 PID 在本补丁加载前运行，不能归因于本修复；该窗口
  资源 `120→125`、`7` 次采集、`12` 次交付、无移动/死亡/Core 伤害，但有一次 `TICK_MISMATCH`，
  且完整链路样本不足。提交后必须重启唯一进程，从新计划完成结算后的下一 Tick 起重新计数。
- **canary 与接受门槛**：先观察 20 个唯一 Tick，无认证/协议/提交/窗口错误、资源失败、移动失败、
  Core/Unit 死亡；随后累计 100–200 个 Tick 和至少 20 条完整采集→入核链路，比较可见资源采集延迟、
  不可达计数、探索保留、`astar_budget_hits`、`decide_ms` P50/P95、入核资源/Tick。样本不足时只
  报告安全与行为修复，不宣称长期收益提升。
- **回滚**：若连续两个 50-Tick 窗口入核率下降 10%、可见资源采集 P95 上升 25%、A* 长尾逼近命令
  窗口，或出现 Core/窗口/协议故障，只反向提交本任务改动；保留持久地图、`uv.lock` 和归档状态文件。

## `RESOURCE-DISPATCH-016` 可达资源覆盖必须满足全局匹配（SAFE_CANARY_VERIFIED，长窗观察中，2026-08-08）

- **新增边界证据**：4 个 Worker、3 个可见节点和 1 个历史提示的夹具中，A 只能到达两个节点，B
  只能到达第三个节点，C/D 均不可达。逐资源检查会误以为最远 C 可以保留探索名额；即使不保留，
  普通 Hungarian 也可能为了填满 3 个资源，把第二个节点分给不可达 D。
- **单一修复**：探索名额候选逐个移除后，用可达边做确定性最大二分匹配，只有能覆盖全部当前可见
  节点才保留探索名额；最终分配对 A* 已证明不可达边做后过滤，宁可少派一个资源也不把目标交给
  不可达 Worker。历史认领迟滞、可见资源优先和不确定（预算耗尽）路径语义不变。
- **回归覆盖**：新增“可达边共享时不保留探索者、不可达边不为凑数分配”的测试；全量 `pytest`
  `188 passed`，`compileall`、`uv pip check`、`git diff --check` 通过。
- **canary 边界**：上一提交 `b4bf4b7` 的新进程窗口约 `t73456..t73471` 暂无移动/资源失败，
  但这只证明上一版本；`20e5edd` 的新版本 20-Tick checkpoint 见 `RESOURCE-DISPATCH-017`
  小节，仍需累计 100–200 Tick/至少 20 条完整采集→入核链路。
- **回滚**：若可见资源覆盖下降、`HARVEST_FAILED/RESOURCE_DEPLETED` 增加、A* 长尾逼近命令窗口，
  或出现 Core/窗口/协议故障，只反向提交本 follow-up；保留 `b4bf4b7` 作为可回退版本，不删除地图和归档。

## `RESOURCE-DISPATCH-017` 保留历史认领时不得牺牲最后的可见资源 Worker（SAFE_CANARY_VERIFIED，长窗观察中，2026-08-08）

- **新增边界证据**：2 个 Worker 中 A 已保留历史认领，B 是唯一空载 Worker；可见资源出现时，旧
  探索分支仍会把 B 预约为 explorer，导致计划只剩 A→历史点、可见资源无人采集。
- **单一修复**：探索候选只从非 retained Worker 中选择，并在删除候选后对剩余 dispatch Worker
  做可达最大匹配；若没有足够 Worker 覆盖全部可见节点，则不保留探索名额，保留历史认领并让空载
  Worker 采集当前资源。
- **回归覆盖与静态结果**：新增 retained 历史认领回归；全量 `pytest` `189 passed`，并通过
  `compileall`、`uv pip check`、`git diff --check`。本项与 `RESOURCE-DISPATCH-016` 同一资源调度
  follow-up，未改变巡逻、战斗、人口或容量电梯门槛。
- **canary**：`20e5edd` 提交后已重启唯一进程并完成 20 Tick 安全窗口（详见下方 checkpoint）；
  不得把 `b4bf4b7` 窗口的安全结果归因给本版本，仍需累计 100–200 Tick/至少 20 条完整采集→入核链路。

### `RESOURCE-DISPATCH-017` 新版本安全 checkpoint（2026-08-08）

- 提交 `20e5edd` 后重启唯一 PID `23156`；严格排除重启边界，从 `t73494..t73513` 统计
  20 个连续唯一 Tick。
- 20 个 Tick 全部为 `ST[ACCEPTED]`；`TICK_MISMATCH`、窗口/提交错误、`MOVE_FAILED`、
  `MOVE_CONTESTED`、`CELL_UNIT_LIMIT`、`HARVEST_FAILED/RESOURCE_DEPLETED`、Unit/Core 损失和
  Core 伤害均为 `0`。A* `budget_hits=6`，未逼近命令窗口。
- 资源保持 `125/125`，人口保持 `25 (W17/V4/R4)`；自然采集 `2`、入核 `0`。这只能证明
  可达性匹配修复没有破坏安全性，不能证明长期资源收益提升。20-Tick 窗口后的持续观察中，
  严格容量电梯最终在 `t73555` 记录 `full1/elev1`，下一状态 `t73556` 变为
  `r117/130、pop26(W18/V4/R4)`，并收到 `CORE_SPAWN_SUCCEEDED`（Worker 成本 `8`）。
- `t73494..t73543` 的约 50 个 Tick 内，满核但多数时刻只有 `14–16/17` 个 Worker 带货，
  `full_core_loaded=false`，形成可观测的 `IDLE_GOLD` 等待段；这证明原严格条件会延迟扩容，
  但不是永久卡死。因此本轮不放宽容忍度，先继续验收扩容后的入核链路和资源净流；扩容策略
  仍作为独立变量，不把等待段归因于可达性匹配。

## `RESOURCE-DISPATCH-018` 追击回退不得解封敌方格（SAFE_CANARY_VERIFIED，长窗观察中，2026-08-08）

- **问题证据**：`_control_rangers()` 的 A* 追击路径把敌方格放进 `blocked`，但 A* 失败后的贪心
  回退使用 `blocked - {chase}`，会在目标正好相邻时提交进入敌方格。该路径与“停在可射击格”的
  注释相矛盾，离线回归先失败后确认了这个行为。旧 live 的 `t73593` 一次 `MOVE_CONTESTED`
  实际由 Worker 与同步移动的敌 Worker 产生，不把它错误归因给 Ranger 修复；但它暴露了同类
  动态碰撞必须单独记录的边界。
- **单一修复**：只把追击贪心回退恢复为使用完整 `blocked` 集合，并同步修正注释；不改变
  `_chase_target()` 的目标记忆、动态重建储备、近核守卫、近/远巡逻编组、Worker 调度或人口门槛。
- **静态验收**：新增“Ranger 在相邻追击目标前不得 MOVE 进入敌格”回归，先在旧实现上复现失败；
  修复后全量 `pytest` `190 passed`，`compileall`、`uv pip check`、`git diff --check` 通过。提交
  `4c9f643` 已推送 `origin/main`，SDK `0.2.9` 与规则 `v0.14` 未变。
- **新版本安全 canary**：停止旧 PID `23156` 后由 LaunchAgent 自动拉起唯一 PID `33191`；严格窗口
  `t73610..t73629` 共 20 个连续 Tick，全部 `ST[ACCEPTED]`，无窗口/提交/决策错误、`MOVE_FAILED`
  （含 `MOVE_CONTESTED`/`CELL_UNIT_LIMIT`）、资源失败、死亡或 Core 伤害。库存 `129→130/130`，
  `DEPOSIT_SUCCEEDED=1`、自然采集 `0`，W18/V4/R4、Core `5/5`；A* budget hits `12`，
  `decide_ms` P50/P95/最大 `76/203/319ms`，总本地耗时 P50/P95/最大 `1006/1173/1267ms`，
  均远低于 15 秒命令窗口。资源在第 20 Tick 后重新可见，21-Tick 延伸样本出现 1 次采集，
  说明安全窗口的低采集量不能单独判定探索失效。
- **后续门槛**：继续保持唯一实例，累计至少 100–200 个新 Tick 与 20–50 条采集→入核链路；
  分开比较四象限覆盖、发现→分配、入核资源/Tick、追击 MOVE_CONTESTED 和 A* 长尾。当前只接受
  安全结论，不放宽容量电梯或继续生产普通 Worker；若连续两个 50-Tick 窗口出现敌格 MOVE、
  `CELL_UNIT_LIMIT`、Core/窗口/协议故障，再回滚本单变量修复。

## `RESOURCE-DISPATCH-019` 四个 Worker 固定四象限回扫（SAFE_CANARY_VERIFIED，长窗观察中，2026-08-09）

- **用户问题与基线**：持久地图已积累后，Worker 历史上长期集中在北/东区域，约有 `2000` 个 Tick
  没有采集；长窗的象限占比约为 `WN 49% / EN 30% / WS 12.5% / ES 8.4%`。最近 `500` Tick
  已回到约 `WN 30.1% / EN 37.2% / WS 14.6% / ES 12.5%`，但仍存在东/北偏置，不能把短期恢复
  解释成长期发现效率已最优。当前旧版本窗口约 `t73610..t78803`，唯一 Tick、协议和提交链路正常，
  Core 保持 `5/5`；长窗仍记录 `MOVE_CONTESTED`，应在新版本 canary 中单独归因。
- **单一变更**：将无当前可见资源时的持久 Chunk 回扫名额从 `2` 提高到 `4`；当空载 Worker 至少
  `4` 个时，按稳定 ID 预留最多 `min(4, N-1)` 个，优先分别绑定 NW/NE/SW/SE 巡查站点。只有
  `4` 个空载 Worker 时保留 `N-1` 的前沿余量，四向覆盖会在下一名 Worker 出现后完整启用；当前
  可见资源分配、带货回 Core、障碍绕行、站点唯一预约和持久地图写入逻辑保持不变。
- **静态验收**：增加“前沿目标存在时仍保留四象限回扫”回归，修正旧的两名巡查断言；全量
  `pytest` `191 passed`，`compileall`、`uv pip check`、`git diff --check` 均通过。由于虚拟环境
  未安装 `pip`，依赖检查使用官方 `uv pip check`。
- **20-Tick 安全 canary（2026-08-09）**：提交 `3cb7b94` 后只重启一个实例，新 PID `13209`；重启
  边界 `t78812` 的一次 `TICK_MISMATCH` 单独剔除，严格窗口 `t78813..t78832` 共 `20` 个唯一
  `ST[ACCEPTED]` Tick，重复/失败/窗口错误均为 `0`。无 `MOVE_CONTESTED`、`CELL_UNIT_LIMIT`、
  `HARVEST_FAILED`、Unit/Core 损失或 Core 伤害；资源 `97→101`，自然采集 `2`、交付 `5`、完整
  采集→入核配对 `1`，刷新预约 `12` 次。`decide_ms` P50/P95/P99=`141/382.25/583.65ms`，
  本地总耗时 P50/P95/P99=`1184.5/1408.7/1601.74ms`，均低于 15 秒窗口；该结果只证明安全，
  不证明四象限长期覆盖或净资源收益提升。
- **后续观察**：继续保持唯一实例，累计 `100–200` 个 Tick 与至少 `50` 条
  `HARVEST_SUCCEEDED → DEPOSIT_SUCCEEDED` 链路，比较四象限占比、发现→分配延迟、入核资源/Tick、
  完整链路 P50/P95、刷新名额和决策耗时。安全 canary 不等于收益提升。
- **100-Tick 长窗 checkpoint（2026-08-09）**：严格窗口 `t78833..t78932` 共 `100` 个唯一
  `ST[ACCEPTED]` Tick，重复/失败/窗口错误、`MOVE_CONTESTED`、`CELL_UNIT_LIMIT`、资源失败、
  死亡和 Core 伤害均为 `0`。资源 `101→114`，自然采集 `7`、交付 `13`、完整采集→入核配对 `4`，
  刷新预约 `272` 次；`decide_ms` P50/P95/P99=`93.5/231.25/333.51ms`，本地总耗时
  P50/P95/P99=`1159/1407.4/1496.57ms`。Worker 位置为 `NW34.2% / NE34.3% / SW10.9% /
  SE12.9%`（另有约 `8%` 在 Core 同轴/同格）；北侧偏置尚未消失，但窗口结束于 128-Tick 驻留
  轮换之前，不能据此回滚或宣称四象限收益完成。继续跨越下一驻留周期观察。
- **专家复核门槛**：该窗口对应进程锚点后的第 `21–120` Tick，首个 128-Tick 轮换点为 `t78940`，
  因而北侧偏置尚未可归因。继续观察 `t78940..t79195` 的两个完整驻留周期；保持移动/资源失败、
  伤亡和 Core 风险为 `0`，`decide_ms P95<1000ms/P99<2000ms`，净资源/Tick 不低于当前约 `0.13`
  的 `90%`，完整链路 P95 不超过约 `41` Tick。若两周期后仍无法归因，先增加只读巡查 Worker/目标
  遥测，不直接修改战术行为。
- **回滚**：若连续两个 `100`-Tick 窗口入核率下降 `10%`、四象限仍未改善、完整链路 P95 上升
  `25%`，或出现移动竞态、Core/窗口/协议故障，只恢复本任务的战术提交；保留持久地图、`uv.lock`
  与用户已有 archive 文件。

## `RESOURCE-DISPATCH-020` 修复巡查站点到达后的隐式回退（SAFE_CANARY_VERIFIED，继续观察，2026-08-09）

- **问题证据**：专家用真实 Worker 夹具复现：`_patrol_step()` 在 `pos == goal` 时返回 `None`，
  `_control_workers()` 随后把该结果当成“没有巡查动作”，继续调用 `_explore_step()`；因此 Worker
  到达 128-Tick 回扫站点后会立刻离站，文档声称的驻留窗口没有真正成立。旧进程
  `t78940..t78999` 的方向统计不能验证本修复，且已停止旧监测会话。
- **单一修复**：Worker 在回扫站点到达时显式提交 `WAIT` 并结束本 Tick；Vanguard/Ranger 在近/远
  巡逻站点到达时同样显式 `WAIT`。敌情攻击、治疗、护核、资源分配、`min(4, N-1)` 前沿余量和
  站点预约均不变，避免把多个行为变量混入同一轮。
- **回归覆盖**：新增 Worker 到站防止 `_explore_step()` 回退、近/远战斗巡逻到站显式 `WAIT` 两项
  测试；全量 `pytest` `193 passed`，`compileall`、`uv pip check`、`git diff --check` 通过。
- **20-Tick canary（2026-08-09）**：提交 `6045042` 后重启唯一 PID `18966`；严格窗口
  `t79015..t79034` 共 `20` 个唯一 `ST[ACCEPTED]` Tick，重复/缺口/窗口/提交错误均为 `0`。
  无 `MOVE_CONTESTED`、`CELL_UNIT_LIMIT`、资源失败、Unit/Core 损失或 Core 伤害；库存 `120→122`，
  自然采集 `1`、交付 `2`，人口稳定 `29 (W21/V4/R4)`。`decide_ms P50/P95/P99=`
  `96/181.75/193.15ms`，本地总耗时 `P50/P95/P99=1156.5/1230.35/1250.87ms`，均低于命令窗口。
  日志确认回扫 Worker 在到站后持续提交 `WAIT`，没有被 `_explore_step()` 覆盖；该窗口只证明安全和
  驻留生效，不证明长期收益提升。
- **后续验收**：继续保持唯一实例，至少观察两个完整 128-Tick 巡查相位；同时比较到站 WAIT 比例、
  四象限覆盖、发现→采集→入核延迟和净资源/Tick。必须继续满足全 Tick accepted、无窗口/提交/移动/
  资源失败、无伤亡/Core 伤害、`decide_ms P95<1000ms/P99<2000ms`。
- **回滚**：若连续两个完整相位仍出现站点到达后下一 Tick 非预期 MOVE、四象限覆盖无改善且入核率
  下降 10%，或出现 `CELL_UNIT_LIMIT`/`MOVE_CONTESTED`、Core/窗口/协议故障，只反向提交本修复；
  保留持久地图、`uv.lock` 与用户已有 archive 文件。

## `RESOURCE-DISPATCH-021` 回扫 cohort 重新编号修复 Worker 象限偏置（LONG_WINDOW_VERIFIED，继续收益采样，2026-08-09）

- **问题证据**：`t79059..t79078` 的 20 Tick 观察中，四个固定回扫 Worker 的完整 Worker 索引为
  `[1,5,9,13]`，原逻辑按全体舰队索引 `%4` 计算象限，四者全部落在 NE；该窗口 Worker 聚合为
  `NW646/NE829/SW326/SE196`，北侧占 `70.2%`。战斗单位仍维持近/远两层巡逻，未见移动、资源或伤亡失败。
- **可证伪假设**：回扫 Worker 的子集索引空洞是偏置主因；将当前巡查 cohort 连续编号后，四名回扫
  Worker 应覆盖至少三个、目标设计上四个象限，同时不改变资源任务优先级、128 Tick 驻留、战斗巡逻
  或人口生产。
- **单一变更**：`_worker_sector_patrol_target()` 新增可选 `cohort_rank`；`_control_workers()` 对
  当前回扫/扇区巡查子集按稳定 UUID 连续编号，再传入该排名。没有排名时保留原索引兼容行为。
- **回归覆盖**：新增间隔索引 `[1,5,9,13]` 的直接站点象限测试，以及真实控制器确认传入排名
  `[(1,0),(5,1),(9,2),(13,3)]`；全量 `pytest 195 passed`，`compileall`、`uv pip check`、
  `git diff --check` 通过。
- **live canary**：提交后重启唯一实例，从新进程首个完整状态开始观察至少 20 Tick；要求全 Tick
  `ST[ACCEPTED]`、无窗口/提交/协议/移动/资源失败、无 Unit/Core 死亡或 Core 伤害。记录固定回扫
  目标的象限覆盖、`max_sector_share`、站点唯一数、资源 `HARVEST→DEPOSIT` 链路与
  `decide_ms P95/P99`。随后至少跨一个 128 Tick 回扫驻留轮换，再判断长期收益。
- **停止/回滚**：若连续两个 50-Tick 窗口仍出现四名回扫全在一象限，或入核资源/Tick 下降 10%、
  `MOVE_CONTESTED`/`CELL_UNIT_LIMIT`/资源失败、Core/窗口/协议故障，反向提交本任务；不回滚已验证的
  战斗巡逻和持久地图逻辑。
- **首轮 live canary**：提交 `fbd59d0` 后重启唯一 LaunchAgent 实例 PID `22321`，从新进程边界
  严格统计 `t79106..t79125` 的 20 个唯一 Tick。20/20 为 `ST[ACCEPTED]`，重复/失败/窗口/提交/
  决策错误、Tick 缺口、`MOVE_CONTESTED`、`CELL_UNIT_LIMIT`、资源失败、Unit/Core 死亡和 Core 受击
  均为 0；Core 保持 `hp5/sh5`，人口保持 `29 (W21/V4/R4)`。窗口采集 3、入核 1（净入核 `+1`），
  资源从 `127/145` 未下降；`decide_ms` 最大 217ms，未触及时限门槛。该 canary 只证明安全和运行
  正确性，未记录到完整巡查目标象限遥测，不能据此宣称长期收益已提升；继续观察至少一个 128-Tick
  驻留周期和 50 条完整采集→入核链路。
- **50-Tick checkpoint（2026-08-09）**：同一 PID `22321` 的严格窗口 `t79106..t79155` 全部
  `ST[ACCEPTED]`，无重复、缺口、窗口/提交/决策错误；`UNIT_MOVE_SUCCEEDED=1175` 且移动失败、
  资源失败、Unit/Core 损失和 Core 受击均为 `0`。采集 `4`、入核 `3`，资源 `127→129/145`；
  四名固定回扫 Worker 的象限采样 `NW73/NE64/SW13/SE50`，`max_sector_share=36.5%`，已覆盖
  四个象限但仍需跨相位验证长期均衡。`decide_ms P50/P95/P99/max=87/192.25/259.03/283ms`，
  本地总耗时 `P50/P95/P99/max=1126.5/1280.95/1335.29/1346ms`；继续等待至少一个完整
  128-Tick 回扫驻留周期和 50 条完整采集→入核链路，不把该窗口当成收益最优证明。
- **128-Tick 驻留 checkpoint（2026-08-09）**：严格窗口 `t79106..t79233` 共 `128/128` 个唯一
  `ST[ACCEPTED]` Tick，无缺口、窗口/提交/决策错误、移动/资源失败、Unit/Core 损失或 Core 受击；
  `UNIT_MOVE_SUCCEEDED=3007`，资源 `127→132/145`，自然采集事件 `11`、入核 `6`、完整采集→入核
  链路 `4`（延迟 P50/P95=`36.5/47.35` Tick）。全体 Worker 位置采样为
  `NW25.7% / NE30.2% / SE23.4% / SW20.6%`，最大象限占比 `30.2%`；四名固定回扫 Worker
  四象限均有样本（`NW229/NE142/SE128/SW13`），说明 cohort 重编号已消除“全员 NE”故障，
  但固定队伍路径仍受障碍和到站时机影响，不能把一个相位当成全局最优。
  `decide_ms P50/P95/P99/max=94.5/218.95/274.09/317ms`，本地总耗时
  `P50/P95/P99/max=1134/1346.65/2040.57/2751ms`，均留在命令窗口安全范围。
- **敌情归因**：窗口 `t79168..t79174` 可见敌 Core 均在 `[323,143]`，距我方 Core `[347,124]`
  为曼哈顿 `43`；战术的动态 Core hunt 门槛是 `<40`，因此没有排队追击或射击是预期行为。
  监控器的 `NO_RAID` 只按“可见 Core + 资源预算”报警，未纳入距离，属于保守误报；不把它与本次
  Worker 回扫修复混改。
- **后续门槛**：本次已完成一个 128-Tick 相位的安全/覆盖验证；继续累计至少 `50` 条完整采集→入核
  链路并观察第二相位，再决定是否只读修正 `NO_RAID` 监控半径或调整经济扩容。暂不改变战斗巡逻、
  hunt 半径、人口生产或资源任务优先级。

## `ECONOMY-CAP-008` 解除历史 W21 容量电梯上限（SAFE_CANARY_VERIFIED，长窗观察中，2026-08-09）

- **当前事实**：规则 gameplay `v0.14` 没有人口 20 硬上限，SDK `0.2.9`；但策略的
  `FREE_UPKEEP_CAP=20`、`ECONOMY_BRIDGE_MAX_WORKERS=15` 只是和平期软预算。当前 live
  严格窗口 `t79106..t79283` 共 `178` 个唯一 Tick，编制始终为 `pop29(W21/V4/R4)`，库存
  `127→137/145`，自然采集 `13`、入核 `11`，没有任何 `CORE_SPAWN_*`、移动/资源失败、伤亡或
  Core 受击。`worker_target=min(20-8,15)=12`，普通 Worker 桥不会再生产；容量电梯的
  `full_core_loaded` 全窗为 `0`，因此 W21 是历史扩容遗留，不是当前库存已满后的服务端卡点。
- **可证伪假设**：若真正出现“Core 满、全部 Worker 带货”的交付死锁，放宽一个容量槽到 W22
  可以用一次动态定价 Worker 把容量从 `145` 提到 `150`，恢复 `DEPOSIT` 出口；若该状态不出现，
  或扩容后的新增吞吐不足以回收成本，则不应继续上调人口。
- **单一变更**：仅将 `MAX_WORKERS` 从 `FREE_UPKEEP_CAP+1`（W21）改为
  `FREE_UPKEEP_CAP+2`（W22）。未改变 `FREE_UPKEEP_CAP`、`ECONOMY_BRIDGE_MAX_WORKERS`、
  军备目标、四象限 Worker 回扫、近/远战斗巡逻、资源匹配或容量电梯触发条件。仍必须同时满足
  和平、军备不短缺、Core 满、全部 Worker 带货、动态 Worker 价格加 3 点储备以及 Core 邻格可
  合法离核等门槛；当前 `r137/145`、仅约 `3–4/21` Worker 带货时不会触发。
- **离线验收**：新增 W21 满核全带货允许第 22 Worker 的回归；满核但未全带货、邻格无合法出口的
  负例仍不生产。全量 `pytest 196 passed`，`compileall`、`uv pip check`、`git diff --check`
  通过。
- **LIVE_CANARY**：提交后重启唯一 `play.py`，从新计划结算后的下一 Tick 起先观察 `20` 个唯一
  Tick，随后至少 `128` 个唯一 Tick。护栏必须保持全 Tick `ST[ACCEPTED]`、无窗口/提交/协议/
  `MOVE_FAILED`/资源失败、无 Unit/Core 损失或 Core 受击；若不出现 `full1/elev1`，记录为“容量
  压力不足”，不继续盲目放宽。若触发，记录 `CORE_SPAWN_SUCCEEDED` 实际成本、容量变化、
  `DEPOSIT`/Tick、完整链路 P50/P95 与扩容后 `256–300` Tick 净资源回本情况；当前基线 gross
  入核约 `0.0618/Tick`，扩容后至少提升 `5%` 且能回收一次 `8` 资源成本才可保留。
- **20-Tick 安全结果（2026-08-09）**：提交 `37e9336` 后重启唯一 PID `35678`，新进程边界为
  `t79316..t79335`，严格窗口 `20/20` 均为 `ST[ACCEPTED]`。编制保持 `pop29(W21/V4/R4)`，Core
  `5/5`，资源 `138→141/145`；事件只有合法 `UNIT_MOVE_SUCCEEDED` 与 `DEPOSIT_SUCCEEDED`，无窗口、
  提交、协议、移动/资源失败、Unit/Core 损失或 Core 受击。`full_core_loaded=0`、`capacity_elevator=0`
  全窗，说明 W22 门槛未触发；这只是安全验证，不是吞吐收益证明。
- **回滚**：出现任一窗口/协议/提交错误、移动/资源失败、伤亡/Core 受击，或扩容后两个干净
  `100`-Tick 窗口的 gross 入核下降 `10%`、链路 P95 上升 `25%`、`256–300` Tick 仍未回收成本，
  只反向提交本任务；不回滚已验证的持久地图、Worker cohort 重编号或战斗巡逻。

## `RESOURCE-DISPATCH-022` 巡查绕障回退修复与恢复后观察（RECOVERY_OBSERVING，2026-08-09）

- **问题证据**：`b8171b8` 修复 boxed Worker 抢占回扫意图后，真实障碍地图仍可能要求巡查站点
  路径先回一步；`_patrol_step()` 若同时使用 Worker 的 anti-backtrack 历史，会屏蔽这一步，A*
  随后退回贪心路径，在 Core 周边形成局部四格小环。该问题会让站点“目标生成正确”但实际到站时间
  失真，不能据此把 NW 偏置归因于象限配额。
- **单一变更**：提交 `c3cfd3e` 仅在有明确巡查站点目标时传入 `avoid=None`，允许 A* 为绕障回步；
  无目标的自由扫掠、带货回 Core 仍保留最近位置 anti-backtrack。配套回归确认巡查调用不再传入
  历史避让集合；未改变资源优先级、四象限站点、128 Tick 驻留、战斗巡逻、人口或生产门槛。
- **静态验收**：全量 `pytest 197 passed`；`compileall`、`uv pip check`、`git diff --check`
  均通过。策略代码基线为 `c3cfd3e`，SDK `0.2.9`、规则 `v0.14` 未变。
- **live 证据（截至 `t79532`）**：唯一 LaunchAgent PID `40708`。`t79454` 出现 1 次
  `TRANSPORT_ERROR`，不能把该段称为无错误 canary；错误后 `t79455..t79532` 共 `78` 个连续
  `ST[ACCEPTED]` Tick，未再出现提交/窗口/协议错误、移动/资源失败、Unit/Core 损失或 Core 受击。
  编制保持 `pop29(W21/V4/R4)`、Core `5/5`、库存 `145/145`；该恢复段有 `5` 次自然采集、
  `0` 次交付（容量已满）。Worker 位置采样约为 `NW 41.9% / NE 19.0% / SW 19.2% /
  SE 17.2%`，另有 `2.6%` 同轴；`decide_ms` P95 约 `203ms`，本地总耗时 P95 约 `7.73s`，
  低于 15 秒窗口但提交长尾仍需监控。
- **延伸观察**：`t79522..t79526` 在无当前资源时出现固定回扫 Worker 的 `WAIT`；`t79527` 资源
  `(340,98)` 出现后 `ref4→ref0`，计划恢复移动/资源调度。该切换证明巡查队确实开始到站，
  也说明不能用资源出现瞬间的当前象限直接判断站点覆盖是否失效。
- **专家结论**：当前持久地图约 `7580` 个已探索格、`1208` 个障碍；半径 40 内仅剩两个真实
  前沿候选，巡查站点重放目标约为 `NW6/NE5/SE5/SW5`。`_EXPLORATION_SECTOR_COVER=4` 在该
  场景不是 NW 偏置根因。资源出现/消失时，回扫 cohort 会从固定四人切换为全部空载 Worker，
  连续重排 `cohort_rank`，可能造成站点目标在短窗频繁换位；需要先用只读遥测确认，不能立即改策略。
- **下一步与停止条件**：至少跨进程锚点后的一个完整 `128` Tick 驻留相位（最好两个），新增
  `frontier_target_sector`、`patrol_target_sector`、A* ETA 与到站 `WAIT` 比例遥测；期间保持唯一
  进程、全 Tick accepted、无移动/资源失败、无伤亡/Core 受击，且 `decide_ms P95<1000ms`、
  `P99<2000ms`。若两个相位后遥测仍证明目标分配失衡，再单变量评估 `COVER=4→8` 或稳定
  `worker_id→patrol_quadrant`；在此之前不修改巡查半径、人口和战斗逻辑。

## `ECONOMY-CAP-009` W22→W23 容量电梯恢复出口（SAFE_CANARY_VERIFIED，继续长窗观察，2026-08-10）

- **单一变更**：仅把 `MAX_WORKERS` 从 W22 提到 W23；不改变巡查、战斗、资源分配或容量电梯触发条件。
  提交 `3036f7d`，规则 `v0.14`、SDK `0.2.9`，唯一 LaunchAgent PID `63313`。
- **触发与结算**：`t80090` 为 W22、`r150/150`、`full1/elev1`；`t80091` 成功生产第 23 个 Worker，
  `CORE_SPAWN_SUCCEEDED.cost=11`，编制变为 `W23/V4/R4`、人口 `31`、容量 `150→155`、库存
  `150→139`。首次扩容后 `DEPOSIT_SUCCEEDED` 在 `t80102`，相对结算延迟 `11` Tick。
- **严格观察窗**：`t80096..t80124` 共 `29/29` 个唯一 Tick，全部 `ST[ACCEPTED]`；累计自然采集 `1`、
  入核 `7`，库存 `139→146/155`，没有再次 `full_core_loaded` 或 `capacity_elevator`，无移动/资源失败、
  窗口/提交/协议错误、Unit/Core 损失或 Core 受击。扩容成本尚未完全回收：相对生产前库存净变化为
  `150→146`（`-4`），不能宣称收益已优化。
- **性能护栏**：覆盖 `t80090..t80125` 的 `36` 个可比较 Tick，`decide_ms` P50/P95/P99/max 为
  `101.5/194/380.05/477ms`，本地总耗时为 `1157/1604.5/1965.2/2094ms`，均低于 15 秒命令窗口。
- **50-Tick checkpoint**：严格窗口 `t80096..t80145` 共 `50/50` 个唯一 Tick，全部 accepted；自然采集 `2`、
  入核 `12`、`UNIT_MOVE_SUCCEEDED=1383`，移动/资源失败、窗口/提交/协议错误、伤亡和 Core 受击均为 `0`。
  资源已从 `139` 回到约 `151/155`，说明已收回 `11` 资源生产成本并有约 `1` 点净增；但仍未再次出现
  `full_core_loaded`，尚不能证明长期吞吐或容量瓶颈已经解决。
- **满核 checkpoint**：截至 `t80169`，扩容后严格窗口 `t80096..t80169` 已有 `74` 个唯一 Tick；累计自然采集
  `2`、入核 `16`，资源 `139→155/155`，成本回收后净增 `+5`。`t80155` 首次回满 Core，但此后仍有空载
  Worker，`full_core_loaded=0`、第二次容量电梯 `0` 次、没有 W24；当前结果说明 W23 已恢复出口，但还没有证明
  长期不会重新锁死。满核后的 `resources<capacity` 巡查过滤不再有效，后续须将“满核带货扫描”和“空载巡查”分开统计。
- **128-Tick checkpoint**：严格窗口 `t80092..t80219` 共 `128/128` 个唯一 Tick，全部 accepted；资源
  `139→155/155`，自然采集 `9`、入核 `16`，`UNIT_MOVE_SUCCEEDED=3565`，无移动/资源失败、窗口/提交/
  协议错误、伤亡或 Core 受击。该窗口 `full_core_loaded=0`、`capacity_elevator=0`，没有 W24；`decide_ms`
  P50/P95/P99/max=`122.5/456.5/567.59/708ms`，本地总耗时=`1230/1642/2045.94/2584ms`，均在命令窗口内。
  满核前的有效空载样本仍显示 NW+NE `83.43%`，满核后应单独归因，不在本容量实验中调整巡查。
- **巡查旁证（不作为本任务改动依据）**：在 `t80096..t80124` 过滤 `resources<capacity` 且
  `cargo==0` 后有 `109` 个有效 Worker 样本；四象限样本 `90` 个，其中 NW `49`（54.4%）、NE `41`
  （45.6%）、SE/SW 均为 `0`，显式 `WAIT` 为 `0`。绝大多数 Worker 仍在带货，故该结果是“有效空载巡查仍
  北偏置”的信号，不等同于全体带货扫描的伪集中；下一轮必须单独验证南侧目标分配、到站驻留和资源任务
  退出原因，不能与容量电梯实验混改。
- **下一步/停止条件**：继续保持唯一实例，至少跨过一个 `128` Tick 巡查驻留相位，并累计扩容后
  `256–300` 个唯一 Tick；达到至少回收 `11` 资源生产成本、主指标不低于基线且护栏全通过后才可接受。
  若再次满核全员带货，记录下一次容量电梯是否在 W23 上工作；若两个干净 `100`-Tick 窗口入核率下降
  `10%`、链路 P95 上升 `25%`、出现任何窗口/协议/提交/移动/资源失败或伤亡，立即停止扩容实验并只回滚
  `3036f7d`。在此之前不修改巡查、战斗、资源分配或容量触发条件。

## `ECONOMY-CAP-010` W23→W24 容量电梯恢复出口（SAFE_CANARY_VERIFIED，长窗观察中，2026-08-10）

- **触发证据**：W23 版本继续运行至约 `t82382`，严格监控得到 `2286` 个唯一成功 Tick；库存长期为
  `155/155`，`full_core_loaded=2055`、最长满核闲置 `2221` Tick，编制始终为 `W23/V4/R4`。
  全窗自然采集 `17`、入核 `16`，没有第二次容量电梯；窗口/提交/决策错误、Core/Unit 死亡和 Core 受击均为
  `0`。这不是短窗噪声，而是 `MAX_WORKERS=23` 的硬上限再次阻断了容量出口。
- **单一变更**：仅将 `MAX_WORKERS` 从 `FREE_UPKEEP_CAP+3`（W23）提高到 `FREE_UPKEEP_CAP+4`（W24）。
  保留“Core 满、全 Worker 带货、和平期、军备不短缺、动态 Worker 成本加 3 点储备、所有带货 Worker
  有合法离核出口”的原触发条件；不改变 Worker 资源匹配、四象限巡查、战斗巡逻、敌情生产或普通 Worker 桥。
- **离线验收**：新增 W23 满核全带货允许 W24、W24 满核硬停止扩容两个回归；同时保留 W21/W22、未全带货、
  无合法出口等已有边界测试。目标为全量测试、编译、依赖和差异检查均通过。
- **LIVE_CANARY**：提交并重启唯一实例前先归档当前 `game.log` 与 `play.out`，保留 `tactic_state.json`。
  新进程边界先验收 `20` 个连续唯一 `ST[ACCEPTED]` Tick，再观察至少 `128` Tick；若触发 W24，记录
  `CORE_SPAWN_SUCCEEDED` 实际成本（预期人口 31 档 Worker 成本为 `11`）、容量 `155→160`、生产后库存、
  首次交付延迟、HARVEST/DEPOSIT 和移动失败。最终以 `256–300` Tick 的净资源回本及完整采集→入核链路判定。
- **20-Tick 安全结果（2026-08-10）**：新 PID `18161` 从 `t82401` 开始，严格窗口 `t82401..t82420`
  共 `20/20` 个唯一 Tick，全部 `ST[ACCEPTED]`。`t82401` 提交容量电梯后，后续状态为 `pop32(W24/V4/R4)`、
  容量 `155→160`，`CORE_SPAWN_SUCCEEDED.cost=11`；库存 `155→144→147`，窗口内自然采集 `1`、入核 `3`。
  移动/资源失败、窗口/提交/决策错误、缺口、Unit/Core 死亡和 Core 受击均为 `0`。这证明 W24 能解除当前
  容量出口死锁并通过安全护栏，但 `20` Tick 结束仍净 `-8`，不能宣称长期资源收益已优化。
- **停止/回滚**：任一窗口/协议/提交/资源/移动失败、伤亡或 Core 受击立即停止；若扩容后两个干净 `100`-Tick
  窗口的 gross 入核下降 `10%`、链路 P95 上升 `25%` 或 `256–300` Tick 仍未回收 `11` 成本，只回滚本次
  W24 变更，不回滚持久地图和既有巡查修复。
- **巡查隔离**：当前最后一个有效空载分层样本仍为北侧 `83.43%`（NW `34.02%`、NE `49.41%`）；满核后
  `resources<capacity` 过滤器失效。本轮不调整巡查，避免把容量出口收益与象限覆盖混为一个变量。

## `RESOURCE-DISPATCH-023` 满核全带货四象限巡查与 Core 护栏（SAFE_CANARY_VERIFIED，2026-08-10）

- **问题证据**：最新 live 为 `W24/V4/R4`、Core `160/160`，24/24 Worker 全部带货，连续窗口没有
  `HARVEST_SUCCEEDED` 或 `DEPOSIT_SUCCEEDED`；旧 `_explore_step()` 让整队回到北偏扫列。另发现远距
  Worker 在 A* 距离早退前不会归一化持久 `col_off`，可能让偏移长期污染。
- **单一行为修复**：满核且所有 Worker 带货时，按当前 Worker cohort 连续编号派发独立四象限站点，
  到站显式 `WAIT`，容量恢复后立即退出巡查并用 A* 回 Core；新增 `full_core_patrol` (`pat`) 遥测。
  `col_off` 在距离早退前归一化。巡查路径及不可达站点回退均显式阻塞 Core，防止把 Core 当作中间格穿越。
  不改变 `MAX_WORKERS=24`、敌情生产、普通 Worker 桥或 W25 上限。
- **离线验收**：新增远距偏移、四象限全带货、容量恢复回程、巡查到站与 Core 阻塞回归；全量
  `pytest 212 passed`，compileall、`uv pip check`（19 包）、`git diff --check` 均通过。SDK 本地为
  `0.2.9`；官方 source/version 页面仍为 gameplay `v0.14` / SDK `0.2.9`，未发现兼容漂移。
- **提交与上线**：代码提交 `577ae8a` 已推送；复核护栏后提交 `4d2c83d` 并推送 `origin/main`。
  唯一 LaunchAgent PID `37476` 从 `t82939` 开始运行最终代码。
- **20-Tick canary**：严格窗口 `t82939..t82958` 共 `20/20` 个唯一 `ST[ACCEPTED]` Tick，无缺口、
  重复、窗口/提交/协议错误、`MOVE_FAILED`、资源失败、Unit/Core 损失或 Core 受击；事件为 `440`
  次 `UNIT_MOVE_SUCCEEDED`。资源保持 `160/160`、编制 `W24/V4/R4`，每 Tick `full1,elev0,pat22`；
  Worker 与 Core 的曼哈顿距离≤1样本为 `0`。`decide_ms` P50/P95/P99=`143/184.5/192.1ms`，
  本地总耗时 P50/P95/P99=`1193.5/1376.85/1389.77ms`。
- **结论与后续**：本轮证明巡查不再穿 Core、满核舰队能持续分散移动且命令窗口安全，但没有证明资源
  吞吐已恢复；`HARVEST/DEPOSIT=0` 是当前 W24 容量锁的直接证据。继续观察至少一个 128-Tick 巡查相位，
  并将 W24→W25 作为下一次独立容量电梯实验（单独核算动态 Worker 成本、容量变化、首个交付延迟和
  256–300 Tick 净回本），不得与本巡查修复混合。

## `ECONOMY-CAP-011` W24→W25 容量电梯修复（STATIC_VERIFIED，待 live canary，2026-08-10）

- **触发证据**：最终巡查代码运行至 `t83115` 已有 `176` 个成功唯一 Tick；资源始终
  `160/160`，编制始终 `W24/V4/R4`，`full_core_loaded=176`，`HARVEST=0`、`DEPOSIT=0`、
  `capacity_elevator=0`。Worker 移动成功 `2104` 次且没有移动失败、伤亡或 Core 受击，证明瓶颈是
  `MAX_WORKERS=24` 的容量护栏，不是路径或协议故障。
- **单一变更**：仅将 `MAX_WORKERS` 从 `FREE_UPKEEP_CAP+4`（W24）提高到 `FREE_UPKEEP_CAP+5`
  （W25）。保留满核、全 Worker 带货、和平期、军备不短缺、动态 Worker 成本加 3 点储备和合法离核
  出口等全部触发条件；不改变普通 Worker 桥、巡查、资源匹配、战斗或 SDK。
- **规则成本**：W24 状态人口为 32，v0.14 Worker 成本为 11；生产 W25 后容量预期从 `160→165`，
  生产后库存预期为 `149`，下一次容量电梯在 W25 被硬停止，避免无限扩容。
- **离线验收**：容量边界定向测试 `8 passed`；全量 `pytest 213 passed`，`compileall`、
  `uv pip check`（19 个包）和 `git diff --check` 通过。
- **live 链路备注**：`t83011` 是唯一一条 `SUBMIT_FAILED/TICK_MISMATCH`，不是 Tick 缺口；其后
  `t83012..t83085` 连续恢复。依据当前 heartbeat 约束，本次不重启 live、不提交或推送；当前 PID
  `37476` 仍加载 `4d2c83d`，W25 修复尚未上线。
- **canary 条件**：经主代理批准后重启唯一实例，先验收 20 个连续唯一 `ST[ACCEPTED]` Tick；若
  W25 触发，再观察至少 `256–300` 个唯一 Tick，记录实际 `CORE_SPAWN_SUCCEEDED.cost`、首个交付
  延迟、入核资源/Tick、生产净回本和满核再次锁死情况。任一窗口/协议/提交错误、Core/Unit 损失，
  或两个干净 100-Tick 窗口入核率下降 10%，立即停止并回滚该单一变更。

## `ECONOMY-CAP-012` W25→W26 残余货物恢复（APPROVED，STATIC_VERIFYING，2026-08-10）

- **批准依据**：用户确认两级容量恢复方案。独立资源复核发现 W25 成本为 `11`、容量只增加 `5`；
  旧 W24 的 `24` 个货物最多先交付 `16` 个，仍会留下 `8` 个带货 Worker。此时 `full_core_loaded`
  变为 `false`，旧门控无法继续生产 W26，W25 单槽不能恢复持续采集→入核闭环。
- **单一实现边界**：将上限设为 `MAX_WORKERS=26`；保留 W25 的满核全带货门控；仅在恰好 W25、Core
  再次满载、仍有残余带货 Worker、和平、军备齐备且有合法离核出口时允许唯一一次 W26。W26 后硬停止。
  不改变巡查、资源匹配、战斗、敌情识别或普通 Worker 桥。
- **规则成本**：pop32→W25 使用动态 Worker 成本 `11`，容量 `160→165`；pop33→W26 仍为 `11`，
  容量 `165→170`。两次生产的可归因成本约 `22`；旧货交付不计为新资源收益。
- **遥测**：保留 `full/elev/pat`，新增 `rec` 表示 W26 恢复槽实际入计划；同时核对实际
  `CORE_SPAWN_SUCCEEDED.cost`、旧货残余数、首个交付、`HARVEST→DEPOSIT` 链路和净资源。
- **严格资格门控**：W26 不再由“W25 + 任意带货”推断。进程必须先记录本进程排队的 W25 Tick，
  再在下一份 authoritative state 中确认 `CORE_SPAWN_SUCCEEDED`、目标是当前 Worker、Worker 数为
  25 且实际 cost 为 11；只记录该事件前已存在且仍带货的 Worker。被清空的 Worker 永久移出资格集，
  重启/手动直达 W25、W25 未成功或仅 W25 新货均无恢复资格；若动态 Worker 价格离开 11 的实验层，
  W25/W26 也停止，等待重新审批。
- **静态验收**：覆盖 W24→W25、W25 残余货物→W26、伪触发、资格清空、无合法出口、W26 硬停止、敌情
  阻断和容量遥测；当前全量 `pytest 227 passed`，并通过 compileall、锁定依赖检查、diff 检查和敏感
  信息扫描。
- **LIVE_CANARY**：重启后先验收 `20` 个连续唯一 accepted Tick；W25/W26 实际成本与容量正确后，
  观察至少 `128` Tick 清空旧货，再观察 `256–300` Tick。两次新增资源/有效战利品至少覆盖 `22` 成本，
  且出现自然 `HARVEST_SUCCEEDED→DEPOSIT_SUCCEEDED` 链路，才可进入 ACCEPTED。
- **停止/回滚**：新的窗口、提交、协议、移动竞态、Core/Unit 损失立即停止；128 Tick 无首个交付、
  256–300 Tick 无自然采集入核、再次满核锁死或净资源未覆盖成本时，回滚本任务，不回滚持久地图和巡查修复。

## `ECONOMY-CAP-013` W26 后资本消耗出口（STATIC_VERIFYING，待 live canary，2026-08-10）

- **问题证据**：W26 后约 `t83575..t84009` 仍为 `170/170`、`W26/V4/R4`；连续满载 `425` Tick，
  最近约 `300` 个成功 Tick 只有 `20` 次采集、`0` 次交付。Worker 已分散，Core 无伤，故瓶颈是
  “全员带货但没有可消费的容量出口”，不是路径拥堵或 SDK 不兼容。
- **单一方案**：满核且全 Worker 带货连续 `15` Tick、W26 已完成、Core 满血满盾、无近核威胁、
  待交付货物不少于生产成本、生产后保留 `3` 点库存时，每个满载周期最多生产一个战斗单位。无合法
  远袭射击位时生产 Vanguard；只有已有 `2V/2R`、存在可达合法 Ranger 射击位且 ETA≤`48` Tick
  时生产 Ranger。普通 Worker 扩容仍停止。
- **动态价格护栏**：只接受当前审查档 `V=22` 或 `R=26`；下一价格档、Manual 改变人口或
  `CORE_SPAWN_SUCCEEDED.values.cost` 不匹配时停止实验，不静默扩大成本。
- **回本门控**：成功回执必须匹配目标 UUID、目标类型和实际 cost；生产后累计新的交付金额至少覆盖
  该 cost，并确认同一 Worker 的新 `HARVEST_SUCCEEDED -> DEPOSIT_SUCCEEDED` 链路后，才允许下一次
  资本消耗。新增 `sink/wait/rep/chain/streak/priceblock` 饱和遥测和监控字段。
- **监控修正**：远距敌核仅满足可见性和资源预算时从 `NO_RAID` 改为至少 12 Tick 的 `RAID_DELAY`，
  明确这是 ETA/射击线待核查，不再直接判定攻击策略失败，也不放宽 `_chase_target()` 的 `<40` 门槛。
- **静态证据**：资本出口、权威回执、回本链路、动态价停止和监控解析均有回归测试；当前需在
  `pytest`、`compileall`、`uv pip check`、`git diff --check` 完成后才进入 canary。
- **canary/停止**：主代理确认后只重启唯一 live 进程，先观察 `20` 个唯一 accepted Tick；若出口触发，
  观察至少 `128` Tick 的首个交付，再用 `256–300` Tick 检查成本回收和自然采集入核。任一窗口/协议/
  提交错误、Core/Unit 损失、价格漂移、成本未回收或再次满载无出口，回滚本任务，不回滚持久地图。
