# 经验总结（LESSONS）

踩过的坑、对应的根因与修复。每次迭代后追加，避免重犯。**任何改动若修复了
某个坑，都要在此归档**，并保持 `GOAL.md` 的"行为→目标"映射同步更新。

---

## L1 — 单位拥堵（UNIT_CLUMPING）— 误诊修正
- **原误判**：早期监控用 `CELL_UNIT_LIMIT 次数 / tick 数`，算出 121% 的"严重拥堵"。
- **真因（专家评审修正）**：分母应为「移动事件总数」而非 tick 数；真实阻塞率约 9%。
  阻塞来自少数「僵尸 Worker」（如 `b91dec`/`286c14` 长期占 Core 邻格死锁），并非全队拥堵。
- **修复（已落地）**：`meta/monitor.py` 改为 `move_failed / (failed+succeeded)`，阈值 0.10。
  监控不再误报；具体死锁 Worker 的解除留待后续迭代（见 L6）。
- **状态**：✅ 监控已修正（💡 教训：统计口径必须量纲一致，先验证再改战术）

## L2 — 采集停滞（LOW_HARVEST 后期）— 已修复
- **现象**：后期仅 ~0.026 harvests/tick；附近资源采空后 Worker 无法有效扩散。
- **根因**：`MAX_HARVEST_FROM_CORE=30` 把 d=41-46 的可见节点全部硬过滤；且全员外派扫图，
  Core 正下方一格资源都无人采。
- **修复（已落地）**：新增饥饿感知 `_harvest_radius(turn)`——健康经济用 30，饥饿（>50 tick
  无 harvest 或 `_known_resources` 为空）放宽到 55，三处采集过滤点统一调用。保证有收入而非空转。
- **状态**：⚪ 历史方案；固定 40/65 半径已被 L13 的全局资源调度取代。

## L3 — 有敌不突袭（NO_RAID）— 部分修复 + 发现死代码
- **现象**：敌人可见 37 ticks，0 个敌核被摧毁；常备军长期 `V2 R1`，且攻击单位从未出现在 plan 里。
- **根因（专家评审关键发现）**：
  1. `decide` 用 `e.unit_type == "CORE"` 检测敌核，但 `CoreView` 只有 `.kind=="CORE"`、
     无 `unit_type` → **整段敌核突袭逻辑是死代码**（`enemy_core_visible` 恒为 False）。
  2. `_chase_target` 猎核门槛 `resources>=60 且 dist<40`，当前 d=44 被卡死。
  3. 唯一 Ranger（index 0）永远走 `_guard_step`，从不猎核。
- **修复（已落地）**：检测改为 `getattr(e,'kind','')=="CORE"`，并复用 `_known_enemy_cores`
  集合（敌核仅在视野闪现几 tick，靠持久记忆才能组队突袭）；见敌把造兵地板降到 2、见敌核
  把目标攻击数提高到 `DEFENSE+2`。后续还需放开猎核门槛、新增 `_raid_squad`、让 index 0 的
  Ranger 也能猎核（见 L6）。
- **状态**：🟡 检测与造兵已修；突袭寻路/编队待续

## L4 — 丢核即清零（CORE_DEFENSE）— 已修复
- **现象**：历史曾因经济卡在 ~5 资源、买不起还击单位而丢核，库存全失。
- **根因**：经济地板之上仍缺军备时继续造 Worker，把资源耗光，无法还击。
- **修复（已落地）**：`army_short` 时（有敌或达经济地板）不再造 Worker，把资源攒给
  攻击单位；护盾仅在受威胁时修。
- **状态**：✅ 已修复

## L5 — 资源闲置（IDLE_GOLD）— 监控修正
- **现象**：资源长时间顶在 capacity。
- **原则**：手里的资源不生息。把结余投入经济、防御或军备，让资本始终运转。
- **修复**：监控改为「连续」计数（idle_gold_streak），阈值降到 15，避免把全程累计误报。
- **状态**：🟡 持续监控

## L6 — 资源凭空蒸发（RESOURCE_LOSS）— 新增检测
- **现象**：`t55135 r95/95` → `t55136 r0/95`，单 tick 掉 95 且无任何 spawn/事件。
- **根因（待查）**：疑似 capacity 溢出销毁（`CORE_RESOURCE_OVERFLOW`）或用户手动消耗。
  原监控只记 res min/max，完全漏报这个最致命的单次损失。
- **修复（已落地）**：`meta/monitor.py` 新增 `RESOURCE_LOSS` 告警（单 tick 跌幅 >10 且无
  spawn），并跟踪 `largest_drop`。
- **状态**：🟡 检测已加；根因/应对待续（若确认是溢出，需在满员且 res 高位时改投修盾/生产/换兵）

## L7 — 迭代方法本身（元教训）
- 监控口径必须量纲一致、先验证再改战术（L1 误诊）。
- 专家子代理并行评审能发现单看代码发现不了的死代码与统计 bug（L3、L1）。
- 每次评审结论都要落到 `tactic.py` / `meta/monitor.py` 的具体改动并跑测试，再 commit/push。
- **状态**：✅ 已建立闭环

## L8 — 改完战术必须重启 live 进程 + 资源"看得见才采得到"
- **现象（用户实测）**：改了 `tactic.py` 后 worker 仍在核心旁闲转，战况没变。
- **根因 1（最坑）**：`play.py` 是长驻进程（启动时把 tactic 载入内存），改文件**不会热加载**。
  不重启就永远是旧逻辑。→ 任何战术改动后必须 `kill` 旧 `play.py` 再重启，否则"改了等于没改"。
- **根因 2（worker 闲转的直接 bug）**：旧锁定逻辑要求 worker 离"已知资源"≤30 格才锁定，
  16 个 worker 铺在整块地，绝大多数时刻离任何已知资源都 >30 → 永不锁定 → 只在核心旁扫荡。
  → 已修复：对"已知资源"取消 worker 距离上限（用采集半径 40/65 代替），任意 worker 都会
  锁定并 A* 走向最近的已知资源；记忆保留半径也对齐到 65，避免会被锁的资源被提前遗忘。
- **根因 3（更深层）**：worker 视野很小（`obs`≈29-39），地图资源大多不在其视野/扫荡带内，
  所以 `_known_resources` 常常为空 → 只能盲扫。用户在网页/大视图看到"满地图资源"，
  worker 却几乎看不见 → 采集率极低（实测 ~0.02 harvest/tick）。
- **应对（根因 3）**：战术侧已让"已知资源必采"；但若资源真的远在扫荡带外，正解是**把 Core
  迁到资源簇附近**（用户可手动迁移；战术支持迁移并会重新扫新区块）。 worker 视野小是规则限制，
  加宽扫荡带会令 worker 跑太远（净亏），不如迁核。
- **状态**：🟡 锁定/记忆 bug 已修并重启生效；发现率取决于资源是否进入视野，必要时迁核。

## L9 — 地图持久化"存了不用"（_explored_cells 死存）
- **现象（用户质问）**："已经做了地图持久化为什么不使用？"
- **根因**：`tactic_state.json` 持久化了 4 张图，其中 3 张**真的在用**——
  `_known_resources`（采集）、`_known_obstacles`（A* 寻路）、`_known_enemy_cores`（猎核）；
  唯独 `_explored_cells`（已探索区域集）**只写不读**：`_observe_terrain` 每 tick 往里塞视野格、
  有障碍变动才落盘，但 `_explore_step` 完全不读它，探索纯靠 chunk 牛耕式 + 每 worker 列偏移。
  docstring 里写的"避免重扫已点亮区 / 定期回访重生资源"从未落地——典型的"建了持久化但没消费"。
- **修复（已落地）**：
  1. 新增 `_next_explore_col_off()`：在 y 边界推进列时，若下一列已被点亮则跳到**最近的**未探索列；
     全部列都探索过才退回牛耕式（兼顾重生回访）。无探索记忆时与 `_advance_col_off` 完全一致。
  2. `_observe_terrain` 按 `_EXPLORED_SAVE_STEP=1000` 批量落盘，重启不再丢失整张图。
  3. `_clear_exploration_state`（Core 迁移触发）现在一并清空 `_explored_cells` 并写回磁盘，
     避免重启读到旧地图。
- **遗留**：SDK 的 `Turn` 无 `game_id`，无法区分"同局崩溃恢复"与"开新局"——目前持久化对崩溃恢复
  有利、但开新局可能带入上局地图。需 SDK 暴露局标识才能根治（已记入待查清单）。
- **状态**：⚪ 第一阶段方案；牛耕式读图仍会形成方向惯性，现已由 L13 的分散前沿调度取代。

## L10 — 击毁敌核 ≠ 固定 +6（CORE_RESOURCES_CAPTURED 真实机制）
- **现象（用户纠正）**："击毁敌方 core 不一定获取 +6 资源"。
- **真相（查 SDK `arena_hero/models.py` 的 `CoreResourceCapture` 类型）**：
  击毁敌核触发 `CORE_RESOURCES_CAPTURED` 事件，携带 4 个字段且约束 `amount + destroyed == available`：
  - `available`：敌核被摧毁时的库存资源（**变量**，取决于对方攒了多少、是否花光）
  - `amount`：**你实际获得**的资源（= available 中未被销毁的部分）
  - `destroyed`：被销毁/溢出的资源（available - amount）
  - `capacity`：敌核容量上限
  即回报 = 敌核当时**库存中未被销毁的那部分**，**不是固定 +6**；且受我方核容量约束
  （超出我方容量的溢出会被销毁）。若敌方把资源全花在生产上（库存≈0），击毁它**几乎得不到资源**。
  （当时打包规则与 SDK 为 v0.13 / 0.2.8；当前已升级到 gameplay v0.14 / SDK 0.2.9，结论仍由
  `CORE_RESOURCES_CAPTURED` 的变量字段支持。）
- **已改（tactic.py）**：删除 5 处"+6 resource jackpot / pays +6 / shipped home"的错误假设，
  改为准确描述 `CORE_RESOURCES_CAPTURED`（变量回报 + 战略价值=清场+不确定战利品）。
  突袭敌核的**战略价值仍在**（消灭对方舰队、削弱竞争者），但不再被当成"稳赚 +6 的资源经济"来过度加权。
  `play.py` 日志现在也会打印真实 `CORE_RESOURCES_CAPTURED[amount]`，便于验证真实数额。
- **后续可优化（未做，避免范围蔓延）**：`_known_enemy_cores` 一旦见过就永久保留，
  会导致"记得的敌核"持续触发额外突击编队——在回报不确定的情况下可能浪费产能。
  可改为仅在**当前可见**敌核时才超编。留待后续评审。
- **状态**：✅ 错误假设已修正并记入；40 测试通过。

## L11 — 更新 tactic.py 后必须重启 play.py（无热加载）
- **纪律（用户要求）**：程序（tactic.py）更新后**及时重启 play.py 进程**，否则改动不生效。
- **原因**：`play.py` 是长驻进程、不热加载（L8）。本地持久化地图 `tactic_state.json` 会在
  重启后自动重载，故重启安全、状态连续；服务器侧也保留对局状态，重连即可续上。

## L12 — 断线缺口与陈旧资源提示必须分开统计
- **现象**：`game.log` 中 `t60450→t60455`、`t60688→t60693` 各缺 4 个 Tick；同一窗口还出现
  `CORE_SPAWN_SUCCEEDED`，旧监控却显示 `spawn=0`。资源分配中历史提示长期占据绝大多数空闲 Worker。
- **根因**：监控只按已写入行聚合，没有检查相邻 Tick 缺口；生产事件名称以 `CORE_` 为前缀；历史
  资源提示没有年龄门槛，也没有给探索保留名额。
- **修复（已落地）**：`meta/monitor.py` 统计 `tick_gaps/missing_ticks`、失败提交耗时并统一识别
  `SPAWN_SUCCEEDED` 与 `CORE_SPAWN_SUCCEEDED`；`tactic.py` 对时间戳历史提示设置 256 Tick 有效期，
  当前无可见资源且空闲 Worker 达 3 个时保留 1 个做前沿探索，并记录 `stale/exp` 遥测。
- **验收**：109 个离线测试通过；旧日志重新分析为 843 个唯一 Tick、8 个缺口、1 次 Core 生产，
  且没有把该生产造成的资源下降误报成 `RESOURCE_LOSS`。新策略仍需重启后做唯一进程 canary，不能
  用旧进程日志宣称收益已提升。
- **状态**：🟡 代码已修；等待 live canary 与至少 200 个唯一 Tick 的资源链路样本。
- **安全重启命令**（括号技巧 `[p]lay\.py` 避免 pkill 误杀自身；`nohup ... & disown` 完全脱离）：
  `cd /Users/hx/myself/ArenaHero && pkill -f "[p]lay\.py"; sleep 2; nohup .venv/bin/python3 play.py > play.out 2>&1 < /dev/null & disown`
  验证：`game.log` 的 tick 继续推进、且 `play.out` 无 error。
- **当前运行方式**：`launchctl` 用户服务 `io.arenahero.tactic` 托管 `play.py`；改动后用
  `launchctl kickstart -k gui/$(id -u)/io.arenahero.tactic` 重启，并验证唯一 PID 与连续新 Tick。
- **自动化边界（2026-08-07 修正）**：当前 `/Users/hx/.codex/automations` 没有任何 heartbeat 配置；
  Codex 自动化创建接口连续调用无返回并被终止，不能再声称每 6 小时复核已经生效。产品接口恢复前，
  每轮人工审查仍显式核对 SDK、规则文档、live KPI 与唯一进程；不使用系统 cron 绕过线程 heartbeat。

## L12（历史 v0.13）— 全量复审：规则真相必须压过历史启发式
- **对角火力漏判**：v0.13 Ranger 支持横、竖和 45 度对角线 1-3 格射击。旧实现先按
  Manhattan 距离过滤，导致 `(2,2)`、`(3,3)` 等合法目标永远不射；现改为八方向射程，
  射击障碍只检查实际横、竖或对角 shot cells；对角线旁的障碍不阻挡。视野遮挡仍使用
  supercover，两者不能混用。
- **历史敌核冻结经济**：`_known_enemy_cores` 是永久坐标集合，过去却与“当前可见敌核”共用
  超编信号，使 4V/4R 的不可达目标长期保持 `army_short`，阻止继续造 Worker。现仅当前可见
  敌核触发超编；历史坐标仍可供有界 Ranger 追踪，不再支配生产。
- **资源认领排序**：最近资源优先排序现在先筛空载 Worker，载货返航者不会影响可认领单位的
  优先级；同距时保留原始顺序，结果可重复；旧的远端锁不能覆盖近 Worker 的新认领。
- **当前 state 胜过旧事件**：上一 Tick 的 `HARVEST_SUCCEEDED` 不能删除当前 Turn 仍明确存在的
  resource cell；剩余 cargo pile 或同坐标 refill 都要求先处理事件、再由当前集合覆盖。
- **禁止为省 upkeep 盲目自毁**：pop21 自毁到19会先损失两个生产资产，再因容量从105降到95
  立即销毁最多10库存，只为省1/tick；且会撤销用户手动扩军。现不再自动自毁。
- **Beacon 所有权与低风险获取**：`CARRIED` 不代表我方持有，必须用友方 `carrier_id` 判断10盾
  上限；Worker/Core 与地面 Beacon 同格时可拾取，但不做高成本跨图追逐。
- **探索必须尊重遮挡**：障碍后的格子不在当前视野，不能写入 `_explored_cells`，否则后续扫描
  会永久跳过真正未知区域。
- **文档漂移**：同步修正 `GOAL.md` 的固定 +6 假设和 `README.md` 的 v0.7 / SDK 0.2.4。
- **重启必须保证单实例**：把多个 PID 拼成带空格字符串交给 zsh `kill` 会被当成一个非法 PID，
  新旧客户端随后竞争同一个 Agent plan 槽。现在直接以 `.venv/bin/python3 play.py` 启动并把真实
  Python PID 写入 `play.pid`；停止时逐个 PID 传参，重启后检查唯一进程和连续新 Tick。
- **状态**：✅ 失败测试复现后修复；本条是历史 v0.13 / SDK 0.2.8 记录，当前规则以 L23 的
  v0.14 / SDK 0.2.9 为准。

## L13 — 地图持久化不等于资源调度：已知资源优先 + 分散前沿
- **用户观察**：16 个 Worker 长期集中在 Core 东南/东侧闲逛，但网页地图上仍有大量资源。
- **根因**：
  1. `_known_resources` 虽然写入 `tactic_state.json`，却会被 Core 距离 40/65 的规则静默裁剪；
     `known_resources=[]` 并非地图没有资源，而是策略主动忘掉了远端事实。
  2. 资源认领按 Worker 顺序逐个贪心，只保证当下这个 Worker 最近，不保证全队总路程最短；
     Worker 锁只在内存，重启后无法从持久化资源池恢复全局调度。
  3. 无资源时使用固定南北扫列。返航、绕障与 Core 相对位置会反复恢复同一方向，形成东南聚团；
     `_explored_cells` 被读取了，但只用于跳列，没有把空闲 Worker 分到不同未知边界。
- **本轮修复**：
  1. 已知资源不再按 Core 距离删除；只在当前完整 Turn 重新照亮且为空，或采集事件确认消失时删除。
  2. 新增确定性 Hungarian 全局匹配：先最大化分配数，再优先当前可见资源，最后最小化全队
     Manhattan 总路程；每个资源和 Worker 都只出现一次，载货 Worker 不参与。
  3. 每 Tick 重算资源归属，新资源出现后最近空闲 Worker 可立即接管；重启加载历史资源后第一 Tick
     即恢复调度，不再依赖旧 Worker 锁。
  4. 无资源任务的 Worker 使用稳定分散前沿：评分同时考虑新增未知视野、目标间距和行走成本；
     目标未失效时保持不变，避免每 Tick 振荡。
  5. 前沿限制在当前 Core 的 `MAX_SWEEP_RADIUS=40` 内。Core 迁移只清临时路线，保留地图事实，
     同时不会让 Worker 回头追旧区域边界。
  6. A* 默认不允许进入动态阻塞目标；只有载货 Worker 返回自家 Core 显式允许，避免资源格或
     前沿格满员时重复提交非法移动。
- **验证**：63 项测试通过，覆盖远端资源保留、可见优先、贪心反例、输入顺序无关、载货排除、
  唯一认领、多方向前沿、目标稳定、障碍阴影、迁移边界和阻塞目标。
- **后续优化方向（按收益排序）**：
  1. 在 `play.py` 日志增加每 Tick 的 `resource_assignment`、`frontier_target`、重分配次数与目标距离，
     用事实判断 Worker 是否真正消费持久化地图。
  2. 统计“发现→分配 Tick”“分配→采集 Tick”“采集→入核 Tick”“每单位往返距离收益”，按资源
     坐标建立收益样本；远端价值应由真实回报衰减，不再由固定 40/65 半径决定。
  3. 为历史资源增加 `last_confirmed_tick` 与不可达冷却。冷却只能暂停本轮追逐，不能删除事实；
     当前 Turn 再次看见时立即恢复最高优先级。
  4. 匹配成本从 Manhattan 升级为缓存的 A* 估计，仅对候选前几名计算，避免 15 秒窗口内做
     `Worker × Resource` 全量路径搜索。
  5. 监控前沿的象限分布、目标最小间距和每 Tick 新增探索格；若某方向长期低覆盖，再调整
     前沿信息增益权重，而不是恢复固定扫列。
  6. 等 SDK 暴露稳定 `game_id` 后，为持久化地图分局存储，彻底消除新对局加载旧地图的歧义。
- **Live 证据**：重启后 `t56165-t56169` 当前可见资源仍为 0，不能验证真实资源接管；但空闲
  Worker 从东侧聚团开始大规模向左分散，`explored_cells` 从约 1275 增至 1634，且无协议错误。
- **状态**：✅ 核心调度已落地；资源接管由测试覆盖，继续等待下一次 live 资源出现。

## L14 — 守卫站位必须计入交付吞吐
- **用户观察**：两个 Vanguard 长期共同占据 `[15,235]`，该格达到 2/2；Core 在 `[15,234]`，
  载货 Worker 实际只能从 `[14,234]` 一侧排队进入，防御单位变成了经济瓶颈。
- **根因**：旧 `_control_vanguards()` 只会让“站在 Core 上”的 Vanguard 离开，并把距 Core >1
  的 Vanguard 拉回；一旦两个 Vanguard 已经位于同一个 Core 邻格，二者都满足“近身守卫”条件，
  没有任何去重或交付通道概念，因此永久保持 2/2。
- **修复**：
  1. 新增 `_vanguard_guard_targets()`，按 UUID 确定性分配唯一守卫槽位。
  2. 只保留一个 Vanguard 在 Manhattan 距 Core 1 的近身位，确保能扫击进入 Core 的敌人。
  3. 其余 Vanguard 分散到距 Core 2 的支援位；两个 Vanguard 时至少三个 Core 邻格完全空闲。
  4. 站位目标避开障碍、敌人和已被非 Vanguard 填满的格；有相邻敌人时仍优先 `SWEEP`。
  5. Worker 前沿分配同步使用 `friendly_full`，不会把稳定探索目标留在两个守卫占满的格子上。
- **收益判断**：守卫并非越贴 Core 越好。近身 Vanguard 的边际防御收益在第一个单位最高；第二个
  叠在同格不增加覆盖方向，却直接损失一条交付入口。最优折中是“一近一外、目标唯一”。
- **验证**：新增守卫槽位唯一、堆叠拆分、满占前沿失效 3 条测试；全量 `66 passed`。
- **后续指标**：记录 Core 四邻格的长期满占率、载货 Worker 距 Core 3 格内等待 Tick、
  `CELL_UNIT_LIMIT` 中目标为 Core/邻格的比例，以及每次采集到入核的 P50/P95 时间。
- **Live 证据**：`t56165` 载入新逻辑后下发拆位动作；`t56166-t56169` 连续保持
  `O[15,235,14,235,15,236]`，原 `[15,235]` 从两个 Vanguard 的 2/2 降为单 Vanguard 1/2，
  第二个稳定在距 Core 2 的 `[14,235]` 支援位；无认证、协议或提交错误。
- **状态**：✅ 算法与实战均验证。

## L15 — 持久资源是“带可信度的提示”，不是永久任务
- **现象**：全局 Hungarian 匹配完成后才过滤敌占或友方 2/2 满占资源。被过滤的 Worker 会直接
  转去探索，即使地图上还有第二个可采资源；雾中的历史资源又没有年龄、失败或冷却信息，可能
  长期占用 Worker。
- **根因**：资源事实只有 `_known_resources: set[position]`，把“曾经看见”和“当前可采”混成
  同一种状态；动态可进入性没有进入匹配候选集，匹配后过滤也没有重跑分配。
- **修复（已落地）**：
  1. 在 Hungarian 前计算敌方占位与友方满占；不可进入资源先从候选集移除，因此 Worker 会在
     同一 Tick 自动匹配到第二资源。已经站在该资源上的空载 Worker 仍保留采集权。
  2. 新增 `ResourceHint(last_confirmed_tick, source, failure_count, cooldown_until)`；旧版
     `known_resources: [[x,y]]` 自动升级为 `source=legacy`，不丢历史地图。
  3. 历史目标被已知障碍/动态占位封死，或 A* 明确耗尽开放集时，采用 4/8/16/32/64 Tick
     指数冷却，失败次数在运算前封顶；冷却只暂停分配，不删除 `_known_resources`。当前 Turn
     再次看见资源时，失败与冷却立即清零。
  4. 只有友方视野重新照亮该格且当前 `resource_cells` 不含它时才删除提示，符合“当前完整 state
     胜过历史”的规则。
  5. `resource_hints` 是元数据而不是资源事实源：孤立 hint 不能反向制造幽灵资源；单条损坏字段
     回退默认值，不再清空资源、障碍、敌核和探索地图。
  6. 冷却只标记持久状态 dirty，`decide()` 末尾合并落盘一次，避免多个不可达 Worker 在 15 秒
     命令窗口内重复序列化整张探索图。
  7. A* 的 4000 次展开预算耗尽不等于不可达：超远但可达的历史资源继续用贪心前进一步，不进入
     冷却；单个 Worker 回退时用完整 idle Worker 集合重算，不清除其他 Worker 的稳定前沿。
  8. 可见资源的 `last_confirmed_tick` 每 64 Tick 批量刷新；没有资源集合、失败/冷却或确认批次
     变化时不写整张状态，避免探索地图增长后每 Tick 产生无意义序列化。
  9. 正常 Core 同格且容量可用时，带货 Worker 必须在 boxed/stuck 恢复之前直接 `DEPOSIT`；
     历史循环轨迹只能影响移动，不能把已经到核的货物重新带走。
- **Telemetry（已落地）**：`play.py` 每 Tick 输出
  `eco[a,av,ah,blk,cool,unr,harv,dep]`；`meta/monitor.py` 已解析并累计到文本与 JSON KPI。
  这些字段分别表示总分配、可见分配、历史分配、动态阻塞、冷却候选、不可达目标、实际采集量、
  实际交付量，且日志路径不读取或拼接 API key。
- **验证**：`85 passed`，覆盖友方满占改派、敌占改派、旧状态升级、元数据往返、坏字段隔离、
  孤立 hint、失败上界、冷却重启/到期、再次可见恢复、合并落盘和 monitor 解析。
- **Live 证据**：最终进程 PID `32572` 连续提交且无新认证、协议或提交错误；`t56410` 记录
  `HARVEST_SUCCEEDED[1]/harv1`，随后 `t56414` 带货 Worker `e098a6` 到 Core 后明确下发
  `deposit`，`t56415` 收到 `DEPOSIT_SUCCEEDED[1]/dep1`，Core `r35 -> r36`。同段日志出现
  `a4,av3,ah1` 与 `a5,av4,ah1`，证明当前可见资源和持久历史提示正并行参与分配。
- **后续优化方向（按证据依赖排序）**：
  1. 连续积累 100-200 个有资源机会的 live Tick，先以 `deposit resource/tick` 为主指标，
     同时看 `blk/cool/unr`；没有样本前不凭感觉调距离或冷却常量。
  2. 记录 Worker 级“首次分配 Tick、采集 Tick、入核 Tick”，计算发现→采集、采集→入核的
     P50/P95；当前 telemetry 只有每 Tick 漏斗计数，还不能诊断时间花在出站还是返航。
  3. 在每个 Worker 的 Manhattan 前 K 个候选上缓存 A*，将成本升级为
     `confidence * yield / (outbound + harvest + return + deposit_wait)`；禁止全量
     `Worker × Resource` A*，避免错过 15 秒窗口。
  4. 为探索加入“虚拟收益候选”，用历史资源可信收益与前沿预期发现收益竞争；当旧提示太远、
     太老或多次冷却时，部分 Worker 应继续扩图，而不是全员追逐历史坐标。
  5. Core 满仓时让载货 Worker 进入距 Core 1-2 格的缓冲队列，并结合本 Tick spawn/消费计划
     预测下一 Tick 的可交付空间，避免带货 Worker 被派远后错过入核窗口。
  6. v0.14 没有 upkeep。人口 19 可以生产第 20 个基础价 Unit；人口达到 20 后，下一次生产才
     进入动态价格。Worker 仍以 20 为软目标，战斗补编可在敌情或军备缺口下突破该目标；超过 20
     的纯 Worker 扩张必须等滚动窗口 ROI 覆盖动态生产成本和防御风险。
- **状态**：✅ 阻塞漏分配、历史提示可信度和监控闭环已修；A* 往返 EV 与 Worker 级时延待 live
  数据支撑后实施。

## L16 — 多个 Agent 客户端会互相覆盖计划，重复 Tick 不能做实验
- **现象**：同时存在 LaunchAgent 和两个孤儿 `play.py`，同一 Tick 在 `game.log` 出现 2-5 次，
  且不同进程为同一 Worker 生成了不同路线。表面上 monitor 有 202 条样本，实际只有约 41 个
  唯一 Tick。
- **规则根因**：同一玩家的所有 Agent 客户端共享一个 Agent 计划槽；同 Tick 后提交的完整计划
  覆盖先提交计划，不会合并。多个客户端既污染日志，又真正改变服务器最终执行的计划。
- **处置**：只保留 `io.arenahero.tactic` 管理的进程，终止两个孤儿进程并重启 LaunchAgent；
  新日志从 `t57607` 单条记录重新起算，首条含 `DEPOSIT_SUCCEEDED[1]`，无协议错误。
- **方法修正**：
  1. 每次测试/live 前同时核验 LaunchAgent PID 与系统 `play.py` 进程数，必须恰好为 1。
  2. KPI 按唯一 Tick 聚合并单独报告重复 Tick；旧的重复日志只作故障证据，不作 A/B 基线。
  3. 执行 Agent 不得启动 live 或操作 LaunchAgent；只有主代理可以重启、canary、commit 和 push。
  4. 外部 Agent 的提交即使测试通过，也必须按最新 `HEAD` 重新做规则、连续 Tick 和 live 审查；
     纯函数目标值不能证明生产策略在真实人口门槛下可达。
- **治理落地**：新增 `OPTIMIZATION_LOOP.md` 与 `ITERATION_PLAN.md`，明确专家只读、执行隔离、
  主代理验收、样本门槛和回滚条件。
- **状态**：✅ 单实例已恢复；唯一 Tick 与耗时 telemetry 列为 `OBS-001` 首个执行任务。

## L17 — 决策耗时、提交网络耗时和资源行为必须分层验收
- **现象**：OBS-001 canary 的 `decide_ms` P50/P95/P99 为 `141/177.55/231.51ms`，但
  `submit_ms` 约 1 秒；若把提交网络耗时直接当作算法计划门槛，会错误触发性能告警。同期战场
  只有 1 个 Worker、0 次采集，说明可观测性 canary 通过不代表资源策略已经改善。
- **根因**：`decide()` 是本地地图/匹配/路径计算，`turn.submit()` 还包含服务端和网络往返；二者
  的优化责任和噪声来源不同。失败提交与重复 Tick 也不能混入成功业务 KPI。
- **修复（已落地）**：日志同时记录 `decide_ms`、`submit_ms`、总本地耗时；严格 P50/P95/P99
  门槛只作用于 `decide_ms`，提交与总耗时保留在报告中。monitor 只累计首次成功唯一 Tick，
  失败记录、错误码和重复记录单独计数。
- **复用规则**：先用唯一 Tick 和错误护栏确认观测链路，再分层判断算法复杂度、网络延迟和资源收益；
  任何低采集告警都必须回到战术/战场状态取证，不能由 telemetry 改动背锅。

## L18 — 同 Tick 资源账本必须覆盖交付、Unit HEAL 与 Core 动作
- **现象**：Unit HEAL 在 Core 动作之前结算，但策略曾用同一个 `turn.resources` 分别判断多个
  Unit 的 HEAL 和 Core 的生产/修盾；同一 Tick 可能同时排出总成本超过库存的动作，失败只在下个
  state 中暴露，既浪费动作窗口，也会占住 Core 格。
- **根因**：规则顺序是 Worker deposit -> combat -> Unit HEAL -> Core action；决策代码按控制器
  分组执行，没有把 pending deposit 的可接收数量和低血 Unit 的完整恢复成本放入同一预算。
- **修复（2026-08-06）**：先计算不超过 Core 容量的 pending deposit，再按 raw UUID 顺序预留
  Vanguard/Ranger 的低血 HEAL 成本，剩余预算才交给 Core 修盾/生产；资源不足时不排队第二个
  HEAL。可见敌人不再要求先有 2 个 Worker，Core 会立即尝试建立 Vanguard。
- **复用规则**：凡是同 Tick 有多个资源消费者，先按完整结算顺序建立预算，再排动作；测试必须
  同时断言 Unit action、Core action 和 pending deposit，不要只验证单个纯函数的目标数量。

## L19 — 监控必须按失败原因和敌方实体类型分层
- **现象**：实时日志有 `41` 次 `UNIT_MOVE_FAILED.MOVE_CONTESTED`，旧监控只累计
  `CELL_UNIT_LIMIT`，把移动阻塞低估为 `0`；同时 `NO_RAID` 曾按“任意敌人可见”触发，容易把
  敌方 Worker 当成敌 Core。
- **根因**：事件名的原因位于 `UNIT_MOVE_FAILED.<reason>`，而 `vis[...]` 还携带 `C`、`WORKER`
  等实体类型，解析器只读取了数量，没有消费这两个维度。
- **修复（2026-08-06）**：新增总移动失败、`CELL_UNIT_LIMIT`、`MOVE_CONTESTED` 分项；拥堵率
  使用所有移动失败/成功事件；新增明确敌 Core 可见 Tick，`NO_RAID` 仅由该字段触发。回归测试
  覆盖混合失败和“仅敌 Worker 不告警”。
- **纠正**：此前“113 个敌人可见但没有敌 Core 证据”的判断不成立；修复后同一窗口显示
  `117` 个敌人可见，其中 `28` 个 Tick 有敌 Core。结论必须以实体级解析后的证据为准。
- **复用规则**：任何告警都要先验证事件子类型和对象类型；统计字段应保留总量与可解释分项，
  避免用一个粗粒度计数替代战术事实。

## L20 — 载货回程必须同时满足全局可达与局部不回头
- **现象**：资源 `[321,134]` 在 `t61485` 采集成功后，载货 Worker 进入敌 Core/障碍密集区的回程
  路线；A* 能找到全局路径，但局部首步可能回到最近离开的格子，造成短环，直到 `t61517` 仍未
  交付。
- **根因**：回程分支只消费 A* 首步，没有把 `_avoid_set()` 纳入首步验证；A* 失败时的回退还会
  清空 `_pos_history`，动态阻塞变化后每 Tick 都重新选择同一局部方向。
- **修复（2026-08-06）**：A* 首步命中最近历史格时改用避回头的贪心步；只有确实没有可行步时才
  允许无历史回退。探索超半径回程沿用同一规则，新增载货回程回归测试；提交 `df14f9a` 已在
  `main` 推送。
- **验证**：静态全量 `112 passed`；新 LaunchAgent 单实例 canary `t61524..t61545` 共 22 个
  唯一 Tick，零提交/窗口/协议错误，决策 P50/P95/P99 为 `23.5/43.7/72.44ms`。该窗口无自然
  资源，不能用 `0 harvest/0 deposit` 判断经济收益。
- **复用规则**：全局寻路负责“有路”，局部历史负责“不要原地绕圈”；任何路径修复都必须同时做
  静态回归和单实例连续 Tick 验收，并把无资源窗口与真正的采集失败分开。

## L21 — 探索名额不能牺牲最近的可见资源采集者
- **现象**：同一窗口反复看到资源 `[321,98]`，但两个空载 Worker 中离资源约 3 格的 Worker
  被保留为探索者，约 52 格外的 Worker 才会接收资源任务；旧进程因此连续移动却没有采集。
- **根因**：资源分配在 Hungarian 之前先按“离 Core 最远”保留探索者。该规则只适合无可见资源，
  有可见资源时会把最近采集者错误排除在派单集合之外。
- **修复（2026-08-06）**：保留探索名额时改按“离当前可见候选最远”选择 Worker，再用原有全局匹配
  分配可见资源；带货 Worker 跳过探索脱困分支，始终先执行回 Core 的 A*/贪心返程。
- **验证**：新增最近可见资源派单和带货 boxed 回程回归测试；静态测试与唯一进程 canary 通过后，
  才把 `visible_assignments`、采集到交付延迟和入核资源/Tick 纳入收益判断。
- **复用规则**：探索保留应按机会成本选择，不能只看 Core 距离；任何“看见资源但 `a0/av0`”都必须
  先核对运行版本、Worker cargo 和派单排除原因。

## L22（历史 v0.13）— Live 协议漂移必须停止验收，不能在客户端猜字段
- **现象**：重启后的单连接在首个 `state` 解析失败；原始 WebSocket 明确缺少
  `population_tier` 与 `upkeep_next_tick`，而 SDK `0.2.8` 将这两个字段定义为必填。
- **确认**：本地规则包与 SDK 版本为 `v0.13 / 0.2.8`，`uv pip install --upgrade --dry-run arena-hero`
  显示无可升级版本；同一原始消息可读但无法通过官方严格模型验证。
- **处置**：停止 LaunchAgent，保留 `ProtocolError` 证据；不修改 site-packages、不放宽 Pydantic
  校验、不凭默认值继续提交计划。服务端、公开 schema 和 SDK 必须先恢复同一协议版本。
- **复用规则**：任何 live 规则相关改动先做原始握手与版本核对；协议错误窗口不得混入资源 KPI，
  也不能把“进程在重启”误报成策略失败。

## L23 — 服务端升级后必须同步严格 SDK，而不是兼容旧状态字段
- **触发**：官方文档已发布 gameplay `v0.14` 与 Python SDK `0.2.9`；新 `PlayerState` 删除
  `population_tier`、`upkeep_next_tick`，并以 `unit_cost()` 暴露动态生产价格。
- **修复（2026-08-06）**：skill 快进到官方提交 `a63222e`，项目依赖升级到 `arena-hero==0.2.9`，
  测试状态夹具移除已删除字段，生产判断改用 SDK `unit_cost`。
- **验证**：线上 source/version 与本地 bundle 均为 v0.14/0.2.9；SDK 严格模型可解析新状态，
  动态价格边界（人口 19/20/25）通过本地探针。协议恢复后才重新启动单实例 canary。
- **复用规则**：遇到 `ProtocolError` 先核对 source/version 和 PyPI 版本；不要给严格模型补默认字段，
  也不要把旧规则的 upkeep 假设带入新价格规则。

## L24 — 前沿寻路失败必须与预算耗尽分开处理

- **现象**：持久地图生成的未知前沿点可能落在障碍口袋后。原逻辑在 A* 返回空步后无条件使用
  `_step_toward()`，Worker 会继续追逐同一不可达坐标，在东南小区域反复移动而不产生新探索。
- **根因**：A* 的空结果有两种含义：开放集耗尽代表已证实不可达；展开预算耗尽只能说明本 Tick
  没搜完，不能把两者混成一个回退分支。无限开放网格上的封闭目标还可能一直耗尽预算，因此目标四邻
  全阻塞也是明确不可达证据。
- **修复（2026-08-06）**：前沿目标增加短期指数冷却和失败计数；目标封闭或 A* 确认不可达时清除
  Worker 旧目标并重新派单，禁止对该目标继续贪心追逐；预算耗尽保留目标并重试。连续 6 Tick
  没有距离下降或新前沿收益时主动换目标。冷却只保存在运行态，不污染持久地图事实。
- **验证**：新增封闭前沿冷却改派、预算耗尽重试、连续停滞计数回归测试；全量 `125 passed`。
- **复用规则**：路径算法必须返回“不可达/预算耗尽”状态，而不是只返回一个空步；任何地图持久化
  策略都要同时验证目标可达性和无进展重派，否则“记住地图”会变成“记住死路”。

## L25 — 低采集率先查“认领兑现链”和测试污染

- **基线事实**：`t63430..t63490` 的 `61` 个唯一 Tick 只有 `2` 次采集、`2` 次交付、`0` 次生产；
  `51` 次资源认领中有 `29` 次历史提示，决策 P50/P95/P99 约为 `16/33/103ms`。因此不能把低收益
  归咎于 A* 或 15 秒窗口，也不能只看 `a/av/ah` 认领数判断采集成功。
- **根因教训**：资源调度必须同时保证“当前可见资源格上的空载 Worker 优先兑现”和“无资源时前沿分散”；
  历史提示只能作为候选，不能抢占现场采集者。采集失败后若继续保留同一提示，会形成重复往返。
- **修复**：匹配前固定当前可见资源格上的 Worker；资源多于 Worker 与 Worker 多于资源两种 Hungarian
  形态都对历史目标加可见性惩罚；探索目标按八方向扇区分散；`HARVEST_FAILED` 的耗尽/非资源原因
  立即清除提示，当前 state 重新出现时再恢复。
- **额外故障**：pytest 直接使用默认 `tactic_state.json`，会在测试结束后写入测试坐标，导致 live 重启加载
  错误地图。这不是战术逻辑，而是验收隔离缺陷；测试夹具现在强制使用 `tmp_path`。
- **可复用规则**：资源 KPI 必须按唯一 Tick 统计完整 `harvest -> deposit` 链路；每次运行测试前后核对
  持久化文件时间与坐标，任何测试不得写 live 状态；只有达到样本门槛后才能调整距离、冷却或人口参数。

## L26 — 资源干旱时扩大探索，不能永久锁死在经济半径

- **现象**：最新 live 窗口 `t63559..t63588` 的 30 个唯一 Tick 全部提交成功、移动无失败、无敌情，
  但 `HARVEST=0/DEPOSIT=0`；持久地图仅有 599 个已探索格且没有有效资源提示，Worker 只在旧边界附近
  往返。继续增加人口不会改变“没有被看见的资源”这一瓶颈。
- **规则依据**：v0.14 自然节点按 chunk 配额每四 Tick 补足，雾区坐标不会下发；资源提示为空不等于
  全图无资源。探索半径若永久固定在 40 格，可能错过邻近 chunk 的补给节点。
- **修复（2026-08-07）**：新增内存态资源干旱计数。无可见且无有效历史提示时，空载 Worker 每 8 Tick
  扩大 16 格前沿半径，最多 96 格；发现资源后立即恢复 40 格。带货 Worker 始终使用固定 A* 回 Core，
  不因扩张牺牲交付。
- **验证**：新增“干旱扩张”和“可见资源复位”回归测试；全量 `133 passed`，并通过 compileall、依赖检查
  与 diff 检查。live 收益尚未达到 200 Tick/50 条完整链路门槛，当前只能确认行为已切换，不能宣称最优。
- **可复用规则**：探索策略应有“常态经济半径 + 无资源自适应扩张 + 见资源立即收缩”三态；扩张实验必须
  同时监测发现率和带货回程延迟，不能只看 Worker 的移动数量。

## L27 — 和平期 Worker 桥必须有上限，敌 Core 信号必须优先

- **现象**：`t63617..t63675` 已能在发现资源后立即派单，但资源增长仍受 `W6/V2/R0` 的发现并发度限制；
  同时旧桥接条件只检查近 Core 威胁，远处可见敌 Core 可能被误当成和平窗口。
- **修复**：无敌情且已有战斗单位时，经济桥上限从 `W7` 提到 `W8`，让一个额外 Worker 承担发现和容量；
  桥接条件改为 `not enemy_present`，任何可见敌人或敌 Core 都会先保留攻击单位预算。
- **验证**：新增 W7->W8 生产与远处敌 Core 阻断回归测试；全量 `135 passed`，compileall 和 diff 检查通过。
- **可复用规则**：资源扩张只能发生在明确无敌情的窗口；“可见但不在威胁半径”的敌方 Core 仍是攻击信号，
  不能因为距离远就消耗军备储备。收益必须用唯一 Tick 的完整 `HARVEST_SUCCEEDED -> DEPOSIT_SUCCEEDED`
  链路验证，不能只看 Worker 数增长。

## L28 — 长回程窗口要增加发现并发，但不能越过动态价边界

- **基线**：`t63692..t63744` 的 `53` 个唯一 Tick 中仅 `3` 次采集、`3` 次交付；新节点距离 Core
  `30-41` 格，决策耗时和协议链路均正常，`W7/V2/R0` 成为经济并发上限。
- **偏差**：把和平期经济桥从 `W8` 延长到 `W12`，仍保留可见敌情阻断和 `population < 20` 的价格边界，
  让更多 Worker 在已有战斗单位守护下并行发现资源和提高容量。
- **结果**：静态回归覆盖 W11->W12 允许生产、W12 在缺失 Ranger 时停止扩张；live 收益需要通过唯一 Tick
  的完整采集到入核样本继续验证，当前不把 canary 前的行为变化宣称为收益提升。
- **可复用判断**：资源点远且回程占主导时，优先增加低价发现并发；每次扩张必须设定动态生产价和敌情
  停止线，并用入核资源/Tick 而不是 Worker 数判断是否值得保留。

## L29 — 历史资源提示不能伪装成当前资源

- **基线**：资源干旱逻辑原本在“当前没有资源”且“存在未过期历史提示”时复位计数；因此只要
  `known_resources` 里留有一条提示，探索半径就长期停在 `40` 格。历史窗口 `t63764..t63784`
  连续 `21` Tick 无可见资源，但 `ah=2-3`，正好重现这一锁死。
- **偏差**：把干旱状态改为只由当前 authoritative `turn.resource_cells` 决定；历史提示仍保留为
  低置信度派单候选，但不再阻止空载 Worker 的前沿扩张。新增测试覆盖历史提示存在、当前资源为空的
  情况。
- **结果**：静态测试从 `137` 增至 `138` 全部通过；干旱计数在历史提示存在时仍增长，达到阈值后
  `_exploration_radius()` 会从 `40` 扩大到 `56`，带货回程逻辑未修改。Live 收益仍待唯一 Tick
  样本达到 `200/50` 门槛后验证。
- **可复用判断**：持久地图只保存“曾经看见”的坐标，绝不能用它复位当前观测状态。凡是自适应计数，
  都要明确区分 authoritative state、历史 hint 和事件回放；历史 hint 可以影响候选排序，不能
  关闭发现恢复机制。

## L30 — 当前资源认领要稳定，历史认领要可让位

- **基线**：`t63830..t63890` 的 `61` 个唯一 Tick 中仅 `3` 次采集、`5` 次交付；可见资源出现时
  调度器每 Tick 重新按距离做匹配，日志无法证明同一资源是否持续由同一 Worker 追踪。
- **偏差**：增加内存态 `_resource_claims`。当前可见节点保留原空载 Worker 的认领，直到采集成功、
  失败或被当前视野确认消失；历史节点在可见节点出现时释放，并保留一个 Worker 做前沿探索。
- **结果**：静态回归从 `138` 增至 `139`；重启后的 canary 首个新窗口出现采集成功，尚未出现新增协议、
  提交或窗口错误，收益样本仍不足以接受该策略。
- **可复用判断**：资源调度应区分“短期行动意图”和“持久地图事实”。现场节点需要防止换主造成的
  目标漂移，历史提示必须有界且可让位；验收必须继续使用完整 `HARVEST_SUCCEEDED -> DEPOSIT_SUCCEEDED`
  链路，而不是只看认领数量。

## L31 — 带货回程必须先预约，监控事件必须按外层字段解析

- **基线**：`t63903..t63966` 的唯一 Tick 中有 `8` 次采集、`7` 次交付和 `2` 次
  `CELL_UNIT_LIMIT` 移动失败；`MOVE_CONTESTED=0`，资源净增只有 `2`。同一 Tick 的 Worker
  规划只依据 Tick 开始时的占位，后续 Worker 可能抢走带货回程的下一格。
- **偏差**：执行 Worker 计划时先处理带货 Worker，并维护本 Tick 的 `reserved_destinations`；
  同时把历史认领的换主条件改为“可见节点至少领先 `6` 格”，保留已经走近的兑现路线。监控的
  `RE_EV` 改为寻找 `] plan[` 外层分隔，能够完整解析 `HARVEST_SUCCEEDED[1];CORE_SPAWN_SUCCEEDED`
  这类复合事件。
- **结果**：静态回归由 `139` 增至 `143`，全量测试、编译、依赖和 diff 检查通过；live 收益尚未
  验收，必须在重启后的单进程窗口按唯一 Tick 重新统计。
- **可复用判断**：多单位路径规划要把“本 Tick 已承诺的目的地”视为动态障碍，并让有经济价值的
  带货路线拥有确定性优先级；日志解析遇到嵌套方括号时不能用首个 `]` 截断，监控修复后应重算
  历史窗口，不能把旧 KPI 直接当作基线。

## L32 — 历史资源要限距，当前可见资源不能被半径策略误杀

- **基线**：`t63903..t64061` 中移动成功率很高，但采集/交付频率低；历史资源认领 `145` 次，
  而当前可见认领不到 `100` 次。采集距离与回程延迟显著相关，但最新样本仍有不少距 Core
  超过 `30` 格的当前可见资源。
- **偏差**：在全局资源匹配中只过滤非可见的远端历史提示：健康期半径 `30`，连续 `24` Tick
  没有成功采集时放宽到 `65`；当前可见节点、资源格上的 Worker、已在路上的认领和距离 Worker
  不超过 `4` 格的历史点继续保留。用 `far` 计数写入经济遥测；Worker 生产目标同时钳在 W12
  桥接上限。
- **结果**：离线回归从 `143` 增至 `147`，覆盖远端历史保留、干旱放宽、可见远点和 W12 上限；
  live 收益尚未宣称，需要单进程 canary 用唯一 Tick 重算。
- **可复用判断**：距离是回程成本，不是资源有效性的硬事实。对陈旧提示做有界主动派发，对
  当前可见事实保留完整机会；任何半径策略都必须同时测试“远但可见”和“近 Worker 已到达”两个
  反例，并通过 `far` 遥测观察是否把候选池收得过窄。

## L33 — 和平期战斗生产要保留库存，空载兜底不能回到 Core

- **基线**：最新单进程窗口 `t64097..t64165` 的 69 个唯一 Tick 有 7 次采集、9 次交付；
  和平期在 `r12` 时连续提交两次 Ranger 生产，成功后库存从 `13` 降到 `1`，随后库存只能
  缓慢回升到 `5`。同一窗口还出现 1 次 `CORE_SPAWN_FAILED.CELL_UNIT_LIMIT`，失败 Tick 的
  空载 Worker 从 Core 邻格进入 Core，生产与交付都白费一个 Tick。
- **偏差**：已有 Vanguard 防线且没有敌人时，Ranger 生产要求支付后仍保留 5 点库存；任意
  可见敌人会取消该保留，继续即时造攻击单位。空载 Worker 的最终脱困兜底也把 Core 格加入
  动态阻塞集合，只有带货 Worker 才能进入 Core 交付。
- **结果**：全量离线测试 `150 passed`，编译、依赖检查、差异检查和敏感信息扫描通过；live
  收益尚未验收，不能把库存改善外推到长期资源吞吐。
- **可复用判断**：高成本的和平期军备必须有库存下限，否则短期“完成编制”会直接吞掉净资源；
  防御下限与敌情即时响应要分开。任何 Worker 兜底路径都必须复用“空载不得进 Core”的
  不变量，并用 `CELL_UNIT_LIMIT` 回归测试锁定。

## L34 — 前沿评分必须先看兑现时间，再看未知格数量

- **基线**：最新唯一 Tick 窗口 `t64191..t64237` 中，2 次采集、4 次交付且无任何命令或移动错误；
  持久探索格只增加约 25 个。复现当前探索地图后发现，旧 `_assign_explore_targets()` 先最大化
  `frontier_gain`，再考虑到达距离，单个 Worker 会被派到 50-70 格外的高收益前沿。
- **偏差**：为探索任务增加四向覆盖预算；先为未覆盖主方向挑选距离最近且间距至少 6 格的目标，
  覆盖完成后，剩余 Worker 也按自身到达距离优先，再用未知格数量作为次级排序。
- **结果**：新增回归测试在旧实现下稳定失败（高收益远点被选中），修复后全量测试 `151 passed`；
  该修改尚未完成 live 收益验收，因此不宣称资源吞吐已提升。

## L35 — 和平期 W12 闸门与 v0.14 人口预算必须一致

- **基线**：新进程 `t64264..t64294` 的 31 个唯一 Tick 无任何命令、移动或 Core 错误，但 4 次采集
  只有 1 次入核；可见节点离 Core `25-48` 格，带货回程形成主要空窗。代码的 `FREE_UPKEEP_CAP=20`
  允许人口未达动态价格边界，但和平期桥接仍把 Worker 硬停在 W12。
- **偏差**：将 `ECONOMY_BRIDGE_MAX_WORKERS` 提高到 W15。最低 V1/R1 防线仍保留，敌情/敌 Core
  仍关闭桥接，且每次 Worker 生产必须保留 3 点库存；总人口最多 19，下一次生产仍是基础价。
- **结果**：离线测试覆盖 W14->W15 允许和 W15 停止；live 代码重启后才能观察是否降低采集到入核的
  P50/P95，当前不把 Worker 数增加等同于资源收益。
- **可复用判断**：v0.14 无 upkeep 时，生产上限应同时受动态价格、库存储备、敌情和长回程并发约束；
  旧的保守人口闸门若低于规则安全边界，必须以完整入核资源/Tick 重新验收，而不是永久沿用历史 W12。

## L36 — 无可见资源时必须同时做前沿发现和 Chunk 回扫

- **基线**：当前 live `t64264..t64324` 的 61 个唯一 Tick 无协议、提交或 Core 故障，但资源仅
  `0 -> 4`；空载 Worker 的 `exp1` 长时间持续，持久探索地图已有约 4,063 格。规则规定每四个
  逻辑 Tick 会在已跟踪 Chunk 内补充自然节点，单纯追逐未探索前沿会漏掉已探索区域的补充资源。
- **偏差**：无当前可见资源且至少有 4 个空载 Worker 时，固定 2 个稳定 Worker 回到既有的
  Chunk 纵向扫描，其余 Worker 继续四向、近端优先的前沿目标；新增 `eco[..refN..]` 遥测记录回扫
  名额。健康期远端历史认领若尚未走近或过半程则释放，避免一个旧提示锁住几十 Tick 的往返。
- **结果**：静态测试新增远端认领释放、近端认领完成和 2 Worker 回扫保留；live 收益需在重启
  后的唯一客户端 canary 中用采集/Tick、入核/Tick、采集到入核 P50/P95 和 `ref` 计数验收。
- **可复用判断**：持久地图应同时保存“已知事实”和“需要回访的区域”。前沿搜索不能取代周期性
  回扫；历史资源只能作为有界提示，已走近的路线才允许继续兑现。

## L37 — 看到敌核不等于已经建立了可执行突袭

- **基线**：约 110 个唯一 Tick 看见敌 Core，策略把目标扩到 V4/R4，但 Ranger 只有在库存达到
  固定 60 才追击，且 `CHASE_MAX_TICKS=8`；实际没有 `SHOOT`、敌核摧毁或捕获事件，新增军备约
  消耗 58 点资源。
- **偏差**：把突袭门槛改成 `unit_cost(RANGER, population) + 3`，追击预算按到合法射击位的
  ETA 计算并限制在 48 Tick；按 UUID 固定守卫和突袭角色。可合法 `SHOOT/SWEEP` 的 Tick 先于
  Unit `HEAL`，避免低血战斗单位为了治疗放弃击杀窗口。
- **结果**：离线测试新增远程敌核距离 34、动态门槛、ETA 预算和低血 Ranger 攻击优先回归；
  这证明计划可以进入追击分支，但尚未达到 live 的 200 Tick/捕获样本门槛，不能把“开始移动”
  宣称为“突袭成功”。
- **可复用判断**：攻击策略必须同时证明“资源可逆、路径可达、最终有合法攻击动作”；只统计看到
  敌核或生产了战斗单位会把军费误判成收益。接受突袭前必须核对首次攻击延迟、捕获资源减去
  增量军备/治疗成本，以及 Core 生存护栏。

## L38 — Core HP 是比盾和生产更高优先级的资源护栏

- **基线**：原策略只在威胁下修盾，Core HP 受伤时不会自动 `HEAL`；同一 Tick 可能继续生产，
  把库存投入到一个即将失去全部资源的 Core。
- **偏差**：遵循 v0.14 结算顺序，在 Unit HEAL 预留后，Core HP < 5 且有可用资源时先排 `HEAL`，
  再考虑修盾、Beacon 或生产；Core HP 满时才回到原有盾优先逻辑。
- **结果**：原“受伤且受威胁应修盾”回归测试已改为断言 Core `HEAL`，全量测试 158 项通过。
- **可复用判断**：涉及多级防御资源时先保护会导致全局库存丢失的状态变量（Core HP），再保护
  可被下一次攻击穿透的盾，最后才是生产；每次动作优先级变化都要同步更新旧测试的业务意图。

## L39 — Manual receipt 必须先进入日志，才能谈 Agent 收益

- **基线**：旧 `play.py` 只消费 `turns()`，日志没有 `MANUAL` receipt；用户手动生产、治疗、迁核
  或消耗资源会被误计入 Agent 的入核率和库存变化。
- **偏差**：切换到 SDK `events()`，记录 canonical `AGENT/MANUAL` receipt 和受限 v0.14 事件
  成本详情；监控预扫描 receipt，把 Manual Tick 从主比较排除但单独保留事件计数，并保留旧
  `turns()` 假客户端兼容路径。
- **结果**：监控新增 Agent/Manual receipt、可比较 Tick、生产/治疗/修盾/捕获/溢出成本字段，
  不输出 API key、请求头或服务端错误正文；离线监控测试仍通过。
- **可复用判断**：任何人工可干预的实时系统，先建立“来源归属 -> 事件成本 -> KPI 分层”链路，
  再讨论算法收益；没有 canonical receipt 的收益结论只能标为不可归因。

## L40 — 突袭必须以实际战斗结算和净资源闭环验收

- **基线**：旧策略在敌核可见时扩充 V4/R4，但固定资源门槛和过短追击预算让军备长期没有合法
  `SHOOT/SWEEP`，只能看到生产成本，无法证明最终目标“最多资源”得到改善。
- **偏差**：本轮使用动态 Ranger 重建储备、按合法射击位 ETA 的有界追击，并将攻击动作置于 Unit
  治疗之前；同时用 `events()` 的 `SHOT_HIT`、`DESTRUCTION_PARTICIPATION.CORE` 和
  `CORE_RESOURCES_CAPTURED` 做事件级核算。
- **结果**：live `t65805..t65843` 的 `39` 个唯一 Tick 中，敌核可见/预算成立各 `9` Tick，出现
  `12` 次 `SHOT_HIT`，`t65832` 摧毁敌核并捕获 `5` 资源；窗口无错误，经济事件为入核 `3`、捕获 `5`、
  生产/治疗/修盾/溢出 `0`，净资源 `+8`；另有 `1` 次 `UNIT_MOVE_FAILED.CELL_UNIT_LIMIT`，
  未形成失败 Tick、资源损失或伤亡。这验证了攻击执行链，但尚未达到 `200` Tick / `50` 条链路的长期
  收益门槛。
- **可复用判断**：敌情策略的验收顺序必须是“可达 -> 合法攻击 -> 实际摧毁/捕获 -> 扣除增量成本”；
  中间状态（看见敌核、扩军、进入追击）只能作为诊断指标，不能当作资源收益。

## L41 — 规划耗时瓶颈先隔离，再改路径算法

- **基线**：历史窗口的决策耗时较低，容易把新出现的 P95/P99 上升误判为策略收益或直接大范围调参。
- **偏差**：本轮观察到突袭窗口 P50/P95/P99 约 `406/1695/2532ms`，但总耗时最高约 `3.7s`，仍远低于
  `15s` 命令窗口；结合经济与遥测专家的复核，把问题限定为路径搜索耗时候选，暂不改 A* 展开上限或
  战斗分配，先继续收集同一版本样本。
- **结果**：39 个 Tick 无窗口、提交、协议或认证错误，且突袭成功；策略行为与性能指标未被一次混合
  修改打乱，后续可以单独比较路径搜索优化前后。
- **可复用判断**：命令窗口仍有数量级余量时，性能告警先做函数级归因和专家复核；只有在窗口风险或
  连续两个窗口恶化时才提交单一性能假设，并用相同唯一进程口径回归。

## L42 — 没有 Worker 链路关联，就不能判断资源兑现瓶颈

- **基线**：旧监控只有 Tick 级 `HARVEST`、`DEPOSIT` 总数，无法区分长回程、窗口边界携货和真正的
  采集失败；“采集到入核 P50/P95”只能停留在计划指标，不能由日志直接证明。
- **偏差**：保留 `play.py` 的事件级 `actor/tick` 详情，在 `meta/monitor.py` 内按 Worker actor 建立
  临时 harvest 队列，匹配 Agent-only 的 deposit，并只输出链路数、未匹配数和 P50/P95 汇总；Manual
  或 unclassified 事件不进入主指标。
- **结果**：新增 Agent/Manual 链路回归测试，全量测试从 `159` 增至 `161` 并通过；live
  `t65805..t65907` 的 `103` Tick 解析出 `7` 条完整链路，延迟 P50/P95 为 `16/39.7` Tick，未匹配
  入核 `1`，没有改变战术计划或 live 进程。
- **可复用判断**：资源优化先建立“对象 actor + resolution tick + 成功事件”的因果链，再决定增减
  Worker、改变搜索半径或优化回程；未匹配事件必须单独报告，不能默认为资源丢失或算法失败。

## L43 — 间歇敌核可见不能当作连续追击失败

- **基线**：突袭验收要求在动态预算成立后观察合法攻击；若把每次敌核出现简单拼接为连续窗口，
  会把视野断开、敌核移动或重新进入视野误判成追击逻辑失效。
- **偏差**：本轮按 authoritative Tick 逐条记录敌 Core 可见和动态预算 Tick，并单独检查
  `SHOT_HIT`、`DESTRUCTION_PARTICIPATION.CORE` 与 `CORE_RESOURCES_CAPTURED`；不因累计可见 Tick
  增长就修改追击预算。
- **结果**：`t65805..t65956` 的 `152` 个唯一 Tick 中敌核可见/预算成立各 `31` Tick，但第二次敌核
  只在间歇可见段出现；Core、单位和命令窗口均无异常，Agent-only 净资源保持 `+13`。当前没有足够
  证据宣称第二次突袭失败。
- **可复用判断**：战斗时序指标必须保存连续可见段的起止 Tick；只有同一连续段超过追击预算且没有
  合法攻击，才允许触发单变量战斗改动或回滚。

## L44 — 连续可见段内的首攻延迟才是突袭可达性证据

- **基线**：只统计“敌核可见 Tick 总数”无法判断远程 Ranger 是否真正进入合法射击位；间歇视野会
  把搜索时间、视野断开和战斗延迟混在一个数字里。
- **偏差**：把第二个敌核的连续可见段、首个 `SHOT_HIT`、摧毁事件和捕获事件按 resolution Tick
  对齐，并把拥挤移动失败单独列为护栏，不调整已验证的 ETA 追击规则。
- **结果**：第二个敌核 `t65965` 首次连续可见，`t65973` 首攻，延迟 `8` Tick；`t65979` 摧毁并捕获
  `5` 资源。此前累计 `179` 个唯一 Tick 的 Agent-only 净资源升至 `+21`，虽有 `5` 次集中式
  `CELL_UNIT_LIMIT` 阻塞，但无单位死亡、Core 受击或命令失败。
- **可复用判断**：突袭验收必须以“连续可见起点 -> 首个合法攻击 -> 摧毁/捕获 -> 增量成本与拥挤
  护栏”四段证据闭环；只要首攻在预算内且护栏未越界，不应因总可见 Tick 或局部阻塞单独回滚。

## L45 — A* 预算耗尽是归因信号，不是扩大预算的理由

- **基线**：旧监控只有 `decide_ms`，无法区分 A* 调用多、单次展开深，还是网络提交变慢；看到 P95
  超标后直接提高搜索预算，反而可能把有界回退变成命令窗口风险。
- **偏差**：在不改变搜索上限的前提下记录每 Tick A* 调用、展开、预算耗尽和成功路径，并由监控输出
  总量与单 Tick P50/P95；Ranger 满格绕行作为单独的可证伪行为修复进入新 canary。
- **结果**：`t66005..t66036` 的 32 Tick 中 A* 调用 `300` 次、展开 `662324`、预算耗尽 `161` 次，
  但决策 P95 仅 `615.25ms`，无窗口错误；资源样本只有 `1` 条完整链路，不能仅凭性能计数接受
  搜索预算改动。
- **可复用判断**：性能优化必须先建立“算法工作量 -> 本地耗时 -> 业务失败/收益”的同 Tick 因果链；
  只有三者同时恶化才调整搜索预算，单看预算耗尽计数不得扩大计算量。

## L46 — 重启后的首个 Turn 仍携带旧计划的结算事件

- **基线**：新进程从 `t66036` 开始记录候选版本，但该 state 同时上报 resolution tick `66035` 的
  `CELL_UNIT_LIMIT`；若只按日志行 Tick 分窗，会把旧版本动作误计为新版本回归。
- **偏差**：同时核对 state Tick、事件自身 `tick` 和进程切换点；性能指标可从首个新 Turn 的新计划
  开始统计，行为结果与失败护栏则从候选版本实际提交后产生的下一次结算开始统计。
- **结果**：`PERF-002` 的有效安全窗口按 `t66037..t66056` 判定，20 Tick 内移动/资源失败为 0；
  边界事件保留在原始日志中，但不错误归因给 `e2aa73e`。
- **可复用判断**：实时回合系统切换版本时必须区分“当前版本生成的计划”和“当前 state 携带的上一
  Tick 结果”；canary 护栏从首个候选计划完成结算后起算，不能仅按进程启动后的第一行切日志。

## L47 — 降低搜索预算先证明护栏安全，再谈资源收益

- **基线**：PERF-001 每 Tick A* 展开 P50/P95 为 `22649.5/37918.8`，决策 P50/P95 为
  `268/615.25ms`；但只有 `1` 条完整资源链路，无法直接判断更少计算是否会损害兑现率。
- **偏差**：只把允许直线回退的空载 Worker 资源/前沿搜索预算从 `4000` 降到 `2000`，保持带货回核、
  战斗、认领和人口不变；新窗口独立计数，不与重启前日志拼接。
- **结果**：21-Tick canary 的 A* 展开 P50/P95 降至 `12502/20576`，决策 P50/P95 降至
  `204/385ms`；无窗口、协议、Core/Unit 或资源失败。日志中唯一 `1` 次满格阻塞属于上一 Tick 的
  旧计划结算（见 L46）。完整链路仍只有 `1`，所以当前只接受“性能护栏通过”，不接受“资源收益提升”。
- **可复用判断**：搜索预算实验分两阶段验收：短 canary 证明安全与计算量下降，长窗口再证明业务收益
  不退化；缺少足量完整链路时不得用入核边界事件或短窗净资源替代长期结论。

## L48 — “可见敌人”与“近核威胁”是两个不同信号

- **基线**：源码和最终目标都写着任意可见敌人应及时触发攻击单位，但实现只把距 Core `<=6` 的
  `threats` 或敌 Core 计入 `enemy_present`；前线 Worker 看见的远处普通敌方 Unit 仍走和平生产。
- **偏差**：先用 `W1/r10`、前线 Worker 与距 Core 7 格敌 Ranger 的测试复现旧策略错误生产 Worker，
  再只传递当前 Turn 的 `enemy_visible` 布尔信号；修盾半径、防御扩编和敌核突袭信号均保持原边界。
- **结果**：回归测试修复前得到 `SPAWN WORKER` 并按预期失败，修复后得到 `SPAWN VANGUARD`；全量
  `167 passed`。新版本 `t66135..t66154` 的 20-Tick 安全 canary 无错误、伤亡或资源/移动失败，但
  可见敌人 Tick 为 `0`；真正的敌情行为仍需等远处普通敌人再次可见后确认。
- **可复用判断**：实时策略要把“当前可见任何敌人”“已进入资产威胁半径”“可突袭的敌 Core”分成
  三个信号：前者关闭经济桥，第二个触发修盾/防御编制，第三个决定突袭扩编；不能用一个距离过滤结果
  同时代表三种业务含义。

## L49 — 满核时必须区分“有库存”与“有可交付容量”

- **基线**：此前只把资源达到容量视为“继续探索”，但当所有 Worker 都已经带货时，探索不会增加
  可用库存；`r==capacity` 会让 Worker 的 `DEPOSIT` 全部失败。W15/V4/R4 的实际窗口出现约
  1,000 Tick 满核平台，监控在窗口结束后又把最长平台错误显示为 0。
- **偏差**：把“满核 + 全 Worker 带货”作为独立的经济状态，只有在和平、军备齐备且未到 W21 时
  生产一个按 `unit_cost` 定价的容量电梯 Worker；同时让监控保存满容量历史最长连续段，输出
  `sat[full,elev]` 和 `FULL_CORE_LOCK`，不把 A* 预算或敌情拥挤与容量修复混在一起。
- **结果**：离线回归覆盖动态价 W23 的 Worker、满核空载不触发、满核全带货触发、障碍包围时不
  排必败生产及遥测；全量测试 `171 passed`。`8342d8c` 的 `t72455..t72475` 安全窗口无错误、伤亡
  或移动/资源失败，但没有满核样本，不能宣称收益；窗口后的普通 `CELL_UNIT_LIMIT` 单独归入拥挤
  移动问题，历史 50/50/95 资源骤降继续按未归因外部消费处理。
- **可复用判断**：经济状态机应同时观测“库存水平、容量、Worker cargo、消费出口”；满库存不是成功，
  满库存且全员带货是可证伪的交付死锁。任何容量释放策略都必须先通过敌情/军备/动态价格边界，再用
  `spawn -> capacity increase -> deposit` 的同一版本事件链验收。

## L50 — Core 已有占位时，带货退让必须显式封锁 Core

- **基线**：`_step_away_from` 为避免 Worker 在拥挤环路中停滞，会在没有增加 Core 距离的候选时
  退化到任意开放格。交付分支原本只把障碍、敌方和满格友方单元加入 blocked，Core 本身被解封；
  当 Core 上已有带货 Worker 正在同 Tick `DEPOSIT`，邻格带货 Worker 可能把 Core 作为唯一兜底目标，
  产生必败的 `CELL_UNIT_LIMIT`，虽然不丢资源却延迟一 Tick 交付。
- **偏差**：只在“Core 有占位且当前 Worker 邻接 Core、可用相邻出口不超过一个”的局部退让分支中，
  使用 `blocked | {core_pos}` 调用退让函数；没有合法离核步就不提交移动。满核容量电梯所用的其他
  `_step_away_from` 调用保持不变，避免扩大行为范围。
- **结果**：新增“Core 是唯一剩余候选格”回归测试；离线目标测试通过。live 版本需在新进程边界后
  用 20 Tick 安全 canary 和至少 100 Tick/20 条完整链路继续验证，现有 `t72476` 的一次冲突不与
  新版本混算。
- **可复用判断**：在共享容量的移动规划中，Core 既是可进入的交付目标也是不可抢占的占位实体；
  同一 helper 在不同经济状态下的“开放格”语义不能直接复用。任何 fallback 退让都要按分支明确
  排除会在本 Tick 解析顺序中必败的 Core 目标，并以 `CELL_UNIT_LIMIT` 的事件级计数验收。

## L51 — 主动追击必须先证明可逆，再扩大敌情暴露范围

- **基线**：既有非守卫 Ranger 会在 Core 周边 `CHASE_RADIUS=15` 内追击普通敌方 Unit，但没有区分
  “近核护卫”和“中距离主动出击”；库存为 0 时也会把 7–14 格敌人当作追击目标。这样可能把唯一
  的远程火力带离 Core，且没有资源重建余量。
- **偏差**：保持 15 格范围、12–48 Tick ETA 预算和首个 Ranger Core guard，只在普通敌方 Unit
  超过 6 格且仍未超过 15 格时增加 `unit_cost(RANGER, population)+3` 动态门槛；低资源保留近核
  拦截，高资源才执行可逆的中距离主动追击。合法 1–3 格射击不受门槛影响。
- **结果**：新增三项边界测试，测试先失败后通过；全量测试达到 174 项。当前 live 尚未出现可见
  敌方 Unit，因此新行为仍需独立 canary，不能把静态测试或“发现敌人”当成 `SHOT_HIT`/资源收益。
- **可复用判断**：实时战斗策略要把“可立即命中”“Core 近区防守”和“中距离主动出击”分成不同
  状态。主动行为的最低门槛应至少覆盖一次同类单位的动态重建成本和小额缓冲，并用首攻、伤亡、
  成本与入核净资源的同一 Tick 链路验收。

## L52 — 主动追击安全 canary 只能证明护栏，不能证明战斗收益

- **基线**：`RULE-005` 的离线测试已证明低资源中距离不追、高资源中距离追击、低资源近核仍追击，
  但真实敌方 Unit 尚未进入当前视野。
- **偏差**：重启唯一 `io.arenahero.tactic` 后只用当前 authoritative state 验收，不人为注入敌情，
  观察 `t72602..t72623` 共 22 个唯一 Tick；逐 Tick 检查 Agent receipt、失败事件、Core/Unit 状态、
  资源和可见敌情。
- **结果**：22/22 `ST[ACCEPTED]`，无 Manual、窗口/协议/提交错误、移动/资源失败、死亡或 Core 伤害；
  库存 `100/100`，Core `5/5`，`W12/V4/R4`，`vis0[-]` 覆盖全部窗口。仅出现两次采集，没有
  `SHOT_HIT`、`SWEEP`、生产或捕获事件，因此没有主动攻击收益样本。
- **可复用判断**：安全 canary 的“无敌情”是明确的未触发条件，不是战斗逻辑通过；只有记录到
  7–14 格目标、库存门槛、首攻延迟、命中/伤亡和资源成本的同一连续链路后，才能评价主动出击收益。

## L53 — 动态价格边界不是人口上限，扩容还必须通过移动拥塞收益门槛

- **基线**：旧策略把“人口 20 的首个动态价档”与 Worker 目标共用，长期停在 `W13/V4/R4`、
  人口 21 左右；规则本身允许 `N=20..24` 使用同一动态价档，但这不等于扩容一定提高资源净收益。
- **偏差**：将总人口预算设为 24、Worker bridge 放宽到 W16，并用 `unit_cost`、敌情优先、军备地板
  和容量电梯护栏控制单变量实验；静态测试通过后提交 `d8c2b6d`，而不是把人口 24 当成永久上限。
- **结果**：live 编制达到 `W16/V4/R4`、人口 24，Worker 单价 7，但库存约 `99→78` 后仅回升到
  约 80；20 Tick 新版窗口出现约 6 次 Core 邻格 `CELL_UNIT_LIMIT`，旧窗口同期约 1 次。Core
  未死亡，但资源收益未证实且移动拥塞明显，遂用 `4be4b41` 回滚并推送。回滚后不销毁已生成的
  W16，20 Tick 安全窗口仍有 2 次移动失败，说明“代码回滚”和“运行态人口回退”是两个不同动作。
- **可复用判断**：遇到人口卡点先拆开“规则价格边界、总人口预算、Worker 目标、运行态编制”四层；
  扩容验收必须同时比较净资源/Tick、采集→入核链路和 `CELL_UNIT_LIMIT`，并把同 Tick Worker 与
  战斗单位目的地预约纳入离线回归。短期人口增长或单次生产成功都不能替代长期资源收益证据。

## L54 — 跨类型移动预约先修复确定性容量冲突，再评估吞吐代价

- **基线**：`decide()` 先排 Ranger/Vanguard 移动、再排 Worker；Worker 只预约自己的目的格。实测
  `t72794/t72797/t72821` 均为“目标已有 1 个单位 + 战斗 MOVE + Worker MOVE”，结算时第三个实体
  以 `CELL_UNIT_LIMIT` 失败，说明不是资源或 Core 生产规则问题。
- **偏差**：只在 `_control_workers()` 初始化预约集合时读取已排队的非 Worker `MOVE` 目的地，并沿用
  一目的格一预约的保守策略；新增 Core 邻格回归先失败后通过，未改战斗互撞、A*、生产、敌情或 Manual。
- **结果**：提交 `5868021` 后重启唯一进程，`t72859..t72878` 20 个唯一 Tick 无任何
  `UNIT_MOVE_FAILED`、资源失败、死亡、Core 伤害或提交异常；资源 `90→91`，2 次采集、1 次入核。
  这证明冲突护栏安全，但短窗净增 `+1` 不能证明资源收益提升；运行态仍保留已有 W16。
- **可复用判断**：实时移动规划要把“当前占用、已排队目的地、同 Tick 解析容量”放在同一张局部图中；
  先用事件级失败证据做最小修复，再用独立 canary 验证安全。保守预约可能阻止空格上本可合法的
  Worker+战斗单位双占，属于下一轮容量感知优化，不能与本修复混合。

## L55 — Worker 分散巡查必须覆盖人口上限并给目标足够停留时间

- **基线**：最近旧进程窗口约 13/16 个 Worker 位于同一东北象限；资源 `90→97` 的 102 次转移中，
  自然采集只有 6 次，Core 邻格没有长期 Worker 聚集，说明发现覆盖和远程回程是主要瓶颈。
- **偏差**：在持久地图前沿不足时，空载 Worker 不再全部回退到同一 chunk 扫列，而是按稳定序号锁定
  四个象限的远端站点；将站点从 12 个扩为 24 个（每象限 6 个、距 Core 32 格），并把轮换周期从
  24 Tick 延长到 64 Tick。已知资源、真实未知前沿和带货回程仍优先于巡查站点。
- **结果**：离线覆盖测试验证 20 个 Worker 目标唯一、四象限齐全、每个目标距离 32，且 64 Tick 内
  目标保持不变；周期锚定进程首次 authoritative Tick，避免重启边界立即换站；加载 6485 个 explored
  cells/1054 个 obstacles 的本地地图做 100 次决策，耗时 P50/P95/最大约 `13.8/177/281ms`，A* 预算耗尽为 0。
  唯一新 PID `80941` 的严格 20-Tick canary
  （`t73013..t73032`）无移动/资源失败，库存 `100→104`，但完整链路样本不足，不能宣称收益。
- **可复用判断**：分散策略不能只看象限数量；必须同时验证“目标数覆盖当前人口上限、最短到达时间小于
  轮换周期、目标在到站前不被改派”。任何 Worker 扩容实验都要先证明四象限覆盖和采集→入核链路没有
  因远端追逐而退化。

## L56 — 双层战斗巡逻不能覆盖敌情优先级或护核回退

- **基线**：战斗单位原有近核守卫和 Ranger 有界追击；直接加入巡逻时，若不区分敌情，Vanguard 可能
  在远处敌人可见时仍被巡逻目标拉走，或因不可达站点等待而失去护核职责。
- **偏差**：保留第一名 Ranger 的 Core guard，最多配两组 Vanguard+Ranger：近层半径约 7、远层约 15，
  每 24 Tick 轮换；巡逻只在无敌情时执行，Ranger/Vanguard 先处理合法攻击、治疗和追击。巡逻 A* 无
  路时落回原护核/探索控制器；所有战斗 MOVE 与 Worker 共用目的地预约。
- **结果**：新增近/远编组、无敌巡逻移动、巡逻单位视野写入持久资源地图、可见敌情不触发 Vanguard
  巡逻等回归测试；补充“巡逻 MOVE 成功后不被回退动作覆盖”的测试后全量 `183 passed`。PID `80941`
  的 20-Tick canary 无移动失败、Core/Unit 损失和敌情事件，但没有真实敌情样本，不能把安全结果当作
  主动出击收益。
- **可复用判断**：巡逻是空闲态的覆盖策略，不是战斗状态机的最高优先级。每次加入巡逻必须明确状态
  优先级（攻击/追击 > 治疗 > 护核 > 巡逻），并测试“站点不可达”和“远敌可见”两条回退路径。

## L57 — 重启边界必须固定版本并锚定巡查周期

- **基线**：Worker 站点按绝对 Tick 取模；进程若恰在 64-Tick 边界重启，第一轮可能刚出发就换站，
  且 `game.log` 会在 play.py 启动时截断，容易把旧计划回执和新版本状态混成同一 canary。
- **偏差**：将 Worker 巡查周期锚定到进程首次 authoritative Tick；监控改用 `play.out` 的最新启动
  marker 切分版本，严格从边界结算后的完整 state 开始计数。新增 `t=63/64/127` 周期边界回归测试。
- **结果**：`4258a17` 的 PID `90297` 在 `t73056..t73075` 完成 20 Tick 安全 canary，库存 `105→107`、
  2 次采集/2 次入核，无移动/资源失败、伤亡或窗口错误；决策 P50/P95/最大 `74/145/185ms`，总耗时
  P50/P95/最大 `1024/1112/1157ms`。四象限长期覆盖和资源收益仍待 100–200 Tick 长窗。
- **可复用判断**：每次重启都要同时记录代码 SHA、PID、启动 marker、严格 canary 起止 Tick 和上一计划
  的结算边界；周期性巡查状态应以进程生命周期或持久状态锚定，不能依赖全局 Tick 的偶然相位。

## L58 — 远端巡查周期必须覆盖实际绕障路径，而不只是曼哈顿距离

- **基线**：四象限 Worker 站点的曼哈顿距离固定为 32，旧周期设为 64 Tick；在真实持久地图
  `6485` 个 explored cells、`1054` 个障碍上，旧 PID `90297` 的 `t73055..t73120` 窗口仍有
  明显北侧偏置（最终 NW/NE/SE/SW 为 `6/5/2/1`）。
- **偏差**：把实际 A* 绕障路径纳入周期评估。专家测得站点路径最长约 53 Tick，旧周期在部分 Worker
  到站前就发生轮换；因此把 `_WORKER_SECTOR_PHASE_TICKS` 从 64 延长到 128（提交 `c6af83a`），
  不抢走资源认领或前沿任务。
- **结果**：边界回归覆盖锚点下 `t=127` 保持目标、`t=128` 才轮换；新 PID `4061` 的 20-Tick
  canary（`t73169..t73188`，排除一次重启边界 `TICK_MISMATCH`）无移动/资源失败、死亡或 Core 伤害，
  资源 `117→120`。四象限长期改善和净资源收益仍未验收。
- **可复用判断**：周期性巡查的 dwell 必须大于“最长可达路径 + 最小观察窗口”，应先用真实地图测得
  P95/最大路径再定周期；安全 canary 只能证明不出错，必须跨完整新周期再判断覆盖和吞吐。

## L59 — 障碍绕行会破坏“唯一站点”保证，必须做 Tick 内预约

- **基线**：离线测试在空障碍地图上验证了四象限巡查站点唯一，但真实持久地图包含 `1067` 个已知障碍；
  Worker index `1` 和 `5` 的首选 NE 站点被障碍挡住后，都回退到相同的 `(12,-20)` 相对站点，实际
  形成重复目标，解释了 `t73169..t73296` 窗口仍有 `NW 8.24 / NE 4.63 / SE 1.55 / SW 1.20`
  的北侧偏置。
- **偏差**：为站点选择函数加入当前 Tick 的 `reserved` 集合，并在 Worker 控制器内先登记前沿目标、再
  逐个登记巡查目标；候选命中已预约格时继续按固定顺序尝试下一个站点。先写回归测试确认旧实现失败，
  再实现最小修复，没有改资源优先级、128 Tick dwell、战斗巡逻或人口预算。
- **结果**：全量测试从 `183` 增至 `184 passed`；用真实障碍集合重放，16 个目标变为 `16/16` 唯一且
  四象限各 `4` 个。PID `11962` 的 `t73311..t73330` 20 状态 canary 全部接受，无移动/竞态/资源/窗口
  失败、死亡或 Core 伤害，`decide_ms` P50/P95/最大 `92.5/172.25/177ms`；采集 `1`、入核 `0`，
  样本不足以证明长期收益。
- **可复用判断**：任何“每个 Agent 目标唯一”的声明都必须在真实障碍、动态占位和同 Tick 计划合并后复核；
  生成阶段的唯一性不等于可达回退阶段的唯一性。站点预约应是当前 Tick 的短期状态，不应写入持久地图事实。
