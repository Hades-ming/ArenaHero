# 最终目标：积累最多的资源

本文件定义战术的**唯一最终目标**，并说明每一条行为如何服务于它。所有迭代、
监控、专家评审都以"是否有利于积累更多资源"为唯一评判标准。

## 最终目标（FINAL OBJECTIVE）

> **在 Arena Hero 中积累最多的资源。**

说明："最多资源"指的是**最高的资源吞吐量与净存量**（持续采集+入核+掠夺敌核），
而不是把资源堆在手里不用——囤积的资源不产生收益，反而在丢核时一次性清零。

## 为什么这样定义

- 游戏分数不暴露在玩家状态里，唯一可观测的、可积累的硬通货就是资源。
- 丢核 = 失去所有库存资源；丢单位 = 失去其携带的货物。所以"保护经济"本身就是
  积累资源的一部分，而非开销。
- 摧毁敌方 Core 可能获得其库存中可容纳的部分；收益取决于敌方库存、同 Tick
  伤害归属和我方战后容量，不能假设固定回报。

## 行为与目标的映射（每个动作都为最终目标服务）

| 行为 | 如何服务"最多资源" |
|------|------------------|
| 主动探索寻找资源 | 采集收入的 #1 杠杆；站着不动收入为 0 |
| 采集并运回 Core | 把发现的资源转化为库存 |
| 不断补 Worker（免费维护带内） | 让收入复利增长 |
| 护核（修盾/常备军/站位） | 避免一次性丢失全部库存（保护经济=保护资源） |
| **有敌及时造攻击单位** | 让来犯之敌付出代价，保护经济不被掠夺 |
| **当前看见敌核时组建突袭队** | 争取不确定库存收益并清除竞争者；历史坐标只用于有限追踪，不永久冻结经济 |
| 避免资源闲置 | 手里的资源不生息；把结余投入经济、防御或军备 |
| 核心迁移（你手动） | 你掌控核心位置；Manual 优先级高于 Agent，战术自动适配 |

## 实时监控与迭代闭环

1. `play.py` 每 tick 把战况写进 `game.log`。
2. `meta/monitor.py` 解析日志算出 KPI 并检测瓶颈（idle 资源 / 单位拥堵 /
   护核失败 / 有敌未突袭 等）。
3. 遇到瓶颈或定时，多个**专家子代理**并行评审 `tactic.py` + 日志，提出改进。
4. 采纳改进 → 跑测试 → `git commit` + `git push` 到 GitHub。
5. 踩坑记入 `LESSONS.md`，避免重犯。
6. 周期性检查 SDK 版本与文档，及时更新；自动化未启用时由每轮审查显式执行。

## 运行

```bash
# 实时跑（需要 API key，默认连服务器；Manual 可随时干预）
uv run python play.py

# 监控战况
uv run python meta/monitor.py
uv run python meta/monitor.py --json
```

## 你（用户）的干预

- 你可直接迁移 Core 或消耗资源；移动成功后战术会清空旧勘探状态、重新扫新区块。
- Manual 指令优先级高于 Agent，所以你的干预不会被执行计划覆盖。
